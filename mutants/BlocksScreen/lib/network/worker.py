import asyncio
import fcntl
import ipaddress
import logging
import os
import socket as _socket
import struct
import threading
from uuid import uuid4

import sdbus
from configfile import get_configparser
from PyQt6.QtCore import QObject, pyqtSignal
from sdbus_async import networkmanager as dbus_nm

from .models import (
    ConnectionPriority,
    ConnectionResult,
    ConnectivityState,
    HotspotConfig,
    HotspotSecurity,
    NetworkInfo,
    NetworkState,
    NetworkStatus,
    SavedNetwork,
    SecurityType,
    VlanInfo,
    is_connectable_security,
    is_hidden_ssid,
)

logger = logging.getLogger(__name__)

_CAN_RELOAD_CONNECTIONS: bool = os.getuid() == 0

# Debounce window for coalescing rapid D-Bus signal bursts (seconds).
_DEBOUNCE_DELAY: float = 0.8
# Delay before restarting a failed signal listener (seconds).
_LISTENER_RESTART_DELAY: float = 3.0
# Timeout for _wait_for_connection: must cover 802.11 handshake + DHCP.
_WIFI_CONNECT_TIMEOUT: float = 20.0
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore


class NetworkManagerWorker(QObject):
    """Async NetworkManager worker (signal-reactive).

    Owns an asyncio event loop running on a dedicated daemon thread.
    All D-Bus operations execute as coroutines on that loop.

    Primary state updates are driven by D-Bus signals, not polling.
    """

    state_changed = pyqtSignal(NetworkState, name="stateChanged")
    networks_scanned = pyqtSignal(list, name="networksScanned")
    saved_networks_loaded = pyqtSignal(list, name="savedNetworksLoaded")
    connection_result = pyqtSignal(ConnectionResult, name="connectionResult")
    connectivity_changed = pyqtSignal(ConnectivityState, name="connectivityChanged")
    error_occurred = pyqtSignal(str, str, name="errorOccurred")
    hotspot_info_ready = pyqtSignal(str, str, str, name="hotspotInfoReady")
    reconnect_complete = pyqtSignal(name="reconnectComplete")

    _MAX_DBUS_ERRORS_BEFORE_RECONNECT: int = 3

    initialized = pyqtSignal(name="workerInitialized")

    def __init__(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ__init____mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ__init____mutmut_orig(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_1(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = None
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_2(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = True
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_3(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = ""

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_4(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = None
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_5(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = "XXXX"
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_6(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = None
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_7(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = "XXXX"
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_8(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = None
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_9(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = "XXXX"
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_10(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = None

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_11(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = "XXXX"

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_12(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = None

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_13(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = None
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_14(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = None
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_15(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = None
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_16(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = False
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_17(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = None
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_18(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = True
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_19(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = None

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_20(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 1

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_21(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = None

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_22(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = ""
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_23(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = ""
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_24(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = ""
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_25(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = ""

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_26(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = ""
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_27(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = ""

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_28(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = None

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_29(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = None
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_30(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = None
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_31(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = None
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_32(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=None,
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_33(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=None,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_34(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name=None,
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_35(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            daemon=True,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_36(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_37(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_38(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=False,
            name="NetworkManagerAsyncLoop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_39(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="XXNetworkManagerAsyncLoopXX",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_40(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="networkmanagerasyncloop",
        )
        self._asyncio_thread.start()

    def xǁNetworkManagerWorkerǁ__init____mutmut_41(self) -> None:
        """Initialise the worker, creating the asyncio loop and daemon thread.

        Sets up all instance state (interface paths, hotspot config, signal
        proxies, debounce handles) and immediately starts the asyncio daemon
        thread that opens the system D-Bus and drives all NetworkManager
        coroutines.
        """
        super().__init__()
        self._running: bool = False
        self._system_bus: sdbus.SdBus | None = None

        # Path strings only — read-proxies are always created fresh.
        self._primary_wifi_path: str = ""
        self._primary_wifi_iface: str = ""
        self._primary_wired_path: str = ""
        self._primary_wired_iface: str = ""

        self._iface_to_device_path: dict[str, str] = {}

        self._hotspot_config = HotspotConfig()
        self._load_hotspot_config()
        self._saved_cache: list[SavedNetwork] = []
        self._saved_cache_dirty: bool = True
        self._is_hotspot_active: bool = False
        self._consecutive_dbus_errors: int = 0

        self._background_tasks: set[asyncio.Task] = set()

        self._signal_nm: dbus_nm.NetworkManager | None = None
        self._signal_wifi: dbus_nm.NetworkDeviceWireless | None = None
        self._signal_wired: dbus_nm.NetworkDeviceGeneric | None = None
        self._signal_settings: dbus_nm.NetworkManagerSettings | None = None

        self._state_debounce_handle: asyncio.TimerHandle | None = None
        self._scan_debounce_handle: asyncio.TimerHandle | None = None

        # Tracked for cancellation during shutdown.
        self._listener_tasks: list[asyncio.Task] = []

        # Asyncio loop — created here, driven on the daemon thread.
        self.stop_event = asyncio.Event()
        self.stop_event.clear()
        self._asyncio_loop: asyncio.AbstractEventLoop = asyncio.new_event_loop()
        self._asyncio_thread = threading.Thread(
            target=self._run_asyncio_loop,
            daemon=True,
            name="NETWORKMANAGERASYNCLOOP",
        )
        self._asyncio_thread.start()
    
    xǁNetworkManagerWorkerǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ__init____mutmut_1': xǁNetworkManagerWorkerǁ__init____mutmut_1, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_2': xǁNetworkManagerWorkerǁ__init____mutmut_2, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_3': xǁNetworkManagerWorkerǁ__init____mutmut_3, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_4': xǁNetworkManagerWorkerǁ__init____mutmut_4, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_5': xǁNetworkManagerWorkerǁ__init____mutmut_5, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_6': xǁNetworkManagerWorkerǁ__init____mutmut_6, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_7': xǁNetworkManagerWorkerǁ__init____mutmut_7, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_8': xǁNetworkManagerWorkerǁ__init____mutmut_8, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_9': xǁNetworkManagerWorkerǁ__init____mutmut_9, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_10': xǁNetworkManagerWorkerǁ__init____mutmut_10, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_11': xǁNetworkManagerWorkerǁ__init____mutmut_11, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_12': xǁNetworkManagerWorkerǁ__init____mutmut_12, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_13': xǁNetworkManagerWorkerǁ__init____mutmut_13, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_14': xǁNetworkManagerWorkerǁ__init____mutmut_14, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_15': xǁNetworkManagerWorkerǁ__init____mutmut_15, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_16': xǁNetworkManagerWorkerǁ__init____mutmut_16, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_17': xǁNetworkManagerWorkerǁ__init____mutmut_17, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_18': xǁNetworkManagerWorkerǁ__init____mutmut_18, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_19': xǁNetworkManagerWorkerǁ__init____mutmut_19, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_20': xǁNetworkManagerWorkerǁ__init____mutmut_20, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_21': xǁNetworkManagerWorkerǁ__init____mutmut_21, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_22': xǁNetworkManagerWorkerǁ__init____mutmut_22, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_23': xǁNetworkManagerWorkerǁ__init____mutmut_23, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_24': xǁNetworkManagerWorkerǁ__init____mutmut_24, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_25': xǁNetworkManagerWorkerǁ__init____mutmut_25, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_26': xǁNetworkManagerWorkerǁ__init____mutmut_26, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_27': xǁNetworkManagerWorkerǁ__init____mutmut_27, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_28': xǁNetworkManagerWorkerǁ__init____mutmut_28, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_29': xǁNetworkManagerWorkerǁ__init____mutmut_29, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_30': xǁNetworkManagerWorkerǁ__init____mutmut_30, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_31': xǁNetworkManagerWorkerǁ__init____mutmut_31, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_32': xǁNetworkManagerWorkerǁ__init____mutmut_32, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_33': xǁNetworkManagerWorkerǁ__init____mutmut_33, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_34': xǁNetworkManagerWorkerǁ__init____mutmut_34, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_35': xǁNetworkManagerWorkerǁ__init____mutmut_35, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_36': xǁNetworkManagerWorkerǁ__init____mutmut_36, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_37': xǁNetworkManagerWorkerǁ__init____mutmut_37, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_38': xǁNetworkManagerWorkerǁ__init____mutmut_38, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_39': xǁNetworkManagerWorkerǁ__init____mutmut_39, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_40': xǁNetworkManagerWorkerǁ__init____mutmut_40, 
        'xǁNetworkManagerWorkerǁ__init____mutmut_41': xǁNetworkManagerWorkerǁ__init____mutmut_41
    }
    xǁNetworkManagerWorkerǁ__init____mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ__init__'

    def _run_asyncio_loop(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_orig(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_1(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(None)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_2(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = None
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_3(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(None)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_4(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                None
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_5(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(None, name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_6(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name=None)
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_7(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_8(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), )
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_9(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="XXnm_initXX")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_10(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="NM_INIT")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_11(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                None,
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_12(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                None,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_13(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_14(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_15(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "XXD-Bus opened on asyncio thread '%s'XX",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_16(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "d-bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_17(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-BUS OPENED ON ASYNCIO THREAD '%S'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_18(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error(None, exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_19(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", None)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_20(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error(exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_21(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("Failed to open system D-Bus: %s", )
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_22(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("XXFailed to open system D-Bus: %sXX", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_23(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("failed to open system d-bus: %s", exc)
        self._asyncio_loop.run_forever()

    def xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_24(self) -> None:
        """Open the system D-Bus and run the asyncio event loop on this thread."""
        asyncio.set_event_loop(self._asyncio_loop)
        try:
            self._system_bus = sdbus.sd_bus_open_system()
            sdbus.set_default_bus(self._system_bus)
            self._track_task(
                self._asyncio_loop.create_task(self._async_initialize(), name="nm_init")
            )
            logger.debug(
                "D-Bus opened on asyncio thread '%s'",
                threading.current_thread().name,
            )
        except Exception as exc:
            logger.error("FAILED TO OPEN SYSTEM D-BUS: %S", exc)
        self._asyncio_loop.run_forever()
    
    xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_1': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_2': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_3': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_4': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_5': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_6': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_7': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_8': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_9': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_10': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_11': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_12': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_13': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_14': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_15': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_16': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_17': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_18': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_19': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_20': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_21': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_22': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_23': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_24': xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_24
    }
    xǁNetworkManagerWorkerǁ_run_asyncio_loop__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_run_asyncio_loop'

    def _track_task(self, task: asyncio.Task) -> None:
        args = [task]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_track_task__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_track_task__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_track_task__mutmut_orig(self, task: asyncio.Task) -> None:
        """Register a background task so it is cancelled on shutdown."""
        self._background_tasks.add(task)
        task.add_done_callback(self._background_tasks.discard)

    def xǁNetworkManagerWorkerǁ_track_task__mutmut_1(self, task: asyncio.Task) -> None:
        """Register a background task so it is cancelled on shutdown."""
        self._background_tasks.add(None)
        task.add_done_callback(self._background_tasks.discard)

    def xǁNetworkManagerWorkerǁ_track_task__mutmut_2(self, task: asyncio.Task) -> None:
        """Register a background task so it is cancelled on shutdown."""
        self._background_tasks.add(task)
        task.add_done_callback(None)
    
    xǁNetworkManagerWorkerǁ_track_task__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_track_task__mutmut_1': xǁNetworkManagerWorkerǁ_track_task__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_track_task__mutmut_2': xǁNetworkManagerWorkerǁ_track_task__mutmut_2
    }
    xǁNetworkManagerWorkerǁ_track_task__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_track_task'

    async def _async_shutdown(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_orig(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_1(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = None

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_2(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = True

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_3(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_4(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = ""
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_5(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = ""

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_6(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = ""
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_7(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = ""
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_8(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = ""
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_9(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = ""

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_10(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = None
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_11(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = "XXXX"
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_12(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = None
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_13(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = "XXXX"
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_14(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = None
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_15(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = "XXXX"
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_16(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = None
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_17(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = "XXXX"
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_18(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(None):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_19(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_20(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = ""
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_21(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info(None)
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_22(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("XXNetworkManagerWorker async shutdown completeXX")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_23(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("networkmanagerworker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_24(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NETWORKMANAGERWORKER ASYNC SHUTDOWN COMPLETE")
        self._asyncio_loop.call_soon_threadsafe(self._asyncio_loop.stop)

    async def xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_25(self) -> None:
        """Tear down all async state and stop the event loop."""
        self._running = False

        for task in self._listener_tasks:
            if not task.done():
                task.cancel()
        self._listener_tasks.clear()

        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
            self._state_debounce_handle = None
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
            self._scan_debounce_handle = None

        self._signal_nm = None
        self._signal_wifi = None
        self._signal_wired = None
        self._signal_settings = None

        self._primary_wifi_path = ""
        self._primary_wifi_iface = ""
        self._primary_wired_path = ""
        self._primary_wired_iface = ""
        self._iface_to_device_path.clear()
        self._saved_cache.clear()

        for task in list(self._background_tasks):
            if not task.done():
                task.cancel()
        self._background_tasks.clear()
        self._system_bus = None
        logger.info("NetworkManagerWorker async shutdown complete")
        self._asyncio_loop.call_soon_threadsafe(None)
    
    xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_1': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_2': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_3': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_4': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_5': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_6': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_7': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_8': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_9': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_10': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_11': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_12': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_13': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_14': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_15': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_16': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_17': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_18': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_19': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_20': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_21': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_22': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_23': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_24': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_25': xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_25
    }
    xǁNetworkManagerWorkerǁ_async_shutdown__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_shutdown'

    def _nm(self) -> dbus_nm.NetworkManager:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_nm__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_nm__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_nm__mutmut_orig(self) -> dbus_nm.NetworkManager:
        """Return a fresh NetworkManager root D-Bus proxy."""
        return dbus_nm.NetworkManager(bus=self._system_bus)

    def xǁNetworkManagerWorkerǁ_nm__mutmut_1(self) -> dbus_nm.NetworkManager:
        """Return a fresh NetworkManager root D-Bus proxy."""
        return dbus_nm.NetworkManager(bus=None)
    
    xǁNetworkManagerWorkerǁ_nm__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_nm__mutmut_1': xǁNetworkManagerWorkerǁ_nm__mutmut_1
    }
    xǁNetworkManagerWorkerǁ_nm__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_nm'

    def _generic(self, path: str) -> dbus_nm.NetworkDeviceGeneric:
        args = [path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_generic__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_generic__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_generic__mutmut_orig(self, path: str) -> dbus_nm.NetworkDeviceGeneric:
        """Return a fresh generic network device D-Bus proxy for the given path."""
        return dbus_nm.NetworkDeviceGeneric(bus=self._system_bus, device_path=path)

    def xǁNetworkManagerWorkerǁ_generic__mutmut_1(self, path: str) -> dbus_nm.NetworkDeviceGeneric:
        """Return a fresh generic network device D-Bus proxy for the given path."""
        return dbus_nm.NetworkDeviceGeneric(bus=None, device_path=path)

    def xǁNetworkManagerWorkerǁ_generic__mutmut_2(self, path: str) -> dbus_nm.NetworkDeviceGeneric:
        """Return a fresh generic network device D-Bus proxy for the given path."""
        return dbus_nm.NetworkDeviceGeneric(bus=self._system_bus, device_path=None)

    def xǁNetworkManagerWorkerǁ_generic__mutmut_3(self, path: str) -> dbus_nm.NetworkDeviceGeneric:
        """Return a fresh generic network device D-Bus proxy for the given path."""
        return dbus_nm.NetworkDeviceGeneric(device_path=path)

    def xǁNetworkManagerWorkerǁ_generic__mutmut_4(self, path: str) -> dbus_nm.NetworkDeviceGeneric:
        """Return a fresh generic network device D-Bus proxy for the given path."""
        return dbus_nm.NetworkDeviceGeneric(bus=self._system_bus, )
    
    xǁNetworkManagerWorkerǁ_generic__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_generic__mutmut_1': xǁNetworkManagerWorkerǁ_generic__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_generic__mutmut_2': xǁNetworkManagerWorkerǁ_generic__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_generic__mutmut_3': xǁNetworkManagerWorkerǁ_generic__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_generic__mutmut_4': xǁNetworkManagerWorkerǁ_generic__mutmut_4
    }
    xǁNetworkManagerWorkerǁ_generic__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_generic'

    def _wifi(self, path: str | None = None) -> dbus_nm.NetworkDeviceWireless:
        args = [path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wifi__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wifi__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_wifi__mutmut_orig(self, path: str | None = None) -> dbus_nm.NetworkDeviceWireless:
        """Return a fresh wireless device D-Bus proxy (defaults to primary Wi-Fi path)."""
        return dbus_nm.NetworkDeviceWireless(
            bus=self._system_bus,
            device_path=path or self._primary_wifi_path,
        )

    def xǁNetworkManagerWorkerǁ_wifi__mutmut_1(self, path: str | None = None) -> dbus_nm.NetworkDeviceWireless:
        """Return a fresh wireless device D-Bus proxy (defaults to primary Wi-Fi path)."""
        return dbus_nm.NetworkDeviceWireless(
            bus=None,
            device_path=path or self._primary_wifi_path,
        )

    def xǁNetworkManagerWorkerǁ_wifi__mutmut_2(self, path: str | None = None) -> dbus_nm.NetworkDeviceWireless:
        """Return a fresh wireless device D-Bus proxy (defaults to primary Wi-Fi path)."""
        return dbus_nm.NetworkDeviceWireless(
            bus=self._system_bus,
            device_path=None,
        )

    def xǁNetworkManagerWorkerǁ_wifi__mutmut_3(self, path: str | None = None) -> dbus_nm.NetworkDeviceWireless:
        """Return a fresh wireless device D-Bus proxy (defaults to primary Wi-Fi path)."""
        return dbus_nm.NetworkDeviceWireless(
            device_path=path or self._primary_wifi_path,
        )

    def xǁNetworkManagerWorkerǁ_wifi__mutmut_4(self, path: str | None = None) -> dbus_nm.NetworkDeviceWireless:
        """Return a fresh wireless device D-Bus proxy (defaults to primary Wi-Fi path)."""
        return dbus_nm.NetworkDeviceWireless(
            bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_wifi__mutmut_5(self, path: str | None = None) -> dbus_nm.NetworkDeviceWireless:
        """Return a fresh wireless device D-Bus proxy (defaults to primary Wi-Fi path)."""
        return dbus_nm.NetworkDeviceWireless(
            bus=self._system_bus,
            device_path=path and self._primary_wifi_path,
        )
    
    xǁNetworkManagerWorkerǁ_wifi__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_wifi__mutmut_1': xǁNetworkManagerWorkerǁ_wifi__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_wifi__mutmut_2': xǁNetworkManagerWorkerǁ_wifi__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_wifi__mutmut_3': xǁNetworkManagerWorkerǁ_wifi__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_wifi__mutmut_4': xǁNetworkManagerWorkerǁ_wifi__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_wifi__mutmut_5': xǁNetworkManagerWorkerǁ_wifi__mutmut_5
    }
    xǁNetworkManagerWorkerǁ_wifi__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_wifi'

    def _wired(self, path: str | None = None) -> dbus_nm.NetworkDeviceWired:
        args = [path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wired__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wired__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_wired__mutmut_orig(self, path: str | None = None) -> dbus_nm.NetworkDeviceWired:
        """Return a fresh wired device D-Bus proxy (defaults to primary wired path)."""
        return dbus_nm.NetworkDeviceWired(
            bus=self._system_bus,
            device_path=path or self._primary_wired_path,
        )

    def xǁNetworkManagerWorkerǁ_wired__mutmut_1(self, path: str | None = None) -> dbus_nm.NetworkDeviceWired:
        """Return a fresh wired device D-Bus proxy (defaults to primary wired path)."""
        return dbus_nm.NetworkDeviceWired(
            bus=None,
            device_path=path or self._primary_wired_path,
        )

    def xǁNetworkManagerWorkerǁ_wired__mutmut_2(self, path: str | None = None) -> dbus_nm.NetworkDeviceWired:
        """Return a fresh wired device D-Bus proxy (defaults to primary wired path)."""
        return dbus_nm.NetworkDeviceWired(
            bus=self._system_bus,
            device_path=None,
        )

    def xǁNetworkManagerWorkerǁ_wired__mutmut_3(self, path: str | None = None) -> dbus_nm.NetworkDeviceWired:
        """Return a fresh wired device D-Bus proxy (defaults to primary wired path)."""
        return dbus_nm.NetworkDeviceWired(
            device_path=path or self._primary_wired_path,
        )

    def xǁNetworkManagerWorkerǁ_wired__mutmut_4(self, path: str | None = None) -> dbus_nm.NetworkDeviceWired:
        """Return a fresh wired device D-Bus proxy (defaults to primary wired path)."""
        return dbus_nm.NetworkDeviceWired(
            bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_wired__mutmut_5(self, path: str | None = None) -> dbus_nm.NetworkDeviceWired:
        """Return a fresh wired device D-Bus proxy (defaults to primary wired path)."""
        return dbus_nm.NetworkDeviceWired(
            bus=self._system_bus,
            device_path=path and self._primary_wired_path,
        )
    
    xǁNetworkManagerWorkerǁ_wired__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_wired__mutmut_1': xǁNetworkManagerWorkerǁ_wired__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_wired__mutmut_2': xǁNetworkManagerWorkerǁ_wired__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_wired__mutmut_3': xǁNetworkManagerWorkerǁ_wired__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_wired__mutmut_4': xǁNetworkManagerWorkerǁ_wired__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_wired__mutmut_5': xǁNetworkManagerWorkerǁ_wired__mutmut_5
    }
    xǁNetworkManagerWorkerǁ_wired__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_wired'

    def _get_wifi_iface_name(self) -> str:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_orig(self) -> str:
        """Return the detected Wi-Fi interface name.

        ``_primary_wifi_iface`` is set atomically with ``_primary_wifi_path``
        in ``_detect_interfaces()``.  The dict-lookup and ``"wlan0"`` branches
        are defensive fallbacks in case the two somehow diverge.
        """
        if self._primary_wifi_iface:
            return self._primary_wifi_iface
        for iface, path in self._iface_to_device_path.items():
            if path == self._primary_wifi_path:
                return iface
        return "wlan0"  # safe fallback

    def xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_1(self) -> str:
        """Return the detected Wi-Fi interface name.

        ``_primary_wifi_iface`` is set atomically with ``_primary_wifi_path``
        in ``_detect_interfaces()``.  The dict-lookup and ``"wlan0"`` branches
        are defensive fallbacks in case the two somehow diverge.
        """
        if self._primary_wifi_iface:
            return self._primary_wifi_iface
        for iface, path in self._iface_to_device_path.items():
            if path != self._primary_wifi_path:
                return iface
        return "wlan0"  # safe fallback

    def xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_2(self) -> str:
        """Return the detected Wi-Fi interface name.

        ``_primary_wifi_iface`` is set atomically with ``_primary_wifi_path``
        in ``_detect_interfaces()``.  The dict-lookup and ``"wlan0"`` branches
        are defensive fallbacks in case the two somehow diverge.
        """
        if self._primary_wifi_iface:
            return self._primary_wifi_iface
        for iface, path in self._iface_to_device_path.items():
            if path == self._primary_wifi_path:
                return iface
        return "XXwlan0XX"  # safe fallback

    def xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_3(self) -> str:
        """Return the detected Wi-Fi interface name.

        ``_primary_wifi_iface`` is set atomically with ``_primary_wifi_path``
        in ``_detect_interfaces()``.  The dict-lookup and ``"wlan0"`` branches
        are defensive fallbacks in case the two somehow diverge.
        """
        if self._primary_wifi_iface:
            return self._primary_wifi_iface
        for iface, path in self._iface_to_device_path.items():
            if path == self._primary_wifi_path:
                return iface
        return "WLAN0"  # safe fallback
    
    xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_1': xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_2': xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_3': xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_3
    }
    xǁNetworkManagerWorkerǁ_get_wifi_iface_name__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_wifi_iface_name'

    def _active_conn(self, path: str) -> dbus_nm.ActiveConnection:
        args = [path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_active_conn__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_active_conn__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_active_conn__mutmut_orig(self, path: str) -> dbus_nm.ActiveConnection:
        """Return a fresh ActiveConnection D-Bus proxy for the given path."""
        return dbus_nm.ActiveConnection(bus=self._system_bus, connection_path=path)

    def xǁNetworkManagerWorkerǁ_active_conn__mutmut_1(self, path: str) -> dbus_nm.ActiveConnection:
        """Return a fresh ActiveConnection D-Bus proxy for the given path."""
        return dbus_nm.ActiveConnection(bus=None, connection_path=path)

    def xǁNetworkManagerWorkerǁ_active_conn__mutmut_2(self, path: str) -> dbus_nm.ActiveConnection:
        """Return a fresh ActiveConnection D-Bus proxy for the given path."""
        return dbus_nm.ActiveConnection(bus=self._system_bus, connection_path=None)

    def xǁNetworkManagerWorkerǁ_active_conn__mutmut_3(self, path: str) -> dbus_nm.ActiveConnection:
        """Return a fresh ActiveConnection D-Bus proxy for the given path."""
        return dbus_nm.ActiveConnection(connection_path=path)

    def xǁNetworkManagerWorkerǁ_active_conn__mutmut_4(self, path: str) -> dbus_nm.ActiveConnection:
        """Return a fresh ActiveConnection D-Bus proxy for the given path."""
        return dbus_nm.ActiveConnection(bus=self._system_bus, )
    
    xǁNetworkManagerWorkerǁ_active_conn__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_active_conn__mutmut_1': xǁNetworkManagerWorkerǁ_active_conn__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_active_conn__mutmut_2': xǁNetworkManagerWorkerǁ_active_conn__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_active_conn__mutmut_3': xǁNetworkManagerWorkerǁ_active_conn__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_active_conn__mutmut_4': xǁNetworkManagerWorkerǁ_active_conn__mutmut_4
    }
    xǁNetworkManagerWorkerǁ_active_conn__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_active_conn'

    def _conn_settings(self, path: str) -> dbus_nm.NetworkConnectionSettings:
        args = [path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_conn_settings__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_conn_settings__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_conn_settings__mutmut_orig(self, path: str) -> dbus_nm.NetworkConnectionSettings:
        """Return a fresh NetworkConnectionSettings D-Bus proxy for the given path."""
        return dbus_nm.NetworkConnectionSettings(
            bus=self._system_bus, settings_path=path
        )

    def xǁNetworkManagerWorkerǁ_conn_settings__mutmut_1(self, path: str) -> dbus_nm.NetworkConnectionSettings:
        """Return a fresh NetworkConnectionSettings D-Bus proxy for the given path."""
        return dbus_nm.NetworkConnectionSettings(
            bus=None, settings_path=path
        )

    def xǁNetworkManagerWorkerǁ_conn_settings__mutmut_2(self, path: str) -> dbus_nm.NetworkConnectionSettings:
        """Return a fresh NetworkConnectionSettings D-Bus proxy for the given path."""
        return dbus_nm.NetworkConnectionSettings(
            bus=self._system_bus, settings_path=None
        )

    def xǁNetworkManagerWorkerǁ_conn_settings__mutmut_3(self, path: str) -> dbus_nm.NetworkConnectionSettings:
        """Return a fresh NetworkConnectionSettings D-Bus proxy for the given path."""
        return dbus_nm.NetworkConnectionSettings(
            settings_path=path
        )

    def xǁNetworkManagerWorkerǁ_conn_settings__mutmut_4(self, path: str) -> dbus_nm.NetworkConnectionSettings:
        """Return a fresh NetworkConnectionSettings D-Bus proxy for the given path."""
        return dbus_nm.NetworkConnectionSettings(
            bus=self._system_bus, )
    
    xǁNetworkManagerWorkerǁ_conn_settings__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_conn_settings__mutmut_1': xǁNetworkManagerWorkerǁ_conn_settings__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_conn_settings__mutmut_2': xǁNetworkManagerWorkerǁ_conn_settings__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_conn_settings__mutmut_3': xǁNetworkManagerWorkerǁ_conn_settings__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_conn_settings__mutmut_4': xǁNetworkManagerWorkerǁ_conn_settings__mutmut_4
    }
    xǁNetworkManagerWorkerǁ_conn_settings__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_conn_settings'

    def _nm_settings(self) -> dbus_nm.NetworkManagerSettings:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_nm_settings__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_nm_settings__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_nm_settings__mutmut_orig(self) -> dbus_nm.NetworkManagerSettings:
        """Return a fresh NetworkManagerSettings D-Bus proxy."""
        return dbus_nm.NetworkManagerSettings(bus=self._system_bus)

    def xǁNetworkManagerWorkerǁ_nm_settings__mutmut_1(self) -> dbus_nm.NetworkManagerSettings:
        """Return a fresh NetworkManagerSettings D-Bus proxy."""
        return dbus_nm.NetworkManagerSettings(bus=None)
    
    xǁNetworkManagerWorkerǁ_nm_settings__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_nm_settings__mutmut_1': xǁNetworkManagerWorkerǁ_nm_settings__mutmut_1
    }
    xǁNetworkManagerWorkerǁ_nm_settings__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_nm_settings'

    def _ap(self, path: str) -> dbus_nm.AccessPoint:
        args = [path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ap__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ap__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_ap__mutmut_orig(self, path: str) -> dbus_nm.AccessPoint:
        """Return a fresh AccessPoint D-Bus proxy for the given path."""
        return dbus_nm.AccessPoint(bus=self._system_bus, point_path=path)

    def xǁNetworkManagerWorkerǁ_ap__mutmut_1(self, path: str) -> dbus_nm.AccessPoint:
        """Return a fresh AccessPoint D-Bus proxy for the given path."""
        return dbus_nm.AccessPoint(bus=None, point_path=path)

    def xǁNetworkManagerWorkerǁ_ap__mutmut_2(self, path: str) -> dbus_nm.AccessPoint:
        """Return a fresh AccessPoint D-Bus proxy for the given path."""
        return dbus_nm.AccessPoint(bus=self._system_bus, point_path=None)

    def xǁNetworkManagerWorkerǁ_ap__mutmut_3(self, path: str) -> dbus_nm.AccessPoint:
        """Return a fresh AccessPoint D-Bus proxy for the given path."""
        return dbus_nm.AccessPoint(point_path=path)

    def xǁNetworkManagerWorkerǁ_ap__mutmut_4(self, path: str) -> dbus_nm.AccessPoint:
        """Return a fresh AccessPoint D-Bus proxy for the given path."""
        return dbus_nm.AccessPoint(bus=self._system_bus, )
    
    xǁNetworkManagerWorkerǁ_ap__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_ap__mutmut_1': xǁNetworkManagerWorkerǁ_ap__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_ap__mutmut_2': xǁNetworkManagerWorkerǁ_ap__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_ap__mutmut_3': xǁNetworkManagerWorkerǁ_ap__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_ap__mutmut_4': xǁNetworkManagerWorkerǁ_ap__mutmut_4
    }
    xǁNetworkManagerWorkerǁ_ap__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_ap'

    def _ipv4(self, path: str) -> dbus_nm.IPv4Config:
        args = [path]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ipv4__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ipv4__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_ipv4__mutmut_orig(self, path: str) -> dbus_nm.IPv4Config:
        """Return a fresh IPv4Config D-Bus proxy for the given path."""
        return dbus_nm.IPv4Config(bus=self._system_bus, ip4_path=path)

    def xǁNetworkManagerWorkerǁ_ipv4__mutmut_1(self, path: str) -> dbus_nm.IPv4Config:
        """Return a fresh IPv4Config D-Bus proxy for the given path."""
        return dbus_nm.IPv4Config(bus=None, ip4_path=path)

    def xǁNetworkManagerWorkerǁ_ipv4__mutmut_2(self, path: str) -> dbus_nm.IPv4Config:
        """Return a fresh IPv4Config D-Bus proxy for the given path."""
        return dbus_nm.IPv4Config(bus=self._system_bus, ip4_path=None)

    def xǁNetworkManagerWorkerǁ_ipv4__mutmut_3(self, path: str) -> dbus_nm.IPv4Config:
        """Return a fresh IPv4Config D-Bus proxy for the given path."""
        return dbus_nm.IPv4Config(ip4_path=path)

    def xǁNetworkManagerWorkerǁ_ipv4__mutmut_4(self, path: str) -> dbus_nm.IPv4Config:
        """Return a fresh IPv4Config D-Bus proxy for the given path."""
        return dbus_nm.IPv4Config(bus=self._system_bus, )
    
    xǁNetworkManagerWorkerǁ_ipv4__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_ipv4__mutmut_1': xǁNetworkManagerWorkerǁ_ipv4__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_ipv4__mutmut_2': xǁNetworkManagerWorkerǁ_ipv4__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_ipv4__mutmut_3': xǁNetworkManagerWorkerǁ_ipv4__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_ipv4__mutmut_4': xǁNetworkManagerWorkerǁ_ipv4__mutmut_4
    }
    xǁNetworkManagerWorkerǁ_ipv4__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_ipv4'

    def _ensure_signal_proxies(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_orig(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_1(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is not None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_2(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = None

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_3(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=None)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_4(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None or self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_5(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is not None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_6(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = None

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_7(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=None,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_8(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=None,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_9(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_10(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_11(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None or self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_12(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is not None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_13(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = None

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_14(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=None,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_15(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=None,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_16(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_17(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_18(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is not None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=self._system_bus,
            )

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_19(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = None

    def xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_20(self) -> None:
        """Create or recreate persistent proxies for D-Bus signal listening.

        These proxies are NOT used for property reads (to avoid the
        sdbus_async caching bug).  They exist solely so the ``async for``
        signal iterators stay alive.  Must be called on the asyncio thread.
        """
        if self._signal_nm is None:
            self._signal_nm = dbus_nm.NetworkManager(bus=self._system_bus)

        if self._signal_wifi is None and self._primary_wifi_path:
            self._signal_wifi = dbus_nm.NetworkDeviceWireless(
                bus=self._system_bus,
                device_path=self._primary_wifi_path,
            )

        if self._signal_wired is None and self._primary_wired_path:
            self._signal_wired = dbus_nm.NetworkDeviceGeneric(
                bus=self._system_bus,
                device_path=self._primary_wired_path,
            )

        if self._signal_settings is None:
            self._signal_settings = dbus_nm.NetworkManagerSettings(
                bus=None,
            )
    
    xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_1': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_2': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_3': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_4': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_5': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_6': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_7': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_8': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_9': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_10': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_11': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_12': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_13': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_14': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_15': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_16': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_17': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_18': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_19': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_20': xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_20
    }
    xǁNetworkManagerWorkerǁ_ensure_signal_proxies__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_ensure_signal_proxies'

    def get_ip_by_interface(self, interface: str = "wlan0") -> str:
        args = [interface]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_orig(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), self._asyncio_loop
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_1(self, interface: str = "XXwlan0XX") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), self._asyncio_loop
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_2(self, interface: str = "WLAN0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), self._asyncio_loop
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_3(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = None
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_4(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            None, self._asyncio_loop
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_5(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), None
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_6(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._asyncio_loop
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_7(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_8(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(None), self._asyncio_loop
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_9(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), self._asyncio_loop
        )
        try:
            return future.result(timeout=None)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_10(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), self._asyncio_loop
        )
        try:
            return future.result(timeout=6.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return ""

    def xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_11(self, interface: str = "wlan0") -> str:
        """Return the current IPv4 address for *interface*, blocking up to 5 s."""
        future = asyncio.run_coroutine_threadsafe(
            self._get_ip_by_interface(interface), self._asyncio_loop
        )
        try:
            return future.result(timeout=5.0)
        except Exception:
            # Timeout or cancellation from the async loop; caller treats "" as unknown.
            return "XXXX"
    
    xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_1': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_1, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_2': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_2, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_3': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_3, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_4': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_4, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_5': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_5, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_6': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_6, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_7': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_7, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_8': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_8, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_9': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_9, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_10': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_10, 
        'xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_11': xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_11
    }
    xǁNetworkManagerWorkerǁget_ip_by_interface__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁget_ip_by_interface'

    @property
    def hotspot_ssid(self) -> str:
        """The SSID configured for the hotspot."""
        return self._hotspot_config.ssid

    @property
    def hotspot_password(self) -> str:
        """The password configured for the hotspot."""
        return self._hotspot_config.password

    async def _async_initialize(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_orig(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_1(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_2(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit(None, "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_3(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", None)
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_4(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_5(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", )
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_6(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("XXinitializeXX", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_7(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("INITIALIZE", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_8(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "XXNo D-Bus connectionXX")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_9(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "no d-bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_10(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "NO D-BUS CONNECTION")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_11(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = None
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_12(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = False
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_13(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                None,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_14(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                None,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_15(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                None,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_16(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_17(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_18(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_19(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan(None)
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_20(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug(None, exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_21(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", None)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_22(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug(exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_23(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", )

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_24(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("XXInitial Wi-Fi scan request ignored: %sXX", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_25(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("initial wi-fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_26(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("INITIAL WI-FI SCAN REQUEST IGNORED: %S", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_27(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                None,
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_28(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                None,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_29(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_30(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_31(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "XXNetworkManagerWorker initialised on thread '%s' XX"
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_32(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "networkmanagerworker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_33(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NETWORKMANAGERWORKER INITIALISED ON THREAD '%S' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_34(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "XX(sdbus_async, signal-reactive)XX",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_35(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(SDBUS_ASYNC, SIGNAL-REACTIVE)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_36(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception(None)
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_37(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("XXFailed to initialise NetworkManagerWorkerXX")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_38(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("failed to initialise networkmanagerworker")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_39(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("FAILED TO INITIALISE NETWORKMANAGERWORKER")
            self.error_occurred.emit("initialize", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_40(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit(None, str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_41(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", None)
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_42(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit(str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_43(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", )
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_44(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("XXinitializeXX", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_45(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("INITIALIZE", str(exc))
            self.initialized.emit()

    async def xǁNetworkManagerWorkerǁ_async_initialize__mutmut_46(self) -> None:
        """Bootstrap the worker on the asyncio thread.

        Detects network interfaces, enforces the boot-time ethernet/Wi-Fi
        mutual exclusion, activates any saved VLANs if ethernet is present,
        triggers an initial Wi-Fi scan, and starts all D-Bus signal listeners.
        Emits ``initialized`` when done (even on failure, so the manager can
        unblock its caller).
        """
        try:
            if not self._system_bus:
                self.error_occurred.emit("initialize", "No D-Bus connection")
                return

            self._running = True
            await self._detect_interfaces()
            await self._enforce_boot_mutual_exclusion()

            if await self._is_ethernet_connected():
                await self._activate_saved_vlans()

            self.hotspot_info_ready.emit(
                self._hotspot_config.ssid,
                self._hotspot_config.password,
                self._hotspot_config.security,
            )

            if self._primary_wifi_path:
                try:
                    await self._wifi().request_scan({})
                except Exception as exc:
                    logger.debug("Initial Wi-Fi scan request ignored: %s", exc)

            await self._start_signal_listeners()

            logger.info(
                "NetworkManagerWorker initialised on thread '%s' "
                "(sdbus_async, signal-reactive)",
                threading.current_thread().name,
            )
            self.initialized.emit()
        except Exception as exc:
            logger.exception("Failed to initialise NetworkManagerWorker")
            self.error_occurred.emit("initialize", str(None))
            self.initialized.emit()
    
    xǁNetworkManagerWorkerǁ_async_initialize__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_1': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_2': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_3': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_4': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_5': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_6': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_7': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_8': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_9': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_10': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_11': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_12': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_13': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_14': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_15': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_16': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_17': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_18': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_19': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_20': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_21': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_22': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_23': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_24': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_25': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_26': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_27': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_28': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_29': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_30': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_31': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_32': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_33': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_34': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_35': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_36': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_37': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_38': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_39': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_40': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_41': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_42': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_43': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_44': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_45': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_initialize__mutmut_46': xǁNetworkManagerWorkerǁ_async_initialize__mutmut_46
    }
    xǁNetworkManagerWorkerǁ_async_initialize__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_initialize'

    async def _detect_interfaces(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_orig(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_1(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = None
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_2(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = None
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_3(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(None)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_4(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = None
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_5(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = None
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_6(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(None).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_7(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = None

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_8(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI or not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_9(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type != dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_10(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_11(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = None
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_12(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = None
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_13(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET or not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_14(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type != dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_15(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_16(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = None
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_17(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = None
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_18(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error(None, exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_19(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", None)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_20(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error(exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_21(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", )

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_22(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("XXFailed to detect interfaces: %sXX", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_23(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_24(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("FAILED TO DETECT INTERFACES: %S", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_25(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path or not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_26(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_27(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_28(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning(None)
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_29(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("XXNo network interfaces detected after scanXX")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_30(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("no network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_31(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("NO NETWORK INTERFACES DETECTED AFTER SCAN")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_32(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit(None, "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_33(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", None)
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_34(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_35(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", )
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_36(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("XXwifi_unavailableXX", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_37(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("WIFI_UNAVAILABLE", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_38(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "XXNo network device foundXX")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_39(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "no network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_40(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "NO NETWORK DEVICE FOUND")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_41(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("No Wi-Fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_42(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning(None)

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_43(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("XXNo Wi-Fi interface detected; ethernet-only modeXX")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_44(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("no wi-fi interface detected; ethernet-only mode")

    async def xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_45(self) -> None:
        """Enumerate NM devices and record the primary Wi-Fi and Ethernet paths.

        Iterates all NetworkManager devices, maps interface names to D-Bus
        object paths, and stores the first WIFI and ETHERNET device found as
        the primary interfaces used for all subsequent operations.  Emits
        ``error_occurred`` if no interfaces at all are found.
        """
        try:
            devices = await self._nm().get_devices()
            for device_path in devices:
                device = self._generic(device_path)
                device_type = await device.device_type
                iface_name = await self._generic(device_path).interface
                if iface_name:
                    self._iface_to_device_path[iface_name] = device_path

                if (
                    device_type == dbus_nm.enums.DeviceType.WIFI
                    and not self._primary_wifi_path
                ):
                    self._primary_wifi_path = device_path
                    self._primary_wifi_iface = iface_name
                elif (
                    device_type == dbus_nm.enums.DeviceType.ETHERNET
                    and not self._primary_wired_path
                ):
                    self._primary_wired_path = device_path
                    self._primary_wired_iface = iface_name
        except Exception as exc:
            logger.error("Failed to detect interfaces: %s", exc)

        if not self._primary_wifi_path and not self._primary_wired_path:
            # Both absent — likely D-Bus not ready yet or no hardware present.
            logger.warning("No network interfaces detected after scan")
            self.error_occurred.emit("wifi_unavailable", "No network device found")
        elif not self._primary_wifi_path:
            # Ethernet-only or Wi-Fi driver still loading — log but don't alarm.
            logger.warning("NO WI-FI INTERFACE DETECTED; ETHERNET-ONLY MODE")
    
    xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_1': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_2': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_3': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_4': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_5': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_6': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_7': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_8': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_9': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_10': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_11': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_12': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_13': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_14': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_15': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_16': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_17': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_18': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_19': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_20': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_21': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_22': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_23': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_24': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_25': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_26': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_27': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_28': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_29': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_30': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_31': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_32': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_33': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_34': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_35': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_36': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_37': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_38': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_39': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_40': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_41': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_42': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_43': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_44': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_45': xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_45
    }
    xǁNetworkManagerWorkerǁ_detect_interfaces__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_detect_interfaces'

    async def _enforce_boot_mutual_exclusion(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_orig(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_1(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_2(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_3(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info(None)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_4(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("XXBoot: ethernet active + Wi-Fi enabled — disabling Wi-FiXX")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_5(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("boot: ethernet active + wi-fi enabled — disabling wi-fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_6(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("BOOT: ETHERNET ACTIVE + WI-FI ENABLED — DISABLING WI-FI")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_7(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(None, exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_8(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", None)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_9(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_10(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", )
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_11(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("XXPre-radio-disable disconnect ignored: %sXX", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_12(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_13(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("PRE-RADIO-DISABLE DISCONNECT IGNORED: %S", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_14(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(None)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_15(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(True)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_16(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(None, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_17(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=None)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_18(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_19(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, )
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_20(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(True, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_21(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=9.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_22(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = None
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_23(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = True
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_24(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning(None, exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_25(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", None)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_26(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning(exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_27(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("Boot mutual exclusion failed (non-fatal): %s", )

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_28(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("XXBoot mutual exclusion failed (non-fatal): %sXX", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_29(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("boot mutual exclusion failed (non-fatal): %s", exc)

    async def xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_30(self) -> None:
        """Disable Wi-Fi at boot if ethernet is already connected.

        Prevents the device from simultaneously using both interfaces at
        startup.  If ethernet is active and the Wi-Fi radio is on, the Wi-Fi
        device is disconnected and the radio is disabled, then we wait up to
        8 s for the radio to confirm it is off.  Failures are logged but not
        propagated — a non-fatal best-effort action at boot.
        """
        try:
            if not await self._is_ethernet_connected():
                return
            if not await self._nm().wireless_enabled:
                return
            logger.info("Boot: ethernet active + Wi-Fi enabled — disabling Wi-Fi")
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-radio-disable disconnect ignored: %s", exc)
            await self._nm().wireless_enabled.set_async(False)
            await self._wait_for_wifi_radio(False, timeout=8.0)
            self._is_hotspot_active = False
        except Exception as exc:
            logger.warning("BOOT MUTUAL EXCLUSION FAILED (NON-FATAL): %S", exc)
    
    xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_1': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_2': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_3': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_4': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_5': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_6': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_7': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_8': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_9': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_10': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_11': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_12': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_13': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_14': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_15': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_16': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_17': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_18': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_19': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_20': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_21': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_22': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_23': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_24': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_25': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_26': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_27': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_28': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_29': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_30': xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_30
    }
    xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_enforce_boot_mutual_exclusion'

    async def _start_signal_listeners(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_orig(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_1(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = None

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_2(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("XXnm_stateXX", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_3(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("NM_STATE", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_4(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("XXwifi_ap_addedXX", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_5(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("WIFI_AP_ADDED", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_6(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("XXwifi_ap_removedXX", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_7(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("WIFI_AP_REMOVED", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_8(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("XXwired_stateXX", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_9(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("WIRED_STATE", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_10(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("XXwifi_stateXX", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_11(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("WIFI_STATE", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_12(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("XXsettings_conn_addedXX", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_13(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("SETTINGS_CONN_ADDED", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_14(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("XXsettings_conn_removedXX", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_15(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("SETTINGS_CONN_REMOVED", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_16(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = None
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_17(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                None,
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_18(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=None,
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_19(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_20(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_21(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(None, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_22(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, None),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_23(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_24(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, ),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_25(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(None)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_26(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(None)

        logger.info("Started %d D-Bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_27(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info(None, len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_28(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", None)

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_29(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info(len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_30(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("Started %d D-Bus signal listeners", )

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_31(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("XXStarted %d D-Bus signal listenersXX", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_32(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("started %d d-bus signal listeners", len(self._listener_tasks))

    async def xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_33(self) -> None:
        """Create persistent proxies and spawn all D-Bus signal listeners.

        Each listener runs in its own Task and automatically restarts
        after transient errors (with a back-off delay).
        """
        self._ensure_signal_proxies()

        listeners = [
            ("nm_state", self._listen_nm_state_changed),
            ("wifi_ap_added", self._listen_ap_added),
            ("wifi_ap_removed", self._listen_ap_removed),
            ("wired_state", self._listen_wired_state_changed),
            ("wifi_state", self._listen_wifi_state_changed),
            ("settings_conn_added", self._listen_settings_new_connection),
            ("settings_conn_removed", self._listen_settings_connection_removed),
        ]

        for name, coro_fn in listeners:
            task = self._asyncio_loop.create_task(
                self._resilient_listener(name, coro_fn),
                name=f"listener_{name}",
            )
            self._listener_tasks.append(task)
            self._track_task(task)

        logger.info("STARTED %D D-BUS SIGNAL LISTENERS", len(self._listener_tasks))
    
    xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_1': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_2': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_3': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_4': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_5': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_6': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_7': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_8': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_9': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_10': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_11': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_12': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_13': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_14': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_15': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_16': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_17': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_18': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_19': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_20': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_21': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_22': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_23': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_24': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_25': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_26': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_27': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_28': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_29': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_30': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_31': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_32': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_33': xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_33
    }
    xǁNetworkManagerWorkerǁ_start_signal_listeners__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_start_signal_listeners'

    async def _resilient_listener(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        args = [name, listener_fn]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_orig(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_1(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug(None, name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_2(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", None)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_3(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug(name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_4(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", )
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_5(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("XXListener '%s' cancelledXX", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_6(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_7(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("LISTENER '%S' CANCELLED", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_8(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_9(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    None,
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_10(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    None,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_11(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    None,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_12(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    None,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_13(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_14(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_15(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_16(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_17(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "XXListener '%s' failed: %s — restarting in %.1f sXX",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_18(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_19(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "LISTENER '%S' FAILED: %S — RESTARTING IN %.1F S",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_20(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = ""
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_21(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = ""
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_22(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = ""
                self._signal_settings = None
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_23(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = ""
                await asyncio.sleep(_LISTENER_RESTART_DELAY)
                if self._running:
                    self._ensure_signal_proxies()

    async def xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_24(
        self, name: str, listener_fn: "asyncio.coroutines"
    ) -> None:
        """Wrapper that restarts *listener_fn* on failure with back-off."""
        while self._running:
            try:
                await listener_fn()
            except asyncio.CancelledError:
                logger.debug("Listener '%s' cancelled", name)
                return
            except Exception as exc:
                if not self._running:
                    return
                logger.warning(
                    "Listener '%s' failed: %s — restarting in %.1f s",
                    name,
                    exc,
                    _LISTENER_RESTART_DELAY,
                )
                # Rebuild signal proxies in case the bus was reset
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                await asyncio.sleep(None)
                if self._running:
                    self._ensure_signal_proxies()
    
    xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_1': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_2': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_3': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_4': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_5': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_6': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_7': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_8': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_9': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_10': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_11': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_12': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_13': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_14': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_15': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_16': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_17': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_18': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_19': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_20': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_21': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_22': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_23': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_24': xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_24
    }
    xǁNetworkManagerWorkerǁ_resilient_listener__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_resilient_listener'

    async def _listen_nm_state_changed(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_orig(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_1(self) -> None:
        """React to NetworkManager global state transitions."""
        if self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_2(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug(None)
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_3(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("XXNM StateChanged listener startedXX")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_4(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("nm statechanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_5(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM STATECHANGED LISTENER STARTED")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_6(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_7(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = None
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_8(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(None)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_9(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    None,
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_10(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    None,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_11(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    None,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_12(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_13(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_14(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_15(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "XXNM StateChanged: %s (%d)XX",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_16(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "nm statechanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_17(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM STATECHANGED: %S (%D)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_18(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug(None, state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_19(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", None)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_20(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug(state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_21(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM StateChanged: unknown (%d)", )

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_22(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("XXNM StateChanged: unknown (%d)XX", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_23(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("nm statechanged: unknown (%d)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_24(self) -> None:
        """React to NetworkManager global state transitions."""
        if not self._signal_nm:
            return
        logger.debug("NM StateChanged listener started")
        async for state_value in self._signal_nm.state_changed:
            if not self._running:
                return
            try:
                nm_state = dbus_nm.NetworkManagerState(state_value)
                logger.debug(
                    "NM StateChanged: %s (%d)",
                    nm_state.name,
                    state_value,
                )
            except ValueError:
                logger.debug("NM STATECHANGED: UNKNOWN (%D)", state_value)

            self._schedule_debounced_state_rebuild()
            self._schedule_debounced_scan()
    
    xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_1': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_2': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_3': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_4': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_5': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_6': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_7': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_8': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_9': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_10': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_11': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_12': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_13': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_14': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_15': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_16': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_17': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_18': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_19': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_20': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_21': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_22': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_23': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_24': xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_24
    }
    xǁNetworkManagerWorkerǁ_listen_nm_state_changed__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_listen_nm_state_changed'

    async def _listen_ap_added(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_orig(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_1(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_2(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug(None, self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_3(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", None)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_4(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug(self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_5(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", )
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_6(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("XXAP Added listener started on %sXX", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_7(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("ap added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_8(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP ADDED LISTENER STARTED ON %S", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_9(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if self._running:
                return
            logger.debug("AP added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_10(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug(None, ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_11(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", None)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_12(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug(ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_13(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP added: %s", )
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_14(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("XXAP added: %sXX", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_15(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("ap added: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_16(self) -> None:
        """React to new access points appearing in scan results.

        Triggers a debounced scan rebuild (not a full rescan — NM has
        already updated its internal AP list).
        """
        if not self._signal_wifi:
            return
        logger.debug("AP Added listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_added:
            if not self._running:
                return
            logger.debug("AP ADDED: %S", ap_path)
            self._schedule_debounced_scan()
    
    xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_1': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_2': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_3': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_4': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_5': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_6': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_7': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_8': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_9': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_10': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_11': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_12': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_13': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_14': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_15': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_16': xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_16
    }
    xǁNetworkManagerWorkerǁ_listen_ap_added__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_listen_ap_added'

    async def _listen_ap_removed(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_orig(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_1(self) -> None:
        """React to access points disappearing from scan results."""
        if self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_2(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug(None, self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_3(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", None)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_4(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug(self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_5(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", )
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_6(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("XXAP Removed listener started on %sXX", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_7(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("ap removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_8(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP REMOVED LISTENER STARTED ON %S", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_9(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if self._running:
                return
            logger.debug("AP removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_10(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug(None, ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_11(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", None)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_12(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug(ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_13(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP removed: %s", )
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_14(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("XXAP removed: %sXX", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_15(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("ap removed: %s", ap_path)
            self._schedule_debounced_scan()

    async def xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_16(self) -> None:
        """React to access points disappearing from scan results."""
        if not self._signal_wifi:
            return
        logger.debug("AP Removed listener started on %s", self._primary_wifi_path)
        async for ap_path in self._signal_wifi.access_point_removed:
            if not self._running:
                return
            logger.debug("AP REMOVED: %S", ap_path)
            self._schedule_debounced_scan()
    
    xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_1': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_2': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_3': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_4': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_5': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_6': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_7': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_8': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_9': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_10': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_11': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_12': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_13': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_14': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_15': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_16': xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_16
    }
    xǁNetworkManagerWorkerǁ_listen_ap_removed__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_listen_ap_removed'

    async def _listen_wired_state_changed(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_orig(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_1(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_2(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug(None, self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_3(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", None)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_4(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug(self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_5(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", )
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_6(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("XXWired state listener started on %sXX", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_7(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_8(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("WIRED STATE LISTENER STARTED ON %S", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_9(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_10(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                None,
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_11(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                None,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_12(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                None,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_13(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                None,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_14(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_15(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_16(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_17(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_18(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "XXWired state: %d -> %d (reason %d)XX",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_19(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "wired state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_20(self) -> None:
        """React to wired device state transitions (cable plug/unplug).

        The ``state_changed`` signal on the Device interface emits
        ``(new_state, old_state, reason)`` with signature ``'uuu'``.
        """
        if not self._signal_wired:
            return
        logger.debug("Wired state listener started on %s", self._primary_wired_path)
        async for new_state, old_state, reason in self._signal_wired.state_changed:
            if not self._running:
                return
            logger.debug(
                "WIRED STATE: %D -> %D (REASON %D)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()
    
    xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_1': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_2': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_3': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_4': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_5': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_6': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_7': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_8': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_9': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_10': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_11': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_12': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_13': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_14': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_15': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_16': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_17': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_18': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_19': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_20': xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_20
    }
    xǁNetworkManagerWorkerǁ_listen_wired_state_changed__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_listen_wired_state_changed'

    async def _listen_wifi_state_changed(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_orig(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_1(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_2(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug(None, self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_3(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", None)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_4(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug(self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_5(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", )
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_6(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("XXWi-Fi state listener started on %sXX", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_7(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("wi-fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_8(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("WI-FI STATE LISTENER STARTED ON %S", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_9(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_10(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                None,
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_11(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                None,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_12(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                None,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_13(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                None,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_14(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_15(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_16(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_17(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "Wi-Fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_18(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "XXWi-Fi state: %d -> %d (reason %d)XX",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_19(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "wi-fi state: %d -> %d (reason %d)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()

    async def xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_20(self) -> None:
        """React to Wi-Fi device state transitions.

        Detects enabled/disabled, connecting, disconnected transitions
        instantly — complements the NM global ``state_changed`` signal
        which may not fire for all device-level transitions.
        """
        if not self._signal_wifi:
            return
        logger.debug("Wi-Fi state listener started on %s", self._primary_wifi_path)
        async for new_state, old_state, reason in self._signal_wifi.state_changed:
            if not self._running:
                return
            logger.debug(
                "WI-FI STATE: %D -> %D (REASON %D)",
                old_state,
                new_state,
                reason,
            )
            self._schedule_debounced_state_rebuild()
    
    xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_1': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_2': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_3': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_4': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_5': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_6': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_7': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_8': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_9': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_10': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_11': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_12': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_13': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_14': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_15': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_16': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_17': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_18': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_19': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_20': xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_20
    }
    xǁNetworkManagerWorkerǁ_listen_wifi_state_changed__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_listen_wifi_state_changed'

    async def _listen_settings_new_connection(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_orig(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_1(self) -> None:
        """React to new saved connection profiles being added."""
        if self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_2(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug(None)
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_3(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("XXSettings NewConnection listener startedXX")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_4(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("settings newconnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_5(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("SETTINGS NEWCONNECTION LISTENER STARTED")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_6(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_7(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug(None, conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_8(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", None)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_9(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug(conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_10(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", )
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_11(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("XXSettings: new connection %sXX", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_12(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_13(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("SETTINGS: NEW CONNECTION %S", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_14(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = None
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_15(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = False
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_16(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                None
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_17(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    None,
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_18(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_19(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    name="saved_on_new_connection",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_20(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_21(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="XXsaved_on_new_connectionXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_22(self) -> None:
        """React to new saved connection profiles being added."""
        if not self._signal_settings:
            return
        logger.debug("Settings NewConnection listener started")
        async for conn_path in self._signal_settings.new_connection:
            if not self._running:
                return
            logger.debug("Settings: new connection %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="SAVED_ON_NEW_CONNECTION",
                )
            )
    
    xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_1': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_2': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_3': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_4': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_5': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_6': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_7': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_8': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_9': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_10': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_11': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_12': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_13': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_14': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_15': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_16': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_17': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_18': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_19': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_20': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_21': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_22': xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_22
    }
    xǁNetworkManagerWorkerǁ_listen_settings_new_connection__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_listen_settings_new_connection'

    async def _listen_settings_connection_removed(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_orig(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_1(self) -> None:
        """React to saved connection profiles being deleted."""
        if self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_2(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug(None)
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_3(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("XXSettings ConnectionRemoved listener startedXX")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_4(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("settings connectionremoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_5(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("SETTINGS CONNECTIONREMOVED LISTENER STARTED")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_6(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_7(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug(None, conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_8(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", None)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_9(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug(conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_10(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", )
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_11(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("XXSettings: connection removed %sXX", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_12(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_13(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("SETTINGS: CONNECTION REMOVED %S", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_14(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = None
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_15(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = False
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_16(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                None
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_17(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    None,
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_18(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_19(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    name="saved_on_connection_removed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_20(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_21(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="XXsaved_on_connection_removedXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_22(self) -> None:
        """React to saved connection profiles being deleted."""
        if not self._signal_settings:
            return
        logger.debug("Settings ConnectionRemoved listener started")
        async for conn_path in self._signal_settings.connection_removed:
            if not self._running:
                return
            logger.debug("Settings: connection removed %s", conn_path)
            self._saved_cache_dirty = True
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_load_saved_networks(),
                    name="SAVED_ON_CONNECTION_REMOVED",
                )
            )
    
    xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_1': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_2': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_3': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_4': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_5': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_6': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_7': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_8': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_9': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_10': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_11': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_12': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_13': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_14': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_15': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_16': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_17': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_18': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_19': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_20': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_21': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_22': xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_22
    }
    xǁNetworkManagerWorkerǁ_listen_settings_connection_removed__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_listen_settings_connection_removed'

    def _schedule_debounced_state_rebuild(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_orig(self) -> None:
        """Schedule a state rebuild after a short debounce window.

        Multiple rapid D-Bus signals (e.g. during a roam or reconnect)
        coalesce into a single ``_build_current_state`` call, saving
        ~12-15 D-Bus round-trips per coalesced burst.
        """
        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
        self._state_debounce_handle = self._asyncio_loop.call_later(
            _DEBOUNCE_DELAY, self._fire_state_rebuild
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_1(self) -> None:
        """Schedule a state rebuild after a short debounce window.

        Multiple rapid D-Bus signals (e.g. during a roam or reconnect)
        coalesce into a single ``_build_current_state`` call, saving
        ~12-15 D-Bus round-trips per coalesced burst.
        """
        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
        self._state_debounce_handle = None

    def xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_2(self) -> None:
        """Schedule a state rebuild after a short debounce window.

        Multiple rapid D-Bus signals (e.g. during a roam or reconnect)
        coalesce into a single ``_build_current_state`` call, saving
        ~12-15 D-Bus round-trips per coalesced burst.
        """
        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
        self._state_debounce_handle = self._asyncio_loop.call_later(
            None, self._fire_state_rebuild
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_3(self) -> None:
        """Schedule a state rebuild after a short debounce window.

        Multiple rapid D-Bus signals (e.g. during a roam or reconnect)
        coalesce into a single ``_build_current_state`` call, saving
        ~12-15 D-Bus round-trips per coalesced burst.
        """
        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
        self._state_debounce_handle = self._asyncio_loop.call_later(
            _DEBOUNCE_DELAY, None
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_4(self) -> None:
        """Schedule a state rebuild after a short debounce window.

        Multiple rapid D-Bus signals (e.g. during a roam or reconnect)
        coalesce into a single ``_build_current_state`` call, saving
        ~12-15 D-Bus round-trips per coalesced burst.
        """
        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
        self._state_debounce_handle = self._asyncio_loop.call_later(
            self._fire_state_rebuild
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_5(self) -> None:
        """Schedule a state rebuild after a short debounce window.

        Multiple rapid D-Bus signals (e.g. during a roam or reconnect)
        coalesce into a single ``_build_current_state`` call, saving
        ~12-15 D-Bus round-trips per coalesced burst.
        """
        if self._state_debounce_handle:
            self._state_debounce_handle.cancel()
        self._state_debounce_handle = self._asyncio_loop.call_later(
            _DEBOUNCE_DELAY, )
    
    xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_1': xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_2': xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_3': xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_4': xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_5': xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_5
    }
    xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_schedule_debounced_state_rebuild'

    def _fire_state_rebuild(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_orig(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_get_current_state(),
                    name="debounced_state_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_1(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = ""
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_get_current_state(),
                    name="debounced_state_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_2(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                None
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_3(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    None,
                    name="debounced_state_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_4(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_get_current_state(),
                    name=None,
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_5(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    name="debounced_state_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_6(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_get_current_state(),
                    )
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_7(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_get_current_state(),
                    name="XXdebounced_state_rebuildXX",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_8(self) -> None:
        """Debounce callback — spawns the actual async state rebuild."""
        self._state_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_get_current_state(),
                    name="DEBOUNCED_STATE_REBUILD",
                )
            )
    
    xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_1': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_2': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_3': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_4': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_5': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_6': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_7': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_8': xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_8
    }
    xǁNetworkManagerWorkerǁ_fire_state_rebuild__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_fire_state_rebuild'

    def _schedule_debounced_scan(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_orig(self) -> None:
        """Schedule a scan-results rebuild after a debounce window.

        AP Added/Removed signals can fire in rapid bursts when
        entering/leaving a dense area.  Coalescing prevents NxN AP
        property reads.
        """
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
        self._scan_debounce_handle = self._asyncio_loop.call_later(
            _DEBOUNCE_DELAY, self._fire_scan_rebuild
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_1(self) -> None:
        """Schedule a scan-results rebuild after a debounce window.

        AP Added/Removed signals can fire in rapid bursts when
        entering/leaving a dense area.  Coalescing prevents NxN AP
        property reads.
        """
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
        self._scan_debounce_handle = None

    def xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_2(self) -> None:
        """Schedule a scan-results rebuild after a debounce window.

        AP Added/Removed signals can fire in rapid bursts when
        entering/leaving a dense area.  Coalescing prevents NxN AP
        property reads.
        """
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
        self._scan_debounce_handle = self._asyncio_loop.call_later(
            None, self._fire_scan_rebuild
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_3(self) -> None:
        """Schedule a scan-results rebuild after a debounce window.

        AP Added/Removed signals can fire in rapid bursts when
        entering/leaving a dense area.  Coalescing prevents NxN AP
        property reads.
        """
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
        self._scan_debounce_handle = self._asyncio_loop.call_later(
            _DEBOUNCE_DELAY, None
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_4(self) -> None:
        """Schedule a scan-results rebuild after a debounce window.

        AP Added/Removed signals can fire in rapid bursts when
        entering/leaving a dense area.  Coalescing prevents NxN AP
        property reads.
        """
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
        self._scan_debounce_handle = self._asyncio_loop.call_later(
            self._fire_scan_rebuild
        )

    def xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_5(self) -> None:
        """Schedule a scan-results rebuild after a debounce window.

        AP Added/Removed signals can fire in rapid bursts when
        entering/leaving a dense area.  Coalescing prevents NxN AP
        property reads.
        """
        if self._scan_debounce_handle:
            self._scan_debounce_handle.cancel()
        self._scan_debounce_handle = self._asyncio_loop.call_later(
            _DEBOUNCE_DELAY, )
    
    xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_1': xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_2': xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_3': xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_4': xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_5': xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_5
    }
    xǁNetworkManagerWorkerǁ_schedule_debounced_scan__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_schedule_debounced_scan'

    def _fire_scan_rebuild(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_orig(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_scan_networks(),
                    name="debounced_scan_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_1(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = ""
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_scan_networks(),
                    name="debounced_scan_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_2(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                None
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_3(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    None,
                    name="debounced_scan_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_4(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_scan_networks(),
                    name=None,
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_5(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    name="debounced_scan_rebuild",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_6(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_scan_networks(),
                    )
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_7(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_scan_networks(),
                    name="XXdebounced_scan_rebuildXX",
                )
            )

    def xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_8(self) -> None:
        """Debounce callback — spawns the async scan rebuild."""
        self._scan_debounce_handle = None
        if self._running:
            self._track_task(
                self._asyncio_loop.create_task(
                    self._async_scan_networks(),
                    name="DEBOUNCED_SCAN_REBUILD",
                )
            )
    
    xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_1': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_2': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_3': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_4': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_5': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_6': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_7': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_8': xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_8
    }
    xǁNetworkManagerWorkerǁ_fire_scan_rebuild__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_fire_scan_rebuild'

    async def _async_fallback_poll(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_orig(self) -> None:
        """Lightweight fallback for missed signals.

        Called at a long interval (default 60 s) by the manager.
        Rebuilds state, connectivity, and saved networks.
        """
        if not self._running:
            return
        await self._async_get_current_state()
        await self._async_check_connectivity()
        await self._async_load_saved_networks()

    async def xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_1(self) -> None:
        """Lightweight fallback for missed signals.

        Called at a long interval (default 60 s) by the manager.
        Rebuilds state, connectivity, and saved networks.
        """
        if self._running:
            return
        await self._async_get_current_state()
        await self._async_check_connectivity()
        await self._async_load_saved_networks()
    
    xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_1': xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_1
    }
    xǁNetworkManagerWorkerǁ_async_fallback_poll__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_fallback_poll'

    async def _ensure_dbus_connection(self) -> bool:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_orig(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_1(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_2(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return True
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_3(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = None
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_4(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = None
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_5(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 1
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_6(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return False
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_7(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors = 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_8(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors -= 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_9(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 2
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_10(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                None,
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_11(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                None,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_12(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                None,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_13(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                None,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_14(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_15(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_16(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_17(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_18(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "XXD-Bus health check failed (%d/%d): %sXX",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_19(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "d-bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_20(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-BUS HEALTH CHECK FAILED (%D/%D): %S",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_21(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors <= self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_22(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return True
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_23(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning(None)
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_24(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("XXAttempting D-Bus reconnection...XX")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_25(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("attempting d-bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_26(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("ATTEMPTING D-BUS RECONNECTION...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_27(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = None
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_28(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(None)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_29(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = None
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_30(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = "XXXX"
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_31(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = None
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_32(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = "XXXX"
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_33(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = None
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_34(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = "XXXX"
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_35(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = None
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_36(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = "XXXX"
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_37(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = ""
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_38(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = ""
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_39(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = ""
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_40(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = ""
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_41(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_42(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = None
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_43(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 1
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_44(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info(None)
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_45(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("XXD-Bus reconnection succeededXX")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_46(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("d-bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_47(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-BUS RECONNECTION SUCCEEDED")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_48(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path and self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_49(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        None, "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_50(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", None
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_51(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_52(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_53(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "XXdevice_reconnectedXX", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_54(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "DEVICE_RECONNECTED", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_55(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "XXNetwork device reconnectedXX"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_56(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_57(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "NETWORK DEVICE RECONNECTED"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_58(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return False
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_59(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error(None, re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_60(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", None)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_61(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error(re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_62(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", )
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_63(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("XXD-Bus reconnection failed: %sXX", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_64(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("d-bus reconnection failed: %s", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_65(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-BUS RECONNECTION FAILED: %S", re_err)
                return False

    async def xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_66(self) -> bool:
        """Verify the D-Bus connection is healthy, reconnecting if needed.

        Performs a lightweight ``version`` property read as a health check.
        Consecutive failures increment ``_consecutive_dbus_errors``; once the
        threshold is reached, opens a new system bus, re-detects interfaces,
        rebuilds signal proxies, and restarts all listener tasks.  Returns
        ``True`` if the bus is usable (either always-healthy or successfully
        reconnected), ``False`` otherwise.
        """
        if not self._running:
            return False
        try:
            _ = await self._nm().version
            self._consecutive_dbus_errors = 0
            return True
        except Exception as exc:
            self._consecutive_dbus_errors += 1
            logger.warning(
                "D-Bus health check failed (%d/%d): %s",
                self._consecutive_dbus_errors,
                self._MAX_DBUS_ERRORS_BEFORE_RECONNECT,
                exc,
            )
            if self._consecutive_dbus_errors < self._MAX_DBUS_ERRORS_BEFORE_RECONNECT:
                return False
            logger.warning("Attempting D-Bus reconnection...")
            try:
                self._system_bus = sdbus.sd_bus_open_system()
                sdbus.set_default_bus(self._system_bus)
                self._primary_wifi_path = ""
                self._primary_wifi_iface = ""
                self._primary_wired_path = ""
                self._primary_wired_iface = ""
                self._iface_to_device_path.clear()
                await self._detect_interfaces()
                # Rebuild signal proxies on new bus
                self._signal_nm = None
                self._signal_wifi = None
                self._signal_wired = None
                self._signal_settings = None
                self._ensure_signal_proxies()
                # Cancel stale listener tasks bound to old proxies
                # and restart them on the new bus connection.
                for task in self._listener_tasks:
                    if not task.done():
                        task.cancel()
                self._listener_tasks.clear()
                await self._start_signal_listeners()
                self._consecutive_dbus_errors = 0
                logger.info("D-Bus reconnection succeeded")
                if self._primary_wifi_path or self._primary_wired_path:
                    self.error_occurred.emit(
                        "device_reconnected", "Network device reconnected"
                    )
                return True
            except Exception as re_err:
                logger.error("D-Bus reconnection failed: %s", re_err)
                return True
    
    xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_1': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_2': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_3': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_4': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_5': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_6': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_7': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_8': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_9': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_10': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_11': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_12': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_13': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_14': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_15': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_16': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_17': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_18': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_19': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_20': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_21': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_22': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_23': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_24': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_25': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_26': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_27': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_28': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_29': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_30': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_31': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_32': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_33': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_34': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_35': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_36': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_37': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_38': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_39': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_40': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_41': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_42': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_43': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_44': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_45': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_46': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_47': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_48': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_49': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_50': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_51': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_52': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_53': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_54': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_55': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_56': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_57': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_58': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_59': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_60': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_61': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_62': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_63': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_64': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_65': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_66': xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_66
    }
    xǁNetworkManagerWorkerǁ_ensure_dbus_connection__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_ensure_dbus_connection'

    async def _is_ethernet_connected(self) -> bool:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_orig(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_1(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_2(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return True
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_3(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(None).state == 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_4(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state != 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_5(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 101
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_6(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug(None, exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_7(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", None)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_8(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug(exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_9(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", )
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_10(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("XXError checking ethernet state: %sXX", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_11(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("error checking ethernet state: %s", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_12(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("ERROR CHECKING ETHERNET STATE: %S", exc)
            return False

    async def xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_13(self) -> bool:
        """Return True if the primary wired device is fully activated (state 100)."""
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state == 100
        except Exception as exc:
            logger.debug("Error checking ethernet state: %s", exc)
            return True
    
    xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_1': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_2': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_3': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_4': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_5': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_6': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_7': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_8': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_9': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_10': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_11': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_12': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_13': xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_13
    }
    xǁNetworkManagerWorkerǁ_is_ethernet_connected__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_is_ethernet_connected'

    async def _has_ethernet_carrier(self) -> bool:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_orig(self) -> bool:
        """Return True if the primary wired device has a physical link (state >= 30).

        State 30 is DISCONNECTED in NM's device state enum, which still implies
        a cable is present.  This is a weaker check than ``_is_ethernet_connected``
        and is used to populate ``NetworkState.ethernet_carrier`` for UI feedback.
        """
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state >= 30
        except Exception:
            # D-Bus read failed; carrier state unknown — treat as no carrier.
            return False

    async def xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_1(self) -> bool:
        """Return True if the primary wired device has a physical link (state >= 30).

        State 30 is DISCONNECTED in NM's device state enum, which still implies
        a cable is present.  This is a weaker check than ``_is_ethernet_connected``
        and is used to populate ``NetworkState.ethernet_carrier`` for UI feedback.
        """
        if self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state >= 30
        except Exception:
            # D-Bus read failed; carrier state unknown — treat as no carrier.
            return False

    async def xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_2(self) -> bool:
        """Return True if the primary wired device has a physical link (state >= 30).

        State 30 is DISCONNECTED in NM's device state enum, which still implies
        a cable is present.  This is a weaker check than ``_is_ethernet_connected``
        and is used to populate ``NetworkState.ethernet_carrier`` for UI feedback.
        """
        if not self._primary_wired_path:
            return True
        try:
            return await self._generic(self._primary_wired_path).state >= 30
        except Exception:
            # D-Bus read failed; carrier state unknown — treat as no carrier.
            return False

    async def xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_3(self) -> bool:
        """Return True if the primary wired device has a physical link (state >= 30).

        State 30 is DISCONNECTED in NM's device state enum, which still implies
        a cable is present.  This is a weaker check than ``_is_ethernet_connected``
        and is used to populate ``NetworkState.ethernet_carrier`` for UI feedback.
        """
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(None).state >= 30
        except Exception:
            # D-Bus read failed; carrier state unknown — treat as no carrier.
            return False

    async def xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_4(self) -> bool:
        """Return True if the primary wired device has a physical link (state >= 30).

        State 30 is DISCONNECTED in NM's device state enum, which still implies
        a cable is present.  This is a weaker check than ``_is_ethernet_connected``
        and is used to populate ``NetworkState.ethernet_carrier`` for UI feedback.
        """
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state > 30
        except Exception:
            # D-Bus read failed; carrier state unknown — treat as no carrier.
            return False

    async def xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_5(self) -> bool:
        """Return True if the primary wired device has a physical link (state >= 30).

        State 30 is DISCONNECTED in NM's device state enum, which still implies
        a cable is present.  This is a weaker check than ``_is_ethernet_connected``
        and is used to populate ``NetworkState.ethernet_carrier`` for UI feedback.
        """
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state >= 31
        except Exception:
            # D-Bus read failed; carrier state unknown — treat as no carrier.
            return False

    async def xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_6(self) -> bool:
        """Return True if the primary wired device has a physical link (state >= 30).

        State 30 is DISCONNECTED in NM's device state enum, which still implies
        a cable is present.  This is a weaker check than ``_is_ethernet_connected``
        and is used to populate ``NetworkState.ethernet_carrier`` for UI feedback.
        """
        if not self._primary_wired_path:
            return False
        try:
            return await self._generic(self._primary_wired_path).state >= 30
        except Exception:
            # D-Bus read failed; carrier state unknown — treat as no carrier.
            return True
    
    xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_1': xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_2': xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_3': xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_4': xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_5': xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_6': xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_6
    }
    xǁNetworkManagerWorkerǁ_has_ethernet_carrier__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_has_ethernet_carrier'

    async def _wait_for_wifi_radio(self, desired: bool, timeout: float = 3.0) -> bool:
        args = [desired, timeout]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_orig(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_1(self, desired: bool, timeout: float = 4.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_2(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = None
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_3(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = None
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_4(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() - timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_5(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = None
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_6(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = True
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_7(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() <= deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_8(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled != desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_9(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return False
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_10(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_11(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug(None, exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_12(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", None)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_13(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug(exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_14(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", )
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_15(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("XXPolling wireless_enabled failed: %sXX", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_16(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_17(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("POLLING WIRELESS_ENABLED FAILED: %S", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_18(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = None
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_19(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = False
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_20(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(None)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_21(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(1.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_22(self, desired: bool, timeout: float = 3.0) -> bool:
        """Poll NM wireless_enabled until it matches *desired* or *timeout* expires."""
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._nm().wireless_enabled == desired:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling wireless_enabled failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return True
    
    xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_1': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_2': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_3': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_4': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_5': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_6': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_7': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_8': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_9': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_10': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_11': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_12': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_13': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_14': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_15': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_16': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_17': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_18': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_19': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_20': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_21': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_22': xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_22
    }
    xǁNetworkManagerWorkerǁ_wait_for_wifi_radio__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_wait_for_wifi_radio'

    async def _wait_for_wifi_device_ready(self, timeout: float = 8.0) -> bool:
        args = [timeout]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_orig(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_1(self, timeout: float = 9.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_2(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_3(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return True
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_4(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = None
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_5(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = None
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_6(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() - timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_7(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = None
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_8(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = True
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_9(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() <= deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_10(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(None).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_11(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state > 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_12(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 31:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_13(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return False
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_14(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_15(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug(None, exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_16(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", None)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_17(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug(exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_18(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", )
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_19(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("XXPolling Wi-Fi device state failed: %sXX", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_20(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("polling wi-fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_21(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("POLLING WI-FI DEVICE STATE FAILED: %S", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_22(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = None
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_23(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = False
            await asyncio.sleep(0.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_24(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(None)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_25(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(1.25)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_26(self, timeout: float = 8.0) -> bool:
        """Poll wlan0 device state until it reaches DISCONNECTED (30) or above."""
        if not self._primary_wifi_path:
            return False
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        _logged = False
        while loop.time() < deadline:
            try:
                if await self._generic(self._primary_wifi_path).state >= 30:
                    return True
            except Exception as exc:
                if not _logged:
                    logger.debug("Polling Wi-Fi device state failed: %s", exc)
                    _logged = True
            await asyncio.sleep(0.25)
        return True
    
    xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_1': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_2': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_3': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_4': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_5': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_6': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_7': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_8': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_9': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_10': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_11': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_12': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_13': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_14': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_15': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_16': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_17': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_18': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_19': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_20': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_21': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_22': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_23': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_24': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_25': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_26': xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_26
    }
    xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_wait_for_wifi_device_ready'

    async def _async_get_current_state(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_orig(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_1(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_2(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(None)
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_3(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = None
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_4(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled or not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_5(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled or not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_6(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected or state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_7(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_8(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_9(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    None
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_10(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "XXRuntime mutual exclusion: ethernet active + XX"
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_11(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_12(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "RUNTIME MUTUAL EXCLUSION: ETHERNET ACTIVE + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_13(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "XXWi-Fi — disabling Wi-FiXX"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_14(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "wi-fi — disabling wi-fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_15(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "WI-FI — DISABLING WI-FI"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_16(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug(None, exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_17(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", None)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_18(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug(exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_19(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", )
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_20(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("XXDisconnect before Wi-Fi disable ignored: %sXX", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_21(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("disconnect before wi-fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_22(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("DISCONNECT BEFORE WI-FI DISABLE IGNORED: %S", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_23(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(None)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_24(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(True)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_25(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(None)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_26(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(1.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_27(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = None
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_28(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_29(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error(None, exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_30(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", None)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_31(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error(exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_32(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", )
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_33(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("XXFailed to get current state: %sXX", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_34(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_35(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("FAILED TO GET CURRENT STATE: %S", exc)
            self.error_occurred.emit("get_current_state", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_36(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit(None, str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_37(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", None)

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_38(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit(str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_39(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", )

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_40(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("XXget_current_stateXX", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_41(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("GET_CURRENT_STATE", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_42(self) -> None:
        """Rebuild and emit the full NetworkState, enforcing runtime mutual exclusion."""
        try:
            if not await self._ensure_dbus_connection():
                self.state_changed.emit(NetworkState())
                return
            state = await self._build_current_state()
            if (
                state.ethernet_connected
                and state.wifi_enabled
                and not state.hotspot_enabled
                and not self._is_hotspot_active
            ):
                logger.info(
                    "Runtime mutual exclusion: ethernet active + "
                    "Wi-Fi — disabling Wi-Fi"
                )
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect before Wi-Fi disable ignored: %s", exc)
                await self._nm().wireless_enabled.set_async(False)
                await asyncio.sleep(0.5)
                state = await self._build_current_state()
            self.state_changed.emit(state)
        except Exception as exc:
            logger.error("Failed to get current state: %s", exc)
            self.error_occurred.emit("get_current_state", str(None))
    
    xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_1': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_2': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_3': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_4': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_5': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_6': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_7': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_8': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_9': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_10': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_11': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_12': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_13': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_14': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_15': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_16': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_17': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_18': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_19': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_20': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_21': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_22': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_23': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_24': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_25': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_26': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_27': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_28': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_29': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_30': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_31': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_32': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_33': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_34': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_35': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_36': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_37': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_38': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_39': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_40': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_41': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_42': xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_42
    }
    xǁNetworkManagerWorkerǁ_async_get_current_state__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_get_current_state'

    @staticmethod
    def _get_ip_os_fallback(iface: str) -> str:
        """Return the IPv4 address for *iface* via a raw ioctl SIOCGIFADDR call.

        Used as a fallback when the NM D-Bus IPv4Config path returns nothing —
        common immediately after DHCP on slower hardware.
        """
        if not iface:
            return ""
        _SIOCGIFADDR = 0x8915
        try:
            with _socket.socket(_socket.AF_INET, _socket.SOCK_DGRAM) as sock:
                ifreq = struct.pack("256s", iface[:15].encode())
                result = fcntl.ioctl(sock.fileno(), _SIOCGIFADDR, ifreq)
                return _socket.inet_ntoa(result[20:24])
        except Exception:
            # ioctl fails when the interface has no address; caller treats "" as unknown.
            return ""

    async def _build_current_state(self) -> NetworkState:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_orig(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_1(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_2(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = None
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_3(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = None
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_4(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(None)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_5(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = None
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_6(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(None)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_7(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = None

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_8(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = None
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_9(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = None
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_10(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    None
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_11(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface and "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_12(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "XXeth0XX"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_13(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "ETH0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_14(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_15(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = None
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_16(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        None
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_17(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface and "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_18(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "XXeth0XX"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_19(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "ETH0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_20(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = None
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_21(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = "XXXX"
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_22(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = None
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_23(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface(None)
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_24(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("XXwlan0XX")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_25(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("WLAN0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_26(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_27(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = None
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_28(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = None

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_29(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = "XXXX"

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_30(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip or connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_31(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_32(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity not in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_33(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface and "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_34(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "XXeth0XX",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_35(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "ETH0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_36(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "XXwlan0XX",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_37(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "WLAN0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_38(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = None
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_39(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(None)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_40(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = None
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_41(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface == "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_42(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "XXwlan0XX":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_43(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "WLAN0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_44(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = None
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_45(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = False
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_46(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug(None, _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_47(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", None, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_48(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, None)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_49(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug(_iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_50(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_51(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, )
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_52(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("XXOS fallback IP for '%s': %sXX", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_53(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("os fallback ip for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_54(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS FALLBACK IP FOR '%S': %S", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_55(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        return

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_56(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = None
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_57(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 1
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_58(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = None
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_59(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = "XXXX"
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_60(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = None
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_61(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = None
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_62(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(None, 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_63(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), None)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_64(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_65(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), )
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_66(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.upper(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_67(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 1)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_68(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = None
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_69(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(None)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_70(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = None

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_71(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else "XXXX"

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_72(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = None

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_73(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid != self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_74(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active or not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_75(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled or self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_76(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_77(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_78(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = None
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_79(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = False
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_80(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = None
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_81(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    None,
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_82(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    None,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_83(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_84(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_85(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "XXHotspot SSID not found via D-Bus, using config: '%s'XX",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_86(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "hotspot ssid not found via d-bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_87(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "HOTSPOT SSID NOT FOUND VIA D-BUS, USING CONFIG: '%S'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_88(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = None
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_89(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_90(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = None

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_91(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface(None)

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_92(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("XXwlan0XX")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_93(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("WLAN0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_94(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=None,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_95(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=None,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_96(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=None,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_97(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=None,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_98(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=None,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_99(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=None,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_100(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=None,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_101(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=None,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_102(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=None,
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_103(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=None,
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_104(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_105(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_106(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_107(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_108(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_109(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_110(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_111(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_112(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_113(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                )
        except Exception as exc:
            logger.error("Error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_114(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error(None, exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_115(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", None)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_116(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error(exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_117(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("Error building current state: %s", )
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_118(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("XXError building current state: %sXX", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_119(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("error building current state: %s", exc)
            return NetworkState()

    async def xǁNetworkManagerWorkerǁ_build_current_state__mutmut_120(self) -> NetworkState:
        """Read all relevant NM properties and assemble a NetworkState snapshot."""
        if not self._system_bus:
            return NetworkState()
        try:
            connectivity_value = await self._nm().check_connectivity()
            connectivity = self._map_connectivity(connectivity_value)
            wifi_enabled = bool(await self._nm().wireless_enabled)
            current_ssid = await self._get_current_ssid()

            eth_connected = await self._is_ethernet_connected()
            if eth_connected:
                current_ip = await self._get_ip_by_interface(
                    self._primary_wired_iface or "eth0"
                )
                if not current_ip:
                    current_ip = self._get_ip_os_fallback(
                        self._primary_wired_iface or "eth0"
                    )
                current_ssid = ""
            elif current_ssid:
                current_ip = await self._get_ip_by_interface("wlan0")
                if not current_ip:
                    current_ip = await self._get_current_ip()
            else:
                current_ip = ""

            if not current_ip and connectivity in (
                ConnectivityState.FULL,
                ConnectivityState.LIMITED,
            ):
                for _iface in (
                    self._primary_wired_iface or "eth0",
                    "wlan0",
                ):
                    _fallback = self._get_ip_os_fallback(_iface)
                    if _fallback:
                        current_ip = _fallback
                        if _iface != "wlan0":
                            eth_connected = True
                        logger.debug("OS fallback IP for '%s': %s", _iface, _fallback)
                        break

            signal = 0
            sec_type = ""
            if current_ssid:
                signal_map = await self._build_signal_map()
                signal = signal_map.get(current_ssid.lower(), 0)
                saved = await self._get_saved_network_cached(current_ssid)
                sec_type = saved.security_type if saved else ""

            hotspot_enabled = current_ssid == self._hotspot_config.ssid

            if not hotspot_enabled and self._is_hotspot_active and not current_ssid:
                hotspot_enabled = True
                current_ssid = self._hotspot_config.ssid
                logger.debug(
                    "Hotspot SSID not found via D-Bus, using config: '%s'",
                    current_ssid,
                )

            if hotspot_enabled:
                sec_type = self._hotspot_config.security
                if not current_ip:
                    current_ip = await self._get_ip_by_interface("wlan0")

            return NetworkState(
                connectivity=connectivity,
                current_ssid=current_ssid,
                current_ip=current_ip,
                wifi_enabled=wifi_enabled,
                hotspot_enabled=hotspot_enabled,
                signal_strength=signal,
                security_type=sec_type,
                ethernet_connected=eth_connected,
                ethernet_carrier=await self._has_ethernet_carrier(),
                active_vlans=await self._get_active_vlans(),
            )
        except Exception as exc:
            logger.error("ERROR BUILDING CURRENT STATE: %S", exc)
            return NetworkState()
    
    xǁNetworkManagerWorkerǁ_build_current_state__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_1': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_2': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_3': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_4': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_5': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_6': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_7': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_8': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_9': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_10': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_11': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_12': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_13': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_14': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_15': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_16': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_17': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_18': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_19': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_20': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_21': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_22': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_23': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_24': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_25': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_26': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_27': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_28': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_29': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_30': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_31': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_32': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_33': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_34': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_35': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_36': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_37': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_38': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_39': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_40': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_41': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_42': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_43': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_44': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_45': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_46': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_47': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_48': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_49': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_50': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_51': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_52': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_53': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_54': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_55': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_56': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_57': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_58': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_59': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_60': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_61': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_62': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_63': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_64': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_65': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_66': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_67': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_68': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_69': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_70': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_71': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_72': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_73': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_74': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_75': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_76': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_77': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_78': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_79': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_80': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_81': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_82': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_83': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_84': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_85': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_86': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_87': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_88': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_89': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_90': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_91': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_92': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_93': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_94': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_95': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_96': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_97': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_98': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_99': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_100': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_101': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_102': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_103': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_104': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_105': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_106': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_107': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_108': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_109': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_110': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_110, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_111': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_111, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_112': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_112, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_113': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_113, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_114': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_114, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_115': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_115, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_116': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_116, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_117': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_117, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_118': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_118, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_119': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_119, 
        'xǁNetworkManagerWorkerǁ_build_current_state__mutmut_120': xǁNetworkManagerWorkerǁ_build_current_state__mutmut_120
    }
    xǁNetworkManagerWorkerǁ_build_current_state__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_build_current_state'

    @staticmethod
    def _map_connectivity(value: int) -> ConnectivityState:
        """Map a raw NM connectivity integer to a ConnectivityState enum member."""
        try:
            return ConnectivityState(value)
        except ValueError:
            return ConnectivityState.UNKNOWN

    async def _async_check_connectivity(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_orig(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_1(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_2(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(None)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_3(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                None
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_4(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(None)
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_5(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error(None, exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_6(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", None)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_7(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error(exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_8(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", )
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_9(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("XXFailed to check connectivity: %sXX", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_10(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("failed to check connectivity: %s", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_11(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("FAILED TO CHECK CONNECTIVITY: %S", exc)
            self.connectivity_changed.emit(ConnectivityState.UNKNOWN)

    async def xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_12(self) -> None:
        """Query NM connectivity and emit connectivity_changed."""
        try:
            if not self._system_bus:
                self.connectivity_changed.emit(ConnectivityState.UNKNOWN)
                return
            self.connectivity_changed.emit(
                self._map_connectivity(await self._nm().check_connectivity())
            )
        except Exception as exc:
            logger.error("Failed to check connectivity: %s", exc)
            self.connectivity_changed.emit(None)
    
    xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_1': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_2': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_3': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_4': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_5': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_6': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_7': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_8': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_9': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_10': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_11': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_12': xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_12
    }
    xǁNetworkManagerWorkerǁ_async_check_connectivity__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_check_connectivity'

    async def _get_current_ssid(self) -> str:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_orig(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_1(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = None
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_2(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con or primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_3(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con == "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_4(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "XX/XX":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_5(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = None
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_6(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(None)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_7(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug(None, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_8(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", None)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_9(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug(exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_10(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", )
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_11(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("XXError getting current SSID: %sXX", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_12(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("error getting current ssid: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_13(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("ERROR GETTING CURRENT SSID: %S", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_14(self) -> str:
        """Return the SSID of the currently active Wi-Fi connection, or empty string."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con and primary_con != "/":
                ssid = await self._ssid_from_active_connection(primary_con)
                if ssid:
                    return ssid
            return await self._get_ssid_from_any_active()
        except Exception as exc:
            logger.debug("Error getting current SSID: %s", exc)
            return "XXXX"
    
    xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_1': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_2': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_3': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_4': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_5': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_6': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_7': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_8': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_9': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_10': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_11': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_12': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_13': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_14': xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_14
    }
    xǁNetworkManagerWorkerǁ_get_current_ssid__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_current_ssid'

    async def _ssid_from_active_connection(self, active_path: str) -> str:
        args = [active_path]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_orig(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_1(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = None
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_2(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(None).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_3(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path and conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_4(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_5(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path != "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_6(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "XX/XX":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_7(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return "XXXX"
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_8(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = None
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_9(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(None).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_10(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "XX802-11-wirelessXX" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_11(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-WIRELESS" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_12(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" not in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_13(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = None
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_14(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["XX802-11-wirelessXX"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_15(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-WIRELESS"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_16(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["XXssidXX"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_17(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["SSID"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_18(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][2].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_19(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                None,
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_20(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                None,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_21(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                None,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_22(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_23(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_24(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_25(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "XXError reading active connection %s: %sXX",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_26(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "error reading active connection %s: %s",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_27(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "ERROR READING ACTIVE CONNECTION %S: %S",
                active_path,
                exc,
            )
        return ""

    async def xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_28(self, active_path: str) -> str:
        """Extract the Wi-Fi SSID from an active connection object path, or return ''."""
        try:
            conn_path = await self._active_conn(active_path).connection
            if not conn_path or conn_path == "/":
                return ""
            settings = await self._conn_settings(conn_path).get_settings()
            if "802-11-wireless" in settings:
                ssid = settings["802-11-wireless"]["ssid"][1].decode()
                return ssid
        except Exception as exc:
            logger.debug(
                "Error reading active connection %s: %s",
                active_path,
                exc,
            )
        return "XXXX"
    
    xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_1': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_2': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_3': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_4': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_5': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_6': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_7': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_8': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_9': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_10': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_11': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_12': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_13': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_14': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_15': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_16': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_17': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_18': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_19': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_20': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_21': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_22': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_23': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_24': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_25': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_26': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_27': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_28': xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_28
    }
    xǁNetworkManagerWorkerǁ_ssid_from_active_connection__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_ssid_from_active_connection'

    async def _get_ssid_from_any_active(self) -> str:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_orig(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("Error scanning active connections: %s", exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_1(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = None
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("Error scanning active connections: %s", exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_2(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = None
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("Error scanning active connections: %s", exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_3(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(None)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("Error scanning active connections: %s", exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_4(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug(None, exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_5(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("Error scanning active connections: %s", None)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_6(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug(exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_7(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("Error scanning active connections: %s", )
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_8(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("XXError scanning active connections: %sXX", exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_9(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("error scanning active connections: %s", exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_10(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("ERROR SCANNING ACTIVE CONNECTIONS: %S", exc)
        return ""

    async def xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_11(self) -> str:
        """Scan all active NM connections and return the first Wi-Fi SSID found."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                ssid = await self._ssid_from_active_connection(active_path)
                if ssid:
                    return ssid
        except Exception as exc:
            logger.debug("Error scanning active connections: %s", exc)
        return "XXXX"
    
    xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_1': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_2': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_3': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_4': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_5': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_6': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_7': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_8': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_9': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_10': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_11': xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_11
    }
    xǁNetworkManagerWorkerǁ_get_ssid_from_any_active__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_ssid_from_any_active'

    async def _get_current_ip(self) -> str:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_orig(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_1(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = None
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_2(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con != "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_3(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "XX/XX":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_4(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return "XXXX"
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_5(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = None
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_6(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(None).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_7(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path != "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_8(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "XX/XX":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_9(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return "XXXX"
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_10(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = None
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_11(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(None).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_12(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[1]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_13(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["XXaddressXX"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_14(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["ADDRESS"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_15(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][2]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_16(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return "XXXX"
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_17(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug(None, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_18(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", None)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_19(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug(exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_20(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", )
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_21(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("XXError getting current IP: %sXX", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_22(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("error getting current ip: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_23(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("ERROR GETTING CURRENT IP: %S", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_24(self) -> str:
        """Return the IPv4 address from the primary NM connection's IP4Config."""
        try:
            primary_con = await self._nm().primary_connection
            if primary_con == "/":
                return ""
            ip4_path = await self._active_conn(primary_con).ip4_config
            if ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.debug("Error getting current IP: %s", exc)
            return "XXXX"
    
    xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_1': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_2': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_3': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_4': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_5': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_6': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_7': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_8': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_9': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_10': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_11': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_12': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_13': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_14': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_15': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_16': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_17': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_18': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_19': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_20': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_21': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_22': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_23': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_24': xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_24
    }
    xǁNetworkManagerWorkerǁ_get_current_ip__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_current_ip'

    async def _get_ip_by_interface(self, interface: str = "wlan0") -> str:
        args = [interface]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_orig(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_1(self, interface: str = "XXwlan0XX") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_2(self, interface: str = "WLAN0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_3(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = None
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_4(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(None)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_5(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_6(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = None
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_7(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(None).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_8(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface != interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_9(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = None
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_10(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = None
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_11(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        return
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_12(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_13(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return "XXXX"
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_14(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = None
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_15(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(None).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_16(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path and ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_17(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_18(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path != "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_19(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "XX/XX":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_20(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return "XXXX"
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_21(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = None
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_22(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(None).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_23(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[1]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_24(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["XXaddressXX"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_25(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["ADDRESS"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_26(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][2]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_27(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return "XXXX"
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_28(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error(None, interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_29(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", None, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_30(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, None)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_31(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error(interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_32(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_33(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, )
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_34(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("XXFailed to get IP for %s: %sXX", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_35(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("failed to get ip for %s: %s", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_36(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("FAILED TO GET IP FOR %S: %S", interface, exc)
            return ""

    async def xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_37(self, interface: str = "wlan0") -> str:
        """Return the IPv4 address assigned to *interface* via NM's IP4Config D-Bus object."""
        try:
            device_path = self._iface_to_device_path.get(interface)
            if not device_path:
                devices = await self._nm().get_devices()
                for dp in devices:
                    if await self._generic(dp).interface == interface:
                        device_path = dp
                        self._iface_to_device_path[interface] = dp
                        break
            if not device_path:
                return ""
            ip4_path = await self._generic(device_path).ip4_config
            if not ip4_path or ip4_path == "/":
                return ""
            addr_data = await self._ipv4(ip4_path).address_data
            if addr_data:
                return addr_data[0]["address"][1]
            return ""
        except Exception as exc:
            logger.error("Failed to get IP for %s: %s", interface, exc)
            return "XXXX"
    
    xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_1': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_2': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_3': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_4': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_5': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_6': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_7': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_8': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_9': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_10': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_11': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_12': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_13': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_14': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_15': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_16': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_17': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_18': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_19': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_20': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_21': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_22': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_23': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_24': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_25': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_26': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_27': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_28': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_29': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_30': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_31': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_32': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_33': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_34': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_35': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_36': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_37': xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_37
    }
    xǁNetworkManagerWorkerǁ_get_ip_by_interface__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_ip_by_interface'

    async def _async_scan_networks(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_orig(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_1(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_2(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit(None)
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_3(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_4(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit(None)
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_5(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_6(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit(None)
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_7(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan(None)
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_8(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    None, exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_9(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", None
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_10(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_11(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_12(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "XXScan request ignored (already scanning or radio off): %sXX", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_13(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_14(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "SCAN REQUEST IGNORED (ALREADY SCANNING OR RADIO OFF): %S", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_15(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan != -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_16(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == +1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_17(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -2:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_18(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit(None)
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_19(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = None
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_20(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = None
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_21(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = None

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_22(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(None)

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_23(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = None
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_24(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = None

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_25(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = None
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_26(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(None, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_27(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, None, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_28(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, None)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_29(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_30(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_31(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, )
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_32(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid) or (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_33(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids or not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_34(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info or info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_35(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_36(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_37(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(None)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_38(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 and info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_39(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength >= 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_40(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 1 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_41(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(None)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_42(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(None)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_43(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug(None, ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_44(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", None, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_45(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, None)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_46(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug(ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_47(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_48(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, )

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_49(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("XXFailed to parse AP %s: %sXX", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_50(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("failed to parse ap %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_51(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("FAILED TO PARSE AP %S: %S", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_52(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=None)
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_53(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: None)
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_54(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (+n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_55(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, +n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_56(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(None)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_57(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error(None, exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_58(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", None)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_59(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error(exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_60(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", )
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_61(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("XXFailed to scan networks: %sXX", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_62(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_63(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("FAILED TO SCAN NETWORKS: %S", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_64(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit(None, str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_65(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", None)
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_66(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit(str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_67(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", )
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_68(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("XXscan_networksXX", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_69(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("SCAN_NETWORKS", str(exc))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_70(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(None))
            self.networks_scanned.emit([])

    async def xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_71(self) -> None:
        """Request an NM rescan, parse visible APs, and emit networks_scanned."""
        try:
            if not self._primary_wifi_path:
                self.networks_scanned.emit([])
                return
            if not await self._ensure_dbus_connection():
                self.networks_scanned.emit([])
                return

            if not await self._nm().wireless_enabled:
                self.networks_scanned.emit([])
                return

            try:
                await self._wifi().request_scan({})
            except Exception as exc:
                logger.debug(
                    "Scan request ignored (already scanning or radio off): %s", exc
                )

            if await self._wifi().last_scan == -1:
                self.networks_scanned.emit([])
                return

            ap_paths = await self._wifi().get_all_access_points()
            current_ssid = await self._get_current_ssid()
            saved_ssids = set(await self._get_saved_ssid_names_cached())

            networks: list[NetworkInfo] = []
            seen_ssids: set[str] = set()

            for ap_path in ap_paths:
                try:
                    info = await self._parse_ap(ap_path, current_ssid, saved_ssids)
                    if (
                        info
                        and info.ssid not in seen_ssids
                        and not is_hidden_ssid(info.ssid)
                        and (info.signal_strength > 0 or info.is_active)
                    ):
                        networks.append(info)
                        seen_ssids.add(info.ssid)
                except Exception as exc:
                    logger.debug("Failed to parse AP %s: %s", ap_path, exc)

            networks.sort(key=lambda n: (-n.network_status, -n.signal_strength))
            self.networks_scanned.emit(networks)

        except Exception as exc:
            logger.error("Failed to scan networks: %s", exc)
            self.error_occurred.emit("scan_networks", str(exc))
            self.networks_scanned.emit(None)
    
    xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_1': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_2': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_3': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_4': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_5': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_6': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_7': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_8': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_9': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_10': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_11': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_12': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_13': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_14': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_15': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_16': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_17': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_18': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_19': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_20': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_21': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_22': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_23': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_24': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_25': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_26': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_27': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_28': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_29': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_30': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_31': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_32': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_33': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_34': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_35': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_36': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_37': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_38': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_39': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_40': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_41': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_42': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_43': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_44': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_45': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_46': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_47': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_48': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_49': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_50': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_51': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_52': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_53': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_54': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_55': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_56': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_57': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_58': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_59': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_60': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_61': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_62': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_63': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_64': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_65': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_66': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_67': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_68': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_69': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_70': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_71': xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_71
    }
    xǁNetworkManagerWorkerǁ_async_scan_networks__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_scan_networks'

    async def _get_all_ap_properties(self, ap_path: str) -> dict[str, object]:
        args = [ap_path]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_orig(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_1(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member=None
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_2(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(None).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_3(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="XXignoreXX"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_4(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="IGNORE"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_5(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug(None, ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_6(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", None, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_7(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", ap_path, None)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_8(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug(ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_9(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_10(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("GetAll failed for AP %s: %s", ap_path, )
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_11(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("XXGetAll failed for AP %s: %sXX", ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_12(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("getall failed for ap %s: %s", ap_path, exc)
            return {}

    async def xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_13(self, ap_path: str) -> dict[str, object]:
        """Fetch all D-Bus properties for an AccessPoint in one round-trip."""
        try:
            return await self._ap(ap_path).properties_get_all_dict(
                on_unknown_member="ignore"
            )
        except Exception as exc:
            logger.debug("GETALL FAILED FOR AP %S: %S", ap_path, exc)
            return {}
    
    xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_1': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_2': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_3': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_4': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_5': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_6': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_7': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_8': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_9': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_10': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_11': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_12': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_13': xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_13
    }
    xǁNetworkManagerWorkerǁ_get_all_ap_properties__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_all_ap_properties'

    async def _build_signal_map(self) -> dict[str, int]:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_orig(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_1(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = None
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_2(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_3(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = None
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_4(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = None
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_5(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(None)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_6(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = None
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_7(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(None)
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_8(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get(None, b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_9(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", None))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_10(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get(b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_11(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", ))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_12(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("XXssidXX", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_13(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("SSID", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_14(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b"XXXX"))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_15(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = None
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_16(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(None)
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_17(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get(None, 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_18(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", None))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_19(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get(0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_20(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", ))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_21(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("XXstrengthXX", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_22(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("STRENGTH", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_23(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 1))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_24(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = None
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_25(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.upper()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_26(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength >= signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_27(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(None, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_28(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, None):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_29(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_30(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, ):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_31(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 1):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_32(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = None
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_33(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug(None, exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_34(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", None)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_35(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug(exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_36(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", )
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_37(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("XXSkipping AP in signal map: %sXX", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_38(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("skipping ap in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_39(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("SKIPPING AP IN SIGNAL MAP: %S", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_40(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    break
        except Exception as exc:
            logger.debug("Error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_41(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug(None, exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_42(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", None)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_43(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug(exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_44(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error building signal map: %s", )
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_45(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("XXError building signal map: %sXX", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_46(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("error building signal map: %s", exc)
        return signal_map

    async def xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_47(self) -> dict[str, int]:
        """Return a mapping of lowercase SSID to best-seen signal strength (0-100)."""
        signal_map: dict[str, int] = {}
        if not self._primary_wifi_path:
            return signal_map
        try:
            ap_paths = await self._wifi().access_points
            for ap_path in ap_paths:
                try:
                    props = await self._get_all_ap_properties(ap_path)
                    ssid = self._decode_ssid(props.get("ssid", b""))
                    if ssid:
                        strength = int(props.get("strength", 0))
                        key = ssid.lower()
                        if strength > signal_map.get(key, 0):
                            signal_map[key] = strength
                except Exception as exc:
                    logger.debug("Skipping AP in signal map: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("ERROR BUILDING SIGNAL MAP: %S", exc)
        return signal_map
    
    xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_1': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_2': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_3': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_4': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_5': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_6': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_7': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_8': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_9': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_10': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_11': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_12': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_13': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_14': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_15': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_16': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_17': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_18': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_19': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_20': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_21': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_22': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_23': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_24': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_25': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_26': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_27': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_28': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_29': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_30': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_31': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_32': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_33': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_34': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_35': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_36': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_37': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_38': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_39': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_40': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_41': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_42': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_43': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_44': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_45': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_46': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_47': xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_47
    }
    xǁNetworkManagerWorkerǁ_build_signal_map__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_build_signal_map'

    async def _parse_ap(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        args = [ap_path, current_ssid, saved_ssids]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_orig(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_1(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = None
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_2(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(None)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_3(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_4(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = None
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_5(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(None)
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_6(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get(None, b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_7(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", None))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_8(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get(b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_9(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", ))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_10(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("XXssidXX", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_11(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("SSID", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_12(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b"XXXX"))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_13(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid and is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_14(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_15(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(None):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_16(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = None
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_17(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(None)
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_18(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get(None, 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_19(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", None))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_20(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get(0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_21(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", ))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_22(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("XXflagsXX", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_23(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("FLAGS", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_24(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 1))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_25(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = None
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_26(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(None)
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_27(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get(None, 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_28(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", None))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_29(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get(0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_30(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", ))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_31(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("XXwpa_flagsXX", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_32(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("WPA_FLAGS", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_33(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 1))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_34(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = None
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_35(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(None)
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_36(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get(None, 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_37(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", None))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_38(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get(0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_39(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", ))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_40(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("XXrsn_flagsXX", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_41(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("RSN_FLAGS", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_42(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 1))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_43(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = None

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_44(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags | 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_45(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 2) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_46(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) != 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_47(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 1

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_48(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = None
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_49(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(None, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_50(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, None, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_51(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, None)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_52(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_53(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_54(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, )
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_55(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_56(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(None):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_57(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = None
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_58(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid != current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_59(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = None
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_60(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid not in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_61(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = None
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_62(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = None
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_63(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = None
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_64(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = None

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_65(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=None,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_66(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=None,
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_67(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=None,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_68(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=None,
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_69(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=None,
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_70(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=None,
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_71(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=None,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_72(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_73(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_74(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_75(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_76(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_77(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_78(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_79(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(None),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_80(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get(None, 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_81(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", None)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_82(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get(0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_83(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", )),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_84(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("XXstrengthXX", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_85(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("STRENGTH", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_86(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 1)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_87(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(None),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_88(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get(None, "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_89(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", None)),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_90(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_91(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", )),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_92(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("XXhw_addressXX", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_93(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("HW_ADDRESS", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_94(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "XXXX")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_95(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(None),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_96(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get(None, 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_97(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", None)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_98(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get(0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_99(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", )),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_100(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("XXfrequencyXX", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_101(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("FREQUENCY", 0)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_102(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 1)),
            max_bitrate=int(props.get("max_bitrate", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_103(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(None),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_104(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get(None, 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_105(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", None)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_106(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get(0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_107(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", )),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_108(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("XXmax_bitrateXX", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_109(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("MAX_BITRATE", 0)),
            security_type=security,
        )

    async def xǁNetworkManagerWorkerǁ_parse_ap__mutmut_110(
        self, ap_path: str, current_ssid: str, saved_ssids: set
    ) -> NetworkInfo | None:
        """Parse an AccessPoint D-Bus object into a NetworkInfo, or None if unusable."""
        props = await self._get_all_ap_properties(ap_path)
        if not props:
            return None

        ssid = self._decode_ssid(props.get("ssid", b""))
        if not ssid or is_hidden_ssid(ssid):
            return None

        flags = int(props.get("flags", 0))
        wpa_flags = int(props.get("wpa_flags", 0))
        rsn_flags = int(props.get("rsn_flags", 0))
        is_open = (flags & 1) == 0

        security = self._determine_security_type(flags, wpa_flags, rsn_flags)
        if not is_connectable_security(security):
            return None

        is_active = ssid == current_ssid
        is_saved = ssid in saved_ssids
        if is_active:
            net_status = NetworkStatus.ACTIVE
        elif is_saved:
            net_status = NetworkStatus.SAVED
        elif is_open:
            net_status = NetworkStatus.OPEN
        else:
            net_status = NetworkStatus.DISCOVERED

        return NetworkInfo(
            ssid=ssid,
            signal_strength=int(props.get("strength", 0)),
            network_status=net_status,
            bssid=str(props.get("hw_address", "")),
            frequency=int(props.get("frequency", 0)),
            max_bitrate=int(props.get("max_bitrate", 1)),
            security_type=security,
        )
    
    xǁNetworkManagerWorkerǁ_parse_ap__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_1': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_2': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_3': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_4': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_5': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_6': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_7': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_8': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_9': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_10': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_11': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_12': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_13': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_14': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_15': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_16': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_17': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_18': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_19': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_20': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_21': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_22': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_23': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_24': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_25': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_26': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_27': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_28': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_29': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_30': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_31': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_32': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_33': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_34': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_35': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_36': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_37': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_38': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_39': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_40': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_41': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_42': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_43': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_44': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_45': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_46': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_47': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_48': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_49': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_50': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_51': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_52': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_53': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_54': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_55': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_56': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_57': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_58': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_59': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_60': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_61': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_62': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_63': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_64': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_65': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_66': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_67': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_68': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_69': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_70': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_71': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_72': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_73': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_74': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_75': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_76': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_77': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_78': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_79': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_80': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_81': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_82': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_83': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_84': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_85': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_86': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_87': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_88': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_89': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_90': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_91': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_92': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_93': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_94': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_95': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_96': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_97': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_98': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_99': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_100': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_101': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_102': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_103': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_104': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_105': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_106': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_107': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_108': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_109': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_parse_ap__mutmut_110': xǁNetworkManagerWorkerǁ_parse_ap__mutmut_110
    }
    xǁNetworkManagerWorkerǁ_parse_ap__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_parse_ap'

    @staticmethod
    def _decode_ssid(raw: object) -> str:
        """Decode a raw SSID byte string to a UTF-8 str, replacing invalid bytes."""
        if isinstance(raw, bytes):
            return raw.decode("utf-8", errors="replace")
        return str(raw) if raw else ""

    @staticmethod
    def _determine_security_type(
        flags: int, wpa_flags: int, rsn_flags: int
    ) -> SecurityType:
        """Determine the Wi-Fi SecurityType from AP capability flags."""
        if (flags & 1) == 0:
            return SecurityType.OPEN
        if rsn_flags:
            if rsn_flags & 0x400:
                return SecurityType.WPA3_SAE
            if rsn_flags & 0x200:
                return SecurityType.WPA_EAP
            return SecurityType.WPA2_PSK
        if wpa_flags:
            if wpa_flags & 0x200:
                return SecurityType.WPA_EAP
            return SecurityType.WPA_PSK
        return SecurityType.WEP

    def _invalidate_saved_cache(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_orig(self) -> None:
        """Mark the saved-networks cache as dirty so it is rebuilt on next access."""
        self._saved_cache_dirty = True

    def xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_1(self) -> None:
        """Mark the saved-networks cache as dirty so it is rebuilt on next access."""
        self._saved_cache_dirty = None

    def xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_2(self) -> None:
        """Mark the saved-networks cache as dirty so it is rebuilt on next access."""
        self._saved_cache_dirty = False
    
    xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_1': xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_2': xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_2
    }
    xǁNetworkManagerWorkerǁ_invalidate_saved_cache__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_invalidate_saved_cache'

    async def _get_saved_ssid_names_cached(self) -> list[str]:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_orig(self) -> list[str]:
        """Return SSID names for all saved Wi-Fi profiles, refreshing cache if dirty."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = False
        return [n.ssid for n in self._saved_cache]

    async def xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_1(self) -> list[str]:
        """Return SSID names for all saved Wi-Fi profiles, refreshing cache if dirty."""
        if self._saved_cache_dirty:
            self._saved_cache = None
            self._saved_cache_dirty = False
        return [n.ssid for n in self._saved_cache]

    async def xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_2(self) -> list[str]:
        """Return SSID names for all saved Wi-Fi profiles, refreshing cache if dirty."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = None
        return [n.ssid for n in self._saved_cache]

    async def xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_3(self) -> list[str]:
        """Return SSID names for all saved Wi-Fi profiles, refreshing cache if dirty."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = True
        return [n.ssid for n in self._saved_cache]
    
    xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_1': xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_2': xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_3': xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_3
    }
    xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_saved_ssid_names_cached'

    async def _get_saved_network_cached(self, ssid: str) -> SavedNetwork | None:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_orig(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = False
        ssid_lower = ssid.lower()
        for n in self._saved_cache:
            if n.ssid.lower() == ssid_lower:
                return n
        return None

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_1(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = None
            self._saved_cache_dirty = False
        ssid_lower = ssid.lower()
        for n in self._saved_cache:
            if n.ssid.lower() == ssid_lower:
                return n
        return None

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_2(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = None
        ssid_lower = ssid.lower()
        for n in self._saved_cache:
            if n.ssid.lower() == ssid_lower:
                return n
        return None

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_3(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = True
        ssid_lower = ssid.lower()
        for n in self._saved_cache:
            if n.ssid.lower() == ssid_lower:
                return n
        return None

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_4(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = False
        ssid_lower = None
        for n in self._saved_cache:
            if n.ssid.lower() == ssid_lower:
                return n
        return None

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_5(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = False
        ssid_lower = ssid.upper()
        for n in self._saved_cache:
            if n.ssid.lower() == ssid_lower:
                return n
        return None

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_6(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = False
        ssid_lower = ssid.lower()
        for n in self._saved_cache:
            if n.ssid.upper() == ssid_lower:
                return n
        return None

    async def xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_7(self, ssid: str) -> SavedNetwork | None:
        """Return the SavedNetwork for *ssid* from cache (case-insensitive), or None."""
        if self._saved_cache_dirty:
            self._saved_cache = await self._get_saved_networks_impl()
            self._saved_cache_dirty = False
        ssid_lower = ssid.lower()
        for n in self._saved_cache:
            if n.ssid.lower() != ssid_lower:
                return n
        return None
    
    xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_1': xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_2': xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_3': xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_4': xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_5': xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_6': xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_7': xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_7
    }
    xǁNetworkManagerWorkerǁ_get_saved_network_cached__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_saved_network_cached'

    async def _async_load_saved_networks(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_orig(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_1(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = None
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_2(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = None
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_3(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = None
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_4(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = True
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_5(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(None)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_6(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error(None, exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_7(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", None)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_8(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error(exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_9(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", )
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_10(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("XXFailed to load saved networks: %sXX", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_11(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_12(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("FAILED TO LOAD SAVED NETWORKS: %S", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_13(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit(None, str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_14(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", None)
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_15(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit(str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_16(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", )
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_17(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("XXload_saved_networksXX", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_18(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("LOAD_SAVED_NETWORKS", str(exc))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_19(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(None))
            self.saved_networks_loaded.emit([])

    async def xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_20(self) -> None:
        """Reload all saved Wi-Fi profiles and emit saved_networks_loaded."""
        try:
            networks = await self._get_saved_networks_impl()
            self._saved_cache = networks
            self._saved_cache_dirty = False
            self.saved_networks_loaded.emit(networks)
        except Exception as exc:
            logger.error("Failed to load saved networks: %s", exc)
            self.error_occurred.emit("load_saved_networks", str(exc))
            self.saved_networks_loaded.emit(None)
    
    xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_1': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_2': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_3': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_4': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_5': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_6': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_7': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_8': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_9': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_10': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_11': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_12': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_13': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_14': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_15': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_16': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_17': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_18': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_19': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_20': xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_20
    }
    xǁNetworkManagerWorkerǁ_async_load_saved_networks__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_load_saved_networks'

    async def _get_saved_networks_impl(self) -> list[SavedNetwork]:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_orig(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_1(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_2(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = None
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_3(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = None
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_4(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = None

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_5(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = None
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_6(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(None).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_7(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["XXconnectionXX"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_8(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["CONNECTION"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_9(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["XXtypeXX"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_10(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["TYPE"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_11(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][2] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_12(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] == "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_13(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "XX802-11-wirelessXX":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_14(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-WIRELESS":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_15(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        break

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_16(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = None
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_17(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["XX802-11-wirelessXX"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_18(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-WIRELESS"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_19(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = None
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_20(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["XXssidXX"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_21(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["SSID"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_22(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][2].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_23(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = None
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_24(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["XXconnectionXX"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_25(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["CONNECTION"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_26(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["XXuuidXX"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_27(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["UUID"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_28(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][2]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_29(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = None

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_30(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(None)

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_31(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get(None, (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_32(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", None)[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_33(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get((None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_34(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", )[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_35(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("XXmodeXX", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_36(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("MODE", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_37(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "XXinfrastructureXX"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_38(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "INFRASTRUCTURE"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_39(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[2])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_40(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = None
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_41(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(None)
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_42(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get(None, (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_43(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", None)[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_44(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get((None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_45(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", )[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_46(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("XXsecurityXX", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_47(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("SECURITY", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_48(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, "XXXX"))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_49(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[2])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_50(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = None
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_51(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = "XXXX"
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_52(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key or security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_53(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key not in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_54(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = None

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_55(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get(None, (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_56(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", None)[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_57(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get((None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_58(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", )[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_59(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("XXkey-mgmtXX", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_60(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("KEY-MGMT", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_61(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, "XXXX"))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_62(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[2]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_63(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = None
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_64(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        None,
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_65(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        None,
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_66(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_67(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_68(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["XXconnectionXX"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_69(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["CONNECTION"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_70(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "XXautoconnect-priorityXX",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_71(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "AUTOCONNECT-PRIORITY",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_72(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[2]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_73(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = None
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_74(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get(None, (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_75(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", None)[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_76(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get((None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_77(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", )[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_78(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["XXconnectionXX"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_79(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["CONNECTION"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_80(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("XXtimestampXX", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_81(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("TIMESTAMP", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_82(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 1))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_83(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[2]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_84(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = None
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_85(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(None, 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_86(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), None)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_87(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_88(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), )
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_89(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.upper(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_90(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 1)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_91(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = None
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_92(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        None, (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_93(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", None
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_94(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_95(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_96(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get(None, {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_97(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", None).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_98(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get({}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_99(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", ).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_100(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("XXipv4XX", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_101(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("IPV4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_102(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "XXmethodXX", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_103(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "METHOD", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_104(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "XXautoXX")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_105(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "AUTO")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_106(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[2]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_107(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = None

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_108(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method == "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_109(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "XXmanualXX"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_110(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "MANUAL"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_111(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        None
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_112(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=None,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_113(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=None,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_114(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=None,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_115(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=None,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_116(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=None,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_117(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=None,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_118(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=None,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_119(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=None,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_120(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=None,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_121(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_122(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_123(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_124(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_125(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_126(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_127(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_128(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_129(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_130(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority and ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_131(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(None),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_132(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp and 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_133(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 1),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_134(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug(None, exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_135(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", None)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_136(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug(exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_137(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", )

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_138(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("XXFailed to parse connection: %sXX", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_139(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_140(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("FAILED TO PARSE CONNECTION: %S", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_141(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error(None, exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_142(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", None)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_143(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error(exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_144(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("Error getting saved networks: %s", )
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_145(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("XXError getting saved networks: %sXX", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_146(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("error getting saved networks: %s", exc)
            return []

    async def xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_147(self) -> list[SavedNetwork]:
        """Enumerate NM connection profiles and return infrastructure Wi-Fi ones."""
        if not self._system_bus:
            return []
        try:
            connections = await self._nm_settings().list_connections()
            signal_map = await self._build_signal_map()
            saved: list[SavedNetwork] = []

            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue

                    wireless = settings["802-11-wireless"]
                    ssid = wireless["ssid"][1].decode()
                    uuid = settings["connection"]["uuid"][1]
                    mode = str(wireless.get("mode", (None, "infrastructure"))[1])

                    security_key = str(wireless.get("security", (None, ""))[1])
                    sec_type = ""
                    if security_key and security_key in settings:
                        sec_type = settings[security_key].get("key-mgmt", (None, ""))[1]

                    priority = settings["connection"].get(
                        "autoconnect-priority",
                        (None, ConnectionPriority.MEDIUM.value),
                    )[1]
                    timestamp = settings["connection"].get("timestamp", (None, 0))[1]
                    signal = signal_map.get(ssid.lower(), 0)
                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    saved.append(
                        SavedNetwork(
                            ssid=ssid,
                            uuid=uuid,
                            connection_path=conn_path,
                            security_type=sec_type,
                            mode=mode,
                            priority=priority or ConnectionPriority.MEDIUM.value,
                            signal_strength=signal,
                            timestamp=int(timestamp or 0),
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Failed to parse connection: %s", exc)

            return saved
        except Exception as exc:
            logger.error("ERROR GETTING SAVED NETWORKS: %S", exc)
            return []
    
    xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_1': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_2': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_3': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_4': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_5': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_6': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_7': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_8': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_9': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_10': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_11': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_12': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_13': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_14': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_15': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_16': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_17': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_18': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_19': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_20': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_21': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_22': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_23': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_24': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_25': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_26': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_27': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_28': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_29': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_30': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_31': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_32': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_33': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_34': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_35': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_36': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_37': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_38': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_39': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_40': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_41': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_42': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_43': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_44': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_45': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_46': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_47': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_48': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_49': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_50': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_51': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_52': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_53': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_54': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_55': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_56': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_57': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_58': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_59': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_60': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_61': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_62': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_63': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_64': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_65': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_66': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_67': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_68': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_69': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_70': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_71': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_72': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_73': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_74': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_75': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_76': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_77': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_78': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_79': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_80': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_81': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_82': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_83': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_84': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_85': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_86': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_87': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_88': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_89': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_90': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_91': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_92': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_93': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_94': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_95': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_96': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_97': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_98': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_99': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_100': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_101': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_102': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_103': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_104': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_105': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_106': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_107': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_108': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_109': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_110': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_110, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_111': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_111, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_112': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_112, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_113': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_113, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_114': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_114, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_115': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_115, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_116': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_116, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_117': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_117, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_118': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_118, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_119': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_119, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_120': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_120, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_121': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_121, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_122': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_122, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_123': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_123, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_124': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_124, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_125': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_125, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_126': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_126, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_127': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_127, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_128': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_128, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_129': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_129, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_130': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_130, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_131': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_131, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_132': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_132, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_133': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_133, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_134': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_134, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_135': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_135, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_136': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_136, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_137': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_137, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_138': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_138, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_139': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_139, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_140': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_140, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_141': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_141, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_142': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_142, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_143': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_143, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_144': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_144, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_145': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_145, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_146': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_146, 
        'xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_147': xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_147
    }
    xǁNetworkManagerWorkerǁ_get_saved_networks_impl__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_saved_networks_impl'

    async def _is_known(self, ssid: str) -> bool:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_is_known__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_is_known__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_is_known__mutmut_orig(self, ssid: str) -> bool:
        """Return True if a saved profile for *ssid* exists in the cache."""
        return await self._get_saved_network_cached(ssid) is not None

    async def xǁNetworkManagerWorkerǁ_is_known__mutmut_1(self, ssid: str) -> bool:
        """Return True if a saved profile for *ssid* exists in the cache."""
        return await self._get_saved_network_cached(None) is not None

    async def xǁNetworkManagerWorkerǁ_is_known__mutmut_2(self, ssid: str) -> bool:
        """Return True if a saved profile for *ssid* exists in the cache."""
        return await self._get_saved_network_cached(ssid) is None
    
    xǁNetworkManagerWorkerǁ_is_known__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_is_known__mutmut_1': xǁNetworkManagerWorkerǁ_is_known__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_is_known__mutmut_2': xǁNetworkManagerWorkerǁ_is_known__mutmut_2
    }
    xǁNetworkManagerWorkerǁ_is_known__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_is_known'

    async def _get_connection_path(self, ssid: str) -> str | None:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_orig(self, ssid: str) -> str | None:
        """Return the D-Bus connection path for a saved *ssid* profile, or None."""
        saved = await self._get_saved_network_cached(ssid)
        return saved.connection_path if saved else None

    async def xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_1(self, ssid: str) -> str | None:
        """Return the D-Bus connection path for a saved *ssid* profile, or None."""
        saved = None
        return saved.connection_path if saved else None

    async def xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_2(self, ssid: str) -> str | None:
        """Return the D-Bus connection path for a saved *ssid* profile, or None."""
        saved = await self._get_saved_network_cached(None)
        return saved.connection_path if saved else None
    
    xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_1': xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_2': xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_2
    }
    xǁNetworkManagerWorkerǁ_get_connection_path__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_connection_path'

    async def _async_add_network(self, ssid: str, password: str, priority: int) -> None:
        args = [ssid, password, priority]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_orig(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_1(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = None
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_2(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(None, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_3(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, None, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_4(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, None)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_5(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_6(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_7(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_8(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(None)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_9(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error(None, exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_10(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", None)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_11(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error(exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_12(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", )
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_13(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("XXFailed to add network: %sXX", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_14(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_15(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("FAILED TO ADD NETWORK: %S", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_16(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_17(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=None,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_18(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=None,
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_19(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_20(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_21(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_22(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_23(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=True,
                    message=str(exc),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_24(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(None),
                    error_code="add_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_25(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="XXadd_failedXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_add_network__mutmut_26(self, ssid: str, password: str, priority: int) -> None:
        """Add and activate a new Wi-Fi profile, emitting connection_result when done."""
        try:
            result = await self._add_network_impl(ssid, password, priority)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Failed to add network: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="ADD_FAILED",
                )
            )
    
    xǁNetworkManagerWorkerǁ_async_add_network__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_1': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_2': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_3': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_4': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_5': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_6': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_7': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_8': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_9': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_10': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_11': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_12': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_13': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_14': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_15': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_16': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_17': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_18': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_19': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_20': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_21': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_22': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_23': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_24': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_25': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_add_network__mutmut_26': xǁNetworkManagerWorkerǁ_async_add_network__mutmut_26
    }
    xǁNetworkManagerWorkerǁ_async_add_network__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_add_network'

    async def _add_network_impl(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        args = [ssid, password, priority]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_orig(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_1(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path and not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_2(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_3(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_4(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(None, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_5(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, None, "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_6(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", None)

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_7(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult("No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_8(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_9(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", )

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_10(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(True, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_11(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "XXNo Wi-Fi interfaceXX", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_12(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "no wi-fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_13(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "NO WI-FI INTERFACE", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_14(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "XXno_interfaceXX")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_15(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "NO_INTERFACE")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_16(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(None):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_17(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(None)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_18(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan(None)
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_19(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug(None, exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_20(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", None)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_21(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug(exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_22(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", )

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_23(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("XXPre-connect scan request ignored: %sXX", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_24(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_25(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("PRE-CONNECT SCAN REQUEST IGNORED: %S", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_26(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = None
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_27(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = ""
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_28(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = None
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_29(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = None
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_30(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(None)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_31(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(None) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_32(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get(None, b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_33(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", None)) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_34(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get(b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_35(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", )) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_36(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("XXssidXX", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_37(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("SSID", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_38(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"XXXX")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_39(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) != ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_40(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = None
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_41(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = None
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_42(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                return

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_43(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_44(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(None, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_45(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, None, "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_46(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", None)

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_47(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_48(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_49(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", )

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_50(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(True, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_51(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "XXnot_foundXX")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_52(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "NOT_FOUND")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_53(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = None
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_54(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = None
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_55(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            None, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_56(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, None, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_57(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, None, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_58(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, None, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_59(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, None
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_60(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_61(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_62(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_63(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_64(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_65(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_66(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                None,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_67(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                None,
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_68(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                None,
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_69(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_70(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_71(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_72(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                True,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_73(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "XXUnsupported security typeXX",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_74(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_75(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "UNSUPPORTED SECURITY TYPE",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_76(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "XXunsupported_securityXX",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_77(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "UNSUPPORTED_SECURITY",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_78(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = None
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_79(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = None
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_80(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(None)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_81(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = None
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_82(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).upper()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_83(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(None).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_84(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str or ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_85(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "XXpskXX" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_86(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "PSK" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_87(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" not in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_88(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str and "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_89(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("XXinvalidXX" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_90(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("INVALID" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_91(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" not in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_92(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "XXpropertyXX" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_93(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "PROPERTY" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_94(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" not in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_95(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    None,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_96(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    None,
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_97(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    None,
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_98(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_99(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_100(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_101(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    True,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_102(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "XXWrong password, try again.XX",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_103(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_104(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "WRONG PASSWORD, TRY AGAIN.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_105(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "XXinvalid_passwordXX",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_106(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "INVALID_PASSWORD",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_107(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(None, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_108(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, None, "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_109(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), None)

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_110(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_111(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_112(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), )

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_113(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(True, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_114(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(None), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_115(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "XXadd_failedXX")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_116(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "ADD_FAILED")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_117(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug(None, reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_118(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", None)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_119(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug(reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_120(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", )

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_121(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("XXreload_connections non-fatal: %sXX", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_122(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("RELOAD_CONNECTIONS NON-FATAL: %S", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_123(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(None)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_124(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_125(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(None, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_126(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=None):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_127(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_128(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, ):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_129(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(None)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_130(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    None,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_131(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    None,
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_132(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    None,
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_133(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_134(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_135(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_136(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    True,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_137(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "XXThe saved profile has been removed.\nXX"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_138(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "the saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_139(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "THE SAVED PROFILE HAS BEEN REMOVED.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_140(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "XXPlease check the password and try again.XX",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_141(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_142(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "PLEASE CHECK THE PASSWORD AND TRY AGAIN.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_143(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "XXauth_failedXX",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_144(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "AUTH_FAILED",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_145(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(None, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_146(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, None)
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_147(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_148(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, )
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_149(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(False, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_150(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning(None, act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_151(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", None)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_152(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning(act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_153(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", )
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_154(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("XXActivate after add failed: %sXX", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_155(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("activate after add failed: %s", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_156(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("ACTIVATE AFTER ADD FAILED: %S", act_err)
            return ConnectionResult(True, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_157(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(None, f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_158(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, None)

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_159(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(f"Network '{ssid}' added (activate manually)")

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_160(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(True, )

    async def xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_161(
        self, ssid: str, password: str, priority: int
    ) -> ConnectionResult:
        """Scan for the SSID, build a connection profile, add it to NM, and activate it.

        Deletes any pre-existing profile for the same SSID before adding.
        Returns a failed ConnectionResult if the SSID is not visible, the
        security type is unsupported, or the 20-second activation wait times out.
        """
        if not self._primary_wifi_path or not self._system_bus:
            return ConnectionResult(False, "No Wi-Fi interface", "no_interface")

        if await self._is_known(ssid):
            await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()

        try:
            await self._wifi().request_scan({})
        except Exception as exc:
            logger.debug("Pre-connect scan request ignored: %s", exc)

        ap_paths = await self._wifi().get_all_access_points()
        target_ap_path: str | None = None
        target_ap_props: dict[str, object] = {}
        for ap_path in ap_paths:
            props = await self._get_all_ap_properties(ap_path)
            if self._decode_ssid(props.get("ssid", b"")) == ssid:
                target_ap_path = ap_path
                target_ap_props = props
                break

        if not target_ap_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")

        interface = await self._wifi().interface
        conn_props = self._build_connection_properties(
            ssid, password, interface, priority, target_ap_props
        )
        if not conn_props:
            return ConnectionResult(
                False,
                "Unsupported security type",
                "unsupported_security",
            )

        try:
            nm_settings = self._nm_settings()
            conn_path = await nm_settings.add_connection(conn_props)
        except Exception as exc:
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "add_failed")

        if _CAN_RELOAD_CONNECTIONS:
            try:
                await self._nm_settings().reload_connections()
            except Exception as reload_err:
                logger.debug("reload_connections non-fatal: %s", reload_err)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                await self._delete_network_impl(ssid)
                self._invalidate_saved_cache()
                return ConnectionResult(
                    False,
                    f"Authentication failed for '{ssid}'.\n"
                    "The saved profile has been removed.\n"
                    "Please check the password and try again.",
                    "auth_failed",
                )
            return ConnectionResult(True, f"Network '{ssid}' added and connecting")
        except Exception as act_err:
            logger.warning("Activate after add failed: %s", act_err)
            return ConnectionResult(False, f"Network '{ssid}' added (activate manually)")
    
    xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_1': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_2': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_3': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_4': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_5': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_6': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_7': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_8': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_9': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_10': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_11': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_12': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_13': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_14': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_15': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_16': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_17': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_18': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_19': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_20': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_21': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_22': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_23': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_24': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_25': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_26': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_27': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_28': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_29': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_30': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_31': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_32': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_33': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_34': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_35': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_36': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_37': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_38': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_39': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_40': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_41': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_42': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_43': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_44': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_45': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_46': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_47': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_48': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_49': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_50': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_51': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_52': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_53': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_54': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_55': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_56': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_57': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_58': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_59': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_60': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_61': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_62': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_63': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_64': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_65': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_66': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_67': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_68': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_69': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_70': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_71': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_72': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_73': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_74': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_75': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_76': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_77': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_78': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_79': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_80': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_81': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_82': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_83': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_84': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_85': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_86': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_87': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_88': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_89': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_90': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_91': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_92': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_93': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_94': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_95': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_96': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_97': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_98': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_99': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_100': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_101': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_102': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_103': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_104': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_105': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_106': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_107': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_108': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_109': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_110': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_110, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_111': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_111, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_112': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_112, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_113': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_113, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_114': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_114, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_115': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_115, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_116': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_116, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_117': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_117, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_118': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_118, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_119': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_119, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_120': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_120, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_121': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_121, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_122': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_122, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_123': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_123, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_124': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_124, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_125': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_125, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_126': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_126, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_127': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_127, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_128': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_128, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_129': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_129, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_130': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_130, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_131': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_131, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_132': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_132, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_133': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_133, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_134': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_134, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_135': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_135, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_136': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_136, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_137': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_137, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_138': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_138, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_139': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_139, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_140': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_140, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_141': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_141, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_142': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_142, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_143': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_143, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_144': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_144, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_145': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_145, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_146': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_146, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_147': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_147, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_148': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_148, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_149': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_149, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_150': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_150, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_151': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_151, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_152': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_152, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_153': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_153, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_154': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_154, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_155': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_155, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_156': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_156, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_157': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_157, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_158': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_158, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_159': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_159, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_160': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_160, 
        'xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_161': xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_161
    }
    xǁNetworkManagerWorkerǁ_add_network_impl__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_add_network_impl'

    def _build_connection_properties(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        args = [ssid, password, interface, priority, ap_props]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_orig(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_1(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = None
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_2(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(None)
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_3(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get(None, 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_4(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", None))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_5(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get(0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_6(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", ))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_7(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("XXflagsXX", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_8(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("FLAGS", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_9(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 1))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_10(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = None
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_11(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(None)
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_12(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get(None, 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_13(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", None))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_14(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get(0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_15(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", ))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_16(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("XXwpa_flagsXX", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_17(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("WPA_FLAGS", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_18(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 1))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_19(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = None

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_20(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(None)

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_21(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get(None, 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_22(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", None))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_23(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get(0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_24(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", ))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_25(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("XXrsn_flagsXX", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_26(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("RSN_FLAGS", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_27(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 1))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_28(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = None

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_29(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "XXconnectionXX": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_30(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "CONNECTION": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_31(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "XXidXX": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_32(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "ID": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_33(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("XXsXX", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_34(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("S", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_35(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "XXuuidXX": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_36(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "UUID": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_37(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("XXsXX", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_38(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("S", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_39(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(None)),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_40(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "XXtypeXX": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_41(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "TYPE": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_42(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("XXsXX", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_43(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("S", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_44(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "XX802-11-wirelessXX"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_45(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-WIRELESS"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_46(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "XXinterface-nameXX": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_47(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "INTERFACE-NAME": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_48(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("XXsXX", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_49(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("S", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_50(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "XXautoconnectXX": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_51(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "AUTOCONNECT": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_52(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("XXbXX", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_53(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("B", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_54(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", False),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_55(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "XXautoconnect-priorityXX": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_56(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "AUTOCONNECT-PRIORITY": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_57(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("XXiXX", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_58(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("I", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_59(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "XX802-11-wirelessXX": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_60(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-WIRELESS": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_61(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "XXmodeXX": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_62(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "MODE": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_63(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("XXsXX", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_64(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("S", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_65(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "XXinfrastructureXX"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_66(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "INFRASTRUCTURE"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_67(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "XXssidXX": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_68(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "SSID": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_69(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("XXayXX", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_70(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("AY", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_71(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode(None)),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_72(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("XXutf-8XX")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_73(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("UTF-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_74(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "XXipv4XX": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_75(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "IPV4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_76(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "XXmethodXX": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_77(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "METHOD": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_78(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("XXsXX", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_79(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("S", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_80(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "XXautoXX"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_81(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "AUTO"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_82(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "XXroute-metricXX": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_83(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "ROUTE-METRIC": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_84(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("XXiXX", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_85(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("I", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_86(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 201),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_87(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "XXipv6XX": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_88(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "IPV6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_89(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"XXmethodXX": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_90(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"METHOD": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_91(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("XXsXX", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_92(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("S", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_93(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "XXautoXX")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_94(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "AUTO")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_95(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags | 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_96(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 2) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_97(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) != 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_98(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 1:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_99(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = None
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_100(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["XX802-11-wirelessXX"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_101(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-WIRELESS"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_102(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["XXsecurityXX"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_103(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["SECURITY"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_104(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "XXsXX",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_105(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "S",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_106(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "XX802-11-wireless-securityXX",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_107(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-WIRELESS-SECURITY",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_108(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = None

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_109(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(None, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_110(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, None, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_111(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, None)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_112(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_113(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_114(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, )

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_115(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_116(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(None):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_117(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                None,
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_118(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                None,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_119(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                None,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_120(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_121(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_122(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_123(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "XXRejecting connection to '%s': unsupported security %sXX",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_124(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_125(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "REJECTING CONNECTION TO '%S': UNSUPPORTED SECURITY %S",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_126(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security != SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_127(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = None
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_128(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool(None)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_129(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) and wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_130(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags | 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_131(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 257) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_132(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    None,
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_133(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    None,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_134(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_135(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_136(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "XXSAE transition for '%s' — using wpa-psk + PMF optionalXX",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_137(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "sae transition for '%s' — using wpa-psk + pmf optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_138(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE TRANSITION FOR '%S' — USING WPA-PSK + PMF OPTIONAL",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_139(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = None
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_140(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["XX802-11-wireless-securityXX"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_141(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-WIRELESS-SECURITY"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_142(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "XXkey-mgmtXX": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_143(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "KEY-MGMT": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_144(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("XXsXX", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_145(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("S", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_146(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "XXwpa-pskXX"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_147(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "WPA-PSK"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_148(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "XXauth-algXX": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_149(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "AUTH-ALG": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_150(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("XXsXX", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_151(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("S", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_152(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "XXopenXX"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_153(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "OPEN"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_154(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "XXpskXX": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_155(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "PSK": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_156(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("XXsXX", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_157(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("S", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_158(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "XXpmfXX": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_159(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "PMF": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_160(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("XXuXX", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_161(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("U", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_162(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_163(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug(None, ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_164(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", None)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_165(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug(ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_166(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_167(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("XXPure SAE detected for '%s'XX", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_168(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("pure sae detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_169(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("PURE SAE DETECTED FOR '%S'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_170(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = None
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_171(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["XX802-11-wireless-securityXX"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_172(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-WIRELESS-SECURITY"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_173(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "XXkey-mgmtXX": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_174(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "KEY-MGMT": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_175(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("XXsXX", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_176(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("S", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_177(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "XXsaeXX"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_178(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "SAE"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_179(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "XXauth-algXX": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_180(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "AUTH-ALG": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_181(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("XXsXX", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_182(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("S", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_183(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "XXopenXX"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_184(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "OPEN"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_185(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "XXpskXX": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_186(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "PSK": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_187(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("XXsXX", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_188(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("S", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_189(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "XXpmfXX": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_190(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "PMF": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_191(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("XXuXX", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_192(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("U", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_193(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 4),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_194(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security not in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_195(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = None
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_196(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["XX802-11-wireless-securityXX"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_197(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-WIRELESS-SECURITY"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_198(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "XXkey-mgmtXX": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_199(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "KEY-MGMT": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_200(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("XXsXX", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_201(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("S", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_202(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "XXwpa-pskXX"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_203(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "WPA-PSK"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_204(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "XXauth-algXX": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_205(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "AUTH-ALG": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_206(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("XXsXX", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_207(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("S", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_208(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "XXopenXX"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_209(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "OPEN"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_210(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "XXpskXX": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_211(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "PSK": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_212(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("XXsXX", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_213(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("S", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_214(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                None,
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_215(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                None,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_216(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                None,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_217(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_218(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_219(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "Unsupported security type '%s' for '%s'",
                security.value,
                )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_220(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "XXUnsupported security type '%s' for '%s'XX",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_221(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "unsupported security type '%s' for '%s'",
                security.value,
                ssid,
            )
            return None

        return props

    def xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_222(
        self,
        ssid: str,
        password: str,
        interface: str,
        priority: int,
        ap_props: dict[str, object],
    ) -> dict[str, object] | None:
        """Build NM connection property dict for *ssid* from its AP capability flags.

        Returns None if the security type is unsupported (e.g. WPA-EAP).
        Handles OPEN, WPA-PSK, WPA2-PSK, and WPA3-SAE (including SAE-transition).
        """
        flags = int(ap_props.get("flags", 0))
        wpa_flags = int(ap_props.get("wpa_flags", 0))
        rsn_flags = int(ap_props.get("rsn_flags", 0))

        props: dict[str, object] = {
            "connection": {
                "id": ("s", ssid),
                "uuid": ("s", str(uuid4())),
                "type": ("s", "802-11-wireless"),
                "interface-name": ("s", interface),
                "autoconnect": ("b", True),
                "autoconnect-priority": ("i", priority),
            },
            "802-11-wireless": {
                "mode": ("s", "infrastructure"),
                "ssid": ("ay", ssid.encode("utf-8")),
            },
            "ipv4": {
                "method": ("s", "auto"),
                "route-metric": ("i", 200),
            },
            "ipv6": {"method": ("s", "auto")},
        }

        if (flags & 1) == 0:
            return props

        props["802-11-wireless"]["security"] = (
            "s",
            "802-11-wireless-security",
        )
        security = self._determine_security_type(flags, wpa_flags, rsn_flags)

        if not is_connectable_security(security):
            logger.warning(
                "Rejecting connection to '%s': unsupported security %s",
                ssid,
                security.value,
            )
            return None

        if security == SecurityType.WPA3_SAE:
            has_psk = bool((rsn_flags & 0x100) or wpa_flags)
            if has_psk:
                logger.debug(
                    "SAE transition for '%s' — using wpa-psk + PMF optional",
                    ssid,
                )
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "wpa-psk"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 2),  # OPTIONAL — required for SAE-transition APs
                }
            else:
                logger.debug("Pure SAE detected for '%s'", ssid)
                props["802-11-wireless-security"] = {
                    "key-mgmt": ("s", "sae"),
                    "auth-alg": ("s", "open"),
                    "psk": ("s", password),
                    "pmf": ("u", 3),  # REQUIRED — mandatory for pure WPA3-SAE
                }
        elif security in (
            SecurityType.WPA2_PSK,
            SecurityType.WPA_PSK,
        ):
            props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "auth-alg": ("s", "open"),
                "psk": ("s", password),
            }
        else:
            logger.warning(
                "UNSUPPORTED SECURITY TYPE '%S' FOR '%S'",
                security.value,
                ssid,
            )
            return None

        return props
    
    xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_1': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_2': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_3': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_4': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_5': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_6': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_7': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_8': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_9': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_10': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_11': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_12': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_13': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_14': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_15': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_16': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_17': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_18': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_19': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_20': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_21': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_22': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_23': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_24': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_25': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_26': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_27': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_28': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_29': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_30': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_31': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_32': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_33': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_34': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_35': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_36': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_37': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_38': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_39': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_40': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_41': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_42': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_43': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_44': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_45': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_46': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_47': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_48': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_49': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_50': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_51': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_52': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_53': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_54': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_55': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_56': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_57': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_58': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_59': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_60': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_61': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_62': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_63': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_64': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_65': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_66': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_67': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_68': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_69': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_70': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_71': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_72': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_73': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_74': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_75': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_76': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_77': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_78': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_79': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_80': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_81': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_82': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_83': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_84': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_85': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_86': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_87': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_88': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_89': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_90': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_91': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_92': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_93': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_94': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_95': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_96': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_97': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_98': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_99': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_100': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_101': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_102': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_103': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_104': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_105': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_106': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_107': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_108': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_109': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_110': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_110, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_111': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_111, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_112': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_112, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_113': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_113, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_114': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_114, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_115': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_115, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_116': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_116, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_117': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_117, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_118': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_118, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_119': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_119, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_120': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_120, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_121': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_121, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_122': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_122, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_123': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_123, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_124': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_124, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_125': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_125, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_126': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_126, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_127': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_127, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_128': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_128, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_129': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_129, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_130': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_130, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_131': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_131, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_132': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_132, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_133': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_133, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_134': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_134, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_135': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_135, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_136': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_136, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_137': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_137, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_138': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_138, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_139': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_139, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_140': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_140, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_141': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_141, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_142': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_142, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_143': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_143, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_144': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_144, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_145': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_145, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_146': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_146, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_147': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_147, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_148': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_148, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_149': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_149, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_150': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_150, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_151': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_151, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_152': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_152, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_153': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_153, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_154': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_154, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_155': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_155, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_156': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_156, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_157': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_157, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_158': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_158, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_159': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_159, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_160': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_160, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_161': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_161, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_162': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_162, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_163': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_163, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_164': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_164, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_165': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_165, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_166': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_166, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_167': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_167, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_168': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_168, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_169': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_169, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_170': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_170, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_171': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_171, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_172': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_172, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_173': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_173, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_174': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_174, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_175': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_175, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_176': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_176, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_177': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_177, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_178': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_178, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_179': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_179, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_180': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_180, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_181': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_181, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_182': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_182, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_183': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_183, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_184': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_184, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_185': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_185, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_186': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_186, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_187': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_187, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_188': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_188, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_189': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_189, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_190': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_190, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_191': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_191, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_192': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_192, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_193': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_193, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_194': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_194, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_195': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_195, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_196': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_196, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_197': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_197, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_198': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_198, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_199': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_199, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_200': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_200, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_201': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_201, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_202': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_202, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_203': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_203, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_204': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_204, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_205': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_205, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_206': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_206, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_207': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_207, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_208': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_208, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_209': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_209, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_210': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_210, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_211': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_211, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_212': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_212, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_213': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_213, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_214': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_214, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_215': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_215, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_216': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_216, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_217': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_217, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_218': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_218, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_219': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_219, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_220': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_220, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_221': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_221, 
        'xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_222': xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_222
    }
    xǁNetworkManagerWorkerǁ_build_connection_properties__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_build_connection_properties'

    async def _async_connect_network(self, ssid: str) -> None:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_orig(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_1(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = None
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_2(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = True
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_3(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = None
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_4(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(None)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_5(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(None)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_6(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_7(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(None, exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_8(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", None)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_9(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_10(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", )
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_11(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("XXFailed to connect: %sXX", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_12(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_13(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("FAILED TO CONNECT: %S", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_14(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_15(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=None,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_16(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=None,
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_17(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_18(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_19(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_20(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_21(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=True,
                    message=str(exc),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_22(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(None),
                    error_code="connect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_23(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="XXconnect_failedXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_24(self, ssid: str) -> None:
        """Activate an existing saved Wi-Fi profile and emit connection_result."""
        try:
            self._is_hotspot_active = False
            result = await self._connect_network_impl(ssid)
            self.connection_result.emit(result)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="CONNECT_FAILED",
                )
            )
    
    xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_1': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_2': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_3': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_4': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_5': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_6': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_7': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_8': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_9': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_10': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_11': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_12': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_13': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_14': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_15': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_16': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_17': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_18': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_19': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_20': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_21': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_22': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_23': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_24': xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_24
    }
    xǁNetworkManagerWorkerǁ_async_connect_network__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_connect_network'

    async def _wait_for_connection(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        args = [ssid, timeout]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_orig(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_1(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = None
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_2(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = None
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_3(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() - timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_4(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(None)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_5(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(2.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_6(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = None
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_7(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 1
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_8(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() <= deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_9(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = None
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_10(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current or current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_11(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.upper() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_12(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() != ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_13(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.upper():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_14(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = None
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_15(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return False
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_16(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = None
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_17(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 1
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_18(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty = 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_19(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty -= 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_20(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 2
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_21(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty > 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_22(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 4:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_23(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return True
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_24(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug(None, exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_25(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", None)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_26(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug(exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_27(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", )
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_28(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("XXConnection wait poll failed: %sXX", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_29(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_30(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("CONNECTION WAIT POLL FAILED: %S", exc)
            await asyncio.sleep(0.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_31(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(None)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_32(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(1.5)
        return False

    async def xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_33(
        self, ssid: str, timeout: float = _WIFI_CONNECT_TIMEOUT
    ) -> bool:
        """Poll until *ssid* is active and has an IP, or until *timeout* expires.

        Starts with a 1.5 s initial delay to let NM begin the association.
        Returns False early if the SSID disappears for 3 consecutive polls.
        """
        loop = asyncio.get_running_loop()
        deadline = loop.time() + timeout
        await asyncio.sleep(1.5)
        consecutive_empty = 0
        while loop.time() < deadline:
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    ip = await self._get_current_ip()
                    if ip:
                        return True
                    consecutive_empty = 0
                else:
                    consecutive_empty += 1
                    if consecutive_empty >= 3:
                        return False
            except Exception as exc:
                logger.debug("Connection wait poll failed: %s", exc)
            await asyncio.sleep(0.5)
        return True
    
    xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_1': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_2': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_3': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_4': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_5': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_6': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_7': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_8': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_9': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_10': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_11': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_12': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_13': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_14': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_15': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_16': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_17': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_18': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_19': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_20': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_21': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_22': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_23': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_24': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_25': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_26': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_27': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_28': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_29': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_30': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_31': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_32': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_33': xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_33
    }
    xǁNetworkManagerWorkerǁ_wait_for_connection__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_wait_for_connection'

    async def _connect_network_impl(self, ssid: str) -> ConnectionResult:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_orig(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_1(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_2(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(None, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_3(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, None, "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_4(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", None)

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_5(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult("NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_6(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_7(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", )

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_8(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(True, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_9(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "XXNetworkManager unavailableXX", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_10(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "networkmanager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_11(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NETWORKMANAGER UNAVAILABLE", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_12(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "XXno_nmXX")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_13(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "NO_NM")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_14(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_15(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(None)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_16(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(False)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_17(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_18(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(None, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_19(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=None):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_20(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_21(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, ):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_22(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(False, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_23(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=9.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_24(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    None,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_25(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    None,
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_26(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    None,
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_27(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_28(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_29(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_30(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    True,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_31(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "XXWi-Fi radio failed to turn on.\nPlease try again.XX",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_32(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "wi-fi radio failed to turn on.\nplease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_33(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "WI-FI RADIO FAILED TO TURN ON.\nPLEASE TRY AGAIN.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_34(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "XXradio_failedXX",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_35(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "RADIO_FAILED",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_36(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=None)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_37(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=9.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_38(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = None
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_39(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(None)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_40(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_41(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = None
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_42(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(None)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_43(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_44(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(None, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_45(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, None, "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_46(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", None)

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_47(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_48(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_49(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", )

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_50(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(True, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_51(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "XXnot_foundXX")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_52(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "NOT_FOUND")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_53(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(None)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_54(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_55(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(None, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_56(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=None):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_57(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_58(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, ):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_59(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    None,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_60(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    None,
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_61(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    None,
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_62(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_63(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_64(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_65(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    True,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_66(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "XXPlease check signal strength and try again.XX",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_67(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_68(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "PLEASE CHECK SIGNAL STRENGTH AND TRY AGAIN.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_69(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "XXconnect_timeoutXX",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_70(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "CONNECT_TIMEOUT",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_71(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(None, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_72(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, None)
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_73(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_74(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, )
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_75(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(False, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_76(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(None, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_77(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, None, "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_78(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), None)

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_79(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_80(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_81(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), )

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_82(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(True, str(exc), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_83(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(None), "connect_failed")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_84(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "XXconnect_failedXX")

    async def xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_85(self, ssid: str) -> ConnectionResult:
        """Enable Wi-Fi if needed, locate the saved profile, and activate it."""
        if not self._system_bus:
            return ConnectionResult(False, "NetworkManager unavailable", "no_nm")

        if not await self._nm().wireless_enabled:
            await self._nm().wireless_enabled.set_async(True)
            if not await self._wait_for_wifi_radio(True, timeout=8.0):
                return ConnectionResult(
                    False,
                    "Wi-Fi radio failed to turn on.\nPlease try again.",
                    "radio_failed",
                )
            await self._wait_for_wifi_device_ready(timeout=8.0)

        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            conn_path = await self._find_connection_path_direct(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not saved", "not_found")

        try:
            await self._nm().activate_connection(conn_path)
            if not await self._wait_for_connection(ssid, timeout=_WIFI_CONNECT_TIMEOUT):
                return ConnectionResult(
                    False,
                    f"Could not connect to '{ssid}'.\n"
                    "Please check signal strength and try again.",
                    "connect_timeout",
                )
            return ConnectionResult(True, f"Connected to '{ssid}'")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "CONNECT_FAILED")
    
    xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_1': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_2': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_3': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_4': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_5': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_6': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_7': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_8': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_9': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_10': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_11': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_12': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_13': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_14': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_15': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_16': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_17': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_18': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_19': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_20': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_21': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_22': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_23': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_24': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_25': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_26': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_27': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_28': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_29': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_30': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_31': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_32': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_33': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_34': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_35': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_36': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_37': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_38': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_39': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_40': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_41': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_42': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_43': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_44': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_45': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_46': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_47': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_48': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_49': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_50': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_51': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_52': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_53': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_54': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_55': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_56': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_57': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_58': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_59': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_60': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_61': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_62': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_63': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_64': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_65': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_66': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_67': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_68': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_69': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_70': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_71': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_72': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_73': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_74': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_75': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_76': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_77': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_78': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_79': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_80': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_81': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_82': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_83': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_84': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_85': xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_85
    }
    xǁNetworkManagerWorkerǁ_connect_network_impl__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_connect_network_impl'

    async def _find_connection_path_direct(self, ssid: str) -> str | None:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_orig(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_1(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = None
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_2(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = None
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_3(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(None).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_4(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["XXconnectionXX"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_5(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["CONNECTION"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_6(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["XXtypeXX"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_7(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["TYPE"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_8(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][2] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_9(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] == "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_10(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "XX802-11-wirelessXX":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_11(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-WIRELESS":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_12(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        break
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_13(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = None
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_14(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["XX802-11-wirelessXX"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_15(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-WIRELESS"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_16(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["XXssidXX"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_17(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["SSID"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_18(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][2].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_19(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.upper() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_20(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() != ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_21(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.upper():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_22(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug(None, exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_23(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", None)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_24(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug(exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_25(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", )
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_26(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("XXSkipping connection in path lookup: %sXX", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_27(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_28(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("SKIPPING CONNECTION IN PATH LOOKUP: %S", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_29(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    break
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_30(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug(None, exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_31(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", None)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_32(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug(exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_33(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Direct connection path lookup failed: %s", )
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_34(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("XXDirect connection path lookup failed: %sXX", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_35(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("direct connection path lookup failed: %s", exc)
        return None

    async def xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_36(self, ssid: str) -> str | None:
        """Search NM settings for an infrastructure profile matching *ssid* directly."""
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    if settings["connection"]["type"][1] != "802-11-wireless":
                        continue
                    conn_ssid = settings["802-11-wireless"]["ssid"][1].decode()
                    if conn_ssid.lower() == ssid.lower():
                        self._invalidate_saved_cache()
                        return conn_path
                except Exception as exc:
                    logger.debug("Skipping connection in path lookup: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("DIRECT CONNECTION PATH LOOKUP FAILED: %S", exc)
        return None
    
    xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_1': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_2': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_3': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_4': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_5': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_6': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_7': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_8': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_9': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_10': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_11': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_12': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_13': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_14': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_15': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_16': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_17': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_18': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_19': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_20': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_21': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_22': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_23': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_24': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_25': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_26': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_27': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_28': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_29': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_30': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_31': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_32': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_33': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_34': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_35': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_36': xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_36
    }
    xǁNetworkManagerWorkerǁ_find_connection_path_direct__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_find_connection_path_direct'

    async def _async_disconnect(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_orig(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_1(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(None)
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_2(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(None, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_3(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, None))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_4(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult("Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_5(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, ))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_6(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(False, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_7(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "XXDisconnectedXX"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_8(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_9(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "DISCONNECTED"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_10(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error(None, exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_11(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", None)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_12(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error(exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_13(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", )
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_14(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("XXDisconnect failed: %sXX", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_15(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_16(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("DISCONNECT FAILED: %S", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_17(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_18(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=None,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_19(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=None,
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_20(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_21(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_22(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_23(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_24(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=True,
                    message=str(exc),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_25(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(None),
                    error_code="disconnect_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_26(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="XXdisconnect_failedXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_27(self) -> None:
        """Disconnect the primary Wi-Fi device and emit connection_result."""
        try:
            if self._primary_wifi_path:
                await self._wifi().disconnect()
            self.connection_result.emit(ConnectionResult(True, "Disconnected"))
        except Exception as exc:
            logger.error("Disconnect failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="DISCONNECT_FAILED",
                )
            )
    
    xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_1': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_2': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_3': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_4': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_5': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_6': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_7': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_8': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_9': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_10': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_11': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_12': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_13': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_14': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_15': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_16': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_17': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_18': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_19': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_20': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_21': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_22': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_23': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_24': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_25': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_26': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_27': xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_27
    }
    xǁNetworkManagerWorkerǁ_async_disconnect__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_disconnect'

    async def _async_delete_network(self, ssid: str) -> None:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_orig(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_1(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = None
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_2(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(None)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_3(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(None)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_4(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_5(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(None, exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_6(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", None)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_7(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_8(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", )
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_9(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("XXDelete failed: %sXX", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_10(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_11(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("DELETE FAILED: %S", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_12(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_13(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=None,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_14(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=None,
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_15(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_16(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_17(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_18(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_19(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=True,
                    message=str(exc),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_20(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(None),
                    error_code="delete_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_21(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="XXdelete_failedXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_22(self, ssid: str) -> None:
        """Delete the saved profile for *ssid* and emit connection_result."""
        try:
            result = await self._delete_network_impl(ssid)
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
            if result.success:
                self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Delete failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="DELETE_FAILED",
                )
            )
    
    xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_1': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_2': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_3': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_4': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_5': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_6': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_7': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_8': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_9': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_10': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_11': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_12': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_13': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_14': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_15': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_16': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_17': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_18': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_19': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_20': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_21': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_22': xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_22
    }
    xǁNetworkManagerWorkerǁ_async_delete_network__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_delete_network'

    async def _delete_network_impl(self, ssid: str) -> ConnectionResult:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_orig(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_1(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = None
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_2(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(None)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_3(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_4(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(None, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_5(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, None, "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_6(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", None)
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_7(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_8(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_9(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", )
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_10(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(True, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_11(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "XXnot_foundXX")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_12(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "NOT_FOUND")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_13(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(None).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_14(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug(None, reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_15(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", None)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_16(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug(reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_17(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", )

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_18(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("XXreload_connections non-fatal: %sXX", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_19(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("RELOAD_CONNECTIONS NON-FATAL: %S", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_20(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = None
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_21(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid or current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_22(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.upper() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_23(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() != ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_24(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.upper():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_25(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug(None, exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_26(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", None)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_27(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug(exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_28(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", )

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_29(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("XXDisconnect after network delete ignored: %sXX", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_30(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_31(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("DISCONNECT AFTER NETWORK DELETE IGNORED: %S", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_32(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(None, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_33(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, None)
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_34(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_35(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, )
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_36(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(False, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_37(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(None, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_38(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, None, "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_39(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), None)

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_40(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_41(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_42(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), )

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_43(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(True, str(exc), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_44(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(None), "delete_failed")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_45(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "XXdelete_failedXX")

    async def xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_46(self, ssid: str) -> ConnectionResult:
        """Delete the NM connection profile for *ssid* and disconnect if it is active."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            await self._conn_settings(conn_path).delete()

            if _CAN_RELOAD_CONNECTIONS:
                try:
                    await self._nm_settings().reload_connections()
                except Exception as reload_err:
                    logger.debug("reload_connections non-fatal: %s", reload_err)

            current_ssid = await self._get_current_ssid()
            if current_ssid and current_ssid.lower() == ssid.lower():
                if self._primary_wifi_path:
                    try:
                        await self._wifi().disconnect()
                    except Exception as exc:
                        logger.debug("Disconnect after network delete ignored: %s", exc)

            return ConnectionResult(True, f"Network '{ssid}' deleted")
        except Exception as exc:
            return ConnectionResult(False, str(exc), "DELETE_FAILED")
    
    xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_1': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_2': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_3': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_4': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_5': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_6': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_7': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_8': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_9': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_10': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_11': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_12': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_13': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_14': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_15': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_16': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_17': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_18': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_19': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_20': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_21': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_22': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_23': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_24': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_25': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_26': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_27': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_28': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_29': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_30': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_31': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_32': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_33': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_34': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_35': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_36': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_37': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_38': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_39': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_40': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_41': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_42': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_43': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_44': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_45': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_46': xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_46
    }
    xǁNetworkManagerWorkerǁ_delete_network_impl__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_delete_network_impl'

    async def _async_update_network(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        args = [ssid, password, priority]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_orig(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_1(
        self,
        ssid: str,
        password: str = "XXXX",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_2(
        self,
        ssid: str,
        password: str = "",
        priority: int = 1,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_3(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = None
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_4(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                None,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_5(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_6(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_7(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_8(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_9(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_10(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password and None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_11(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority == 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_12(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 1 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_13(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(None)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_14(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error(None, exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_15(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", None)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_16(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error(exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_17(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", )
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_18(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("XXUpdate failed: %sXX", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_19(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_20(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("UPDATE FAILED: %S", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_21(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_22(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=None,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_23(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=None,
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_24(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_25(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_26(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_27(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_28(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=True,
                    message=str(exc),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_29(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(None),
                    error_code="update_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_30(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="XXupdate_failedXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_network__mutmut_31(
        self,
        ssid: str,
        password: str = "",
        priority: int = 0,
    ) -> None:
        """Update password and/or priority for a saved profile and emit connection_result."""
        try:
            result = await self._update_network_impl(
                ssid,
                password or None,
                priority if priority != 0 else None,
            )
            self._invalidate_saved_cache()
            self.connection_result.emit(result)
        except Exception as exc:
            logger.error("Update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="UPDATE_FAILED",
                )
            )
    
    xǁNetworkManagerWorkerǁ_async_update_network__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_1': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_2': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_3': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_4': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_5': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_6': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_7': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_8': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_9': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_10': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_11': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_12': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_13': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_14': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_15': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_16': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_17': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_18': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_19': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_20': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_21': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_22': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_23': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_24': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_25': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_26': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_27': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_28': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_29': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_30': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_update_network__mutmut_31': xǁNetworkManagerWorkerǁ_async_update_network__mutmut_31
    }
    xǁNetworkManagerWorkerǁ_async_update_network__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_update_network'

    async def _update_network_impl(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        args = [ssid, password, priority]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_orig(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_1(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = None
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_2(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(None)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_3(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_4(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(None, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_5(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, None, "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_6(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", None)
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_7(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_8(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_9(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", )
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_10(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(True, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_11(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "XXnot_foundXX")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_12(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "NOT_FOUND")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_13(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = None
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_14(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(None)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_15(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = None
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_16(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(None, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_17(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, None)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_18(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_19(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, )

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_20(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password or "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_21(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "XX802-11-wireless-securityXX" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_22(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-WIRELESS-SECURITY" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_23(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" not in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_24(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = None

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_25(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["XX802-11-wireless-securityXX"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_26(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-WIRELESS-SECURITY"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_27(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["XXpskXX"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_28(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["PSK"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_29(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "XXsXX",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_30(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "S",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_31(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_32(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = None
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_33(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["XXconnectionXX"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_34(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["CONNECTION"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_35(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["XXautoconnect-priorityXX"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_36(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["AUTOCONNECT-PRIORITY"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_37(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "XXiXX",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_38(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "I",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_39(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug(None, ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_40(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", None, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_41(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, None)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_42(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug(ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_43(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_44(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, )

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_45(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("XXSetting priority for '%s' to %dXX", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_46(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_47(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("SETTING PRIORITY FOR '%S' TO %D", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_48(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(None)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_49(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug(None, ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_50(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", None)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_51(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug(ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_52(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", )
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_53(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("XXNetwork '%s' update() succeededXX", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_54(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_55(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("NETWORK '%S' UPDATE() SUCCEEDED", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_56(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(None, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_57(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, None)
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_58(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_59(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, )
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_60(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(False, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_61(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error(None, ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_62(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", None, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_63(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, None)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_64(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error(ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_65(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_66(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, )
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_67(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("XXUpdate failed for '%s': %sXX", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_68(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_69(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("UPDATE FAILED FOR '%S': %S", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_70(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = None
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_71(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).upper()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_72(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(None).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_73(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str or ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_74(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "XXpskXX" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_75(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "PSK" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_76(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" not in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_77(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str and "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_78(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("XXinvalidXX" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_79(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("INVALID" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_80(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" not in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_81(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "XXpropertyXX" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_82(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "PROPERTY" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_83(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" not in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_84(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    None,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_85(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    None,
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_86(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    None,
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_87(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_88(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_89(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_90(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    True,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_91(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "XXWrong password, try again.XX",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_92(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_93(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "WRONG PASSWORD, TRY AGAIN.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_94(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "XXinvalid_passwordXX",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_95(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "INVALID_PASSWORD",
                )
            return ConnectionResult(False, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_96(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(None, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_97(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, None, "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_98(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), None)

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_99(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_100(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_101(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), )

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_102(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(True, str(exc), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_103(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(None), "update_failed")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_104(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "XXupdate_failedXX")

    async def xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_105(
        self,
        ssid: str,
        password: str | None,
        priority: int | None,
    ) -> ConnectionResult:
        """Merge updated password/priority into the existing NM connection settings."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            return ConnectionResult(False, f"Network '{ssid}' not found", "not_found")
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            if password and "802-11-wireless-security" in props:
                props["802-11-wireless-security"]["psk"] = (
                    "s",
                    password,
                )

            if priority is not None:
                props["connection"]["autoconnect-priority"] = (
                    "i",
                    priority,
                )
                logger.debug("Setting priority for '%s' to %d", ssid, priority)

            await cs.update(props)
            logger.debug("Network '%s' update() succeeded", ssid)
            return ConnectionResult(True, f"Network '{ssid}' updated")
        except Exception as exc:
            logger.error("Update failed for '%s': %s", ssid, exc)
            err_str = str(exc).lower()
            if "psk" in err_str and ("invalid" in err_str or "property" in err_str):
                return ConnectionResult(
                    False,
                    "Wrong password, try again.",
                    "invalid_password",
                )
            return ConnectionResult(False, str(exc), "UPDATE_FAILED")
    
    xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_1': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_2': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_3': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_4': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_5': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_6': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_7': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_8': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_9': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_10': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_11': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_12': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_13': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_14': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_15': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_16': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_17': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_18': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_19': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_20': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_21': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_22': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_23': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_24': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_25': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_26': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_27': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_28': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_29': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_30': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_31': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_32': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_33': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_34': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_35': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_36': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_37': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_38': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_39': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_40': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_41': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_42': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_43': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_44': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_45': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_46': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_47': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_48': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_49': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_50': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_51': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_52': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_53': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_54': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_55': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_56': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_57': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_58': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_59': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_60': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_61': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_62': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_63': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_64': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_65': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_66': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_67': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_68': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_69': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_70': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_71': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_72': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_73': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_74': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_75': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_76': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_77': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_78': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_79': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_80': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_81': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_82': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_83': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_84': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_85': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_86': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_87': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_88': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_89': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_90': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_91': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_92': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_93': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_94': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_95': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_96': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_97': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_98': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_99': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_100': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_101': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_102': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_103': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_104': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_105': xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_105
    }
    xǁNetworkManagerWorkerǁ_update_network_impl__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_update_network_impl'

    async def _async_set_wifi_enabled(self, enabled: bool) -> None:
        args = [enabled]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_orig(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_1(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_2(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_3(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = None

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_4(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = True

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_5(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled or await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_6(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = None
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_7(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current == enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_8(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_9(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                None, exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_10(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", None
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_11(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_12(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_13(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "XXDisconnect before Wi-Fi toggle ignored: %sXX", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_14(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "disconnect before wi-fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_15(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "DISCONNECT BEFORE WI-FI TOGGLE IGNORED: %S", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_16(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(None)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_17(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(1.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_18(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(None)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_19(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_20(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(None, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_21(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=None):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_22(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_23(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, ):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_24(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=9.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_25(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        None,
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_26(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        None,
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_27(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_28(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_29(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "XXWi-Fi radio did not reach %s within 8 sXX",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_30(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "wi-fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_31(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "WI-FI RADIO DID NOT REACH %S WITHIN 8 S",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_32(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "XXenabledXX" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_33(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "ENABLED" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_34(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "XXdisabledXX",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_35(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "DISABLED",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_36(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                None
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_37(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    None,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_38(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    None,
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_39(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_40(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_41(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    False,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_42(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'XXenabledXX' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_43(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'ENABLED' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_44(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'XXdisabledXX'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_45(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'DISABLED'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_46(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_47(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(None, exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_48(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", None)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_49(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_50(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", )
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_51(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("XXFailed to toggle Wi-Fi: %sXX", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_52(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("failed to toggle wi-fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_53(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("FAILED TO TOGGLE WI-FI: %S", exc)
            self.error_occurred.emit("set_wifi_enabled", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_54(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit(None, str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_55(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", None)

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_56(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit(str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_57(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", )

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_58(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("XXset_wifi_enabledXX", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_59(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("SET_WIFI_ENABLED", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_60(self, enabled: bool) -> None:
        """Enable or disable the Wi-Fi radio, handling ethernet mutual exclusion."""
        try:
            if not self._system_bus:
                return
            if not enabled:
                self._is_hotspot_active = False

            if enabled and await self._is_ethernet_connected():
                await self._async_disconnect_ethernet()

            current = await self._nm().wireless_enabled
            if current != enabled:
                if not enabled:
                    if self._primary_wifi_path:
                        try:
                            await self._wifi().disconnect()
                        except Exception as exc:
                            logger.debug(
                                "Disconnect before Wi-Fi toggle ignored: %s", exc
                            )
                        await asyncio.sleep(0.5)

                await self._nm().wireless_enabled.set_async(enabled)

                if not await self._wait_for_wifi_radio(enabled, timeout=8.0):
                    logger.warning(
                        "Wi-Fi radio did not reach %s within 8 s",
                        "enabled" if enabled else "disabled",
                    )

            self.connection_result.emit(
                ConnectionResult(
                    True,
                    f"Wi-Fi {'enabled' if enabled else 'disabled'}",
                )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle Wi-Fi: %s", exc)
            self.error_occurred.emit("set_wifi_enabled", str(None))
    
    xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_1': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_2': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_3': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_4': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_5': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_6': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_7': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_8': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_9': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_10': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_11': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_12': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_13': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_14': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_15': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_16': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_17': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_18': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_19': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_20': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_21': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_22': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_23': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_24': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_25': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_26': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_27': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_28': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_29': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_30': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_31': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_32': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_33': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_34': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_35': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_36': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_37': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_38': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_39': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_40': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_41': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_42': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_43': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_44': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_45': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_46': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_47': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_48': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_49': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_50': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_51': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_52': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_53': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_54': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_55': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_56': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_57': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_58': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_59': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_60': xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_60
    }
    xǁNetworkManagerWorkerǁ_async_set_wifi_enabled__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_set_wifi_enabled'

    async def _async_disconnect_ethernet(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_orig(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_1(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_2(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = None
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_3(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = None
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_4(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() - 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_5(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 5.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_6(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() <= deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_7(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(None)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_8(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(1.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_9(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_10(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    return
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_11(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info(None)
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_12(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("XXEthernet disconnectedXX")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_13(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_14(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("ETHERNET DISCONNECTED")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_15(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error(None, exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_16(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", None)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_17(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error(exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_18(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("Failed to disconnect ethernet: %s", )

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_19(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("XXFailed to disconnect ethernet: %sXX", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_20(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("failed to disconnect ethernet: %s", exc)

    async def xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_21(self) -> None:
        """Deactivate all VLANs, disconnect ethernet, and wait up to 4 s for teardown."""
        if not self._primary_wired_path:
            return
        try:
            await self._deactivate_all_vlans()
            await self._wired().disconnect()
            loop = asyncio.get_running_loop()
            deadline = loop.time() + 4.0
            while loop.time() < deadline:
                await asyncio.sleep(0.5)
                if not await self._is_ethernet_connected():
                    break
            logger.info("Ethernet disconnected")
        except Exception as exc:
            logger.error("FAILED TO DISCONNECT ETHERNET: %S", exc)
    
    xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_1': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_2': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_3': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_4': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_5': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_6': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_7': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_8': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_9': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_10': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_11': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_12': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_13': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_14': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_15': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_16': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_17': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_18': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_19': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_20': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_21': xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_21
    }
    xǁNetworkManagerWorkerǁ_async_disconnect_ethernet__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_disconnect_ethernet'

    async def _async_connect_ethernet(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_orig(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_1(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_2(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit(None, "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_3(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", None)
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_4(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_5(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", )
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_6(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("XXconnect_ethernetXX", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_7(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("CONNECT_ETHERNET", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_8(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "XXNo wired device foundXX")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_9(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "no wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_10(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "NO WIRED DEVICE FOUND")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_11(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(None)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_12(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(True)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_13(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(None, exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_14(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", None)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_15(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_16(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", )
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_17(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("XXPre-VLAN disconnect ignored: %sXX", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_18(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("pre-vlan disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_19(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("PRE-VLAN DISCONNECT IGNORED: %S", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_20(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(None)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_21(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(1.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_22(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(None)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_23(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_24(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(None, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_25(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=None)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_26(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_27(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, )

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_28(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_29(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=9.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_30(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection(None, self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_31(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", None, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_32(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, None)
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_33(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection(self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_34(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_35(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, )
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_36(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("XX/XX", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_37(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "XX/XX")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_38(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(None)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_39(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(2.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_40(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info(None)
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_41(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("XXEthernet connection activatedXX")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_42(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_43(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("ETHERNET CONNECTION ACTIVATED")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_44(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(None)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_45(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(None, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_46(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, None))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_47(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult("Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_48(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, ))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_49(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(False, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_50(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "XXEthernet connectedXX"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_51(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_52(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "ETHERNET CONNECTED"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_53(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_54(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(None, exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_55(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", None)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_56(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_57(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", )
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_58(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("XXFailed to connect ethernet: %sXX", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_59(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_60(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("FAILED TO CONNECT ETHERNET: %S", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_61(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit(None, str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_62(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", None)
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_63(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit(str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_64(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", )
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_65(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("XXconnect_ethernetXX", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_66(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("CONNECT_ETHERNET", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_67(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(None))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_68(self) -> None:
        """Disable Wi-Fi/hotspot, activate the wired device, and restore saved VLANs."""
        if not self._primary_wired_path:
            self.error_occurred.emit("connect_ethernet", "No wired device found")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            await self._nm().activate_connection("/", self._primary_wired_path, "/")
            await asyncio.sleep(1.5)

            await self._activate_saved_vlans()
            logger.info("Ethernet connection activated")
            self.connection_result.emit(ConnectionResult(True, "Ethernet connected"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to connect ethernet: %s", exc)
            self.error_occurred.emit("connect_ethernet", str(exc))
            self.state_changed.emit(None)
    
    xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_1': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_2': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_3': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_4': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_5': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_6': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_7': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_8': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_9': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_10': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_11': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_12': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_13': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_14': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_15': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_16': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_17': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_18': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_19': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_20': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_21': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_22': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_23': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_24': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_25': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_26': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_27': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_28': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_29': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_30': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_31': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_32': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_33': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_34': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_35': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_36': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_37': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_38': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_39': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_40': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_41': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_42': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_43': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_44': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_45': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_46': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_47': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_48': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_49': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_50': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_51': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_52': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_53': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_54': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_55': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_56': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_57': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_58': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_59': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_60': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_61': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_62': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_63': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_64': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_65': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_66': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_67': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_68': xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_68
    }
    xǁNetworkManagerWorkerǁ_async_connect_ethernet__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_connect_ethernet'

    async def _async_create_vlan(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        args = [vlan_id, ip_address, subnet_mask, gateway, dns1, dns2]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_orig(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_1(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_2(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit(None, "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_3(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", None)
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_4(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_5(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", )
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_6(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("XXcreate_vlanXX", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_7(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("CREATE_VLAN", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_8(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "XXNo wired deviceXX")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_9(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "no wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_10(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "NO WIRED DEVICE")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_11(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(None)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_12(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(True)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_13(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(None, exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_14(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", None)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_15(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_16(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", )
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_17(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("XXPre-VLAN disconnect ignored: %sXX", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_18(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("pre-vlan disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_19(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("PRE-VLAN DISCONNECT IGNORED: %S", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_20(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(None)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_21(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(1.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_22(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(None)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_23(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_24(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(None, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_25(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=None)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_26(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_27(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, )

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_28(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_29(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=9.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_30(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_31(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection(None, self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_32(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", None, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_33(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, None)
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_34(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection(self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_35(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_36(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, )
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_37(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("XX/XX", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_38(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "XX/XX")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_39(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(None)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_40(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(2.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_41(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = None

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_42(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface and "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_43(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "XXeth0XX"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_44(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "ETH0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_45(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = None
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_46(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = None
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_47(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(None).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_48(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id or s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_49(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan" or s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_50(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get(None, (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_51(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", None)[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_52(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get((None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_53(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", )[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_54(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get(None, {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_55(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", None).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_56(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get({}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_57(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", ).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_58(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("XXconnectionXX", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_59(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("CONNECTION", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_60(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("XXtypeXX", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_61(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("TYPE", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_62(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, "XXXX"))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_63(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[2] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_64(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] != "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_65(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "XXvlanXX"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_66(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "VLAN"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_67(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get(None, (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_68(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", None)[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_69(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get((None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_70(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", )[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_71(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get(None, {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_72(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", None).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_73(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get({}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_74(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", ).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_75(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("XXvlanXX", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_76(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("VLAN", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_77(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("XXidXX", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_78(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("ID", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_79(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, +1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_80(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -2))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_81(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[2] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_82(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] != vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_83(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get(None, (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_84(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", None)[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_85(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get((None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_86(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", )[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_87(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get(None, {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_88(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", None).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_89(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get({}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_90(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", ).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_91(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("XXvlanXX", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_92(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("VLAN", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_93(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("XXparentXX", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_94(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("PARENT", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_95(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, "XXXX"))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_96(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[2] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_97(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] != iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_98(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                None
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_99(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    None,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_100(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    None,
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_101(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    None,
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_102(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_103(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_104(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_105(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    True,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_106(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "XXcreating a new one.XX",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_107(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "CREATING A NEW ONE.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_108(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "XXduplicate_vlanXX",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_109(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "DUPLICATE_VLAN",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_110(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            None, exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_111(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", None
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_112(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_113(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_114(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "XXSkipping connection in duplicate VLAN check: %sXX", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_115(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "skipping connection in duplicate vlan check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_116(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "SKIPPING CONNECTION IN DUPLICATE VLAN CHECK: %S", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_117(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        break
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_118(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    None,
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_119(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    None,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_120(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_121(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_122(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "XXDuplicate VLAN check failed (non-fatal): %sXX",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_123(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "duplicate vlan check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_124(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "DUPLICATE VLAN CHECK FAILED (NON-FATAL): %S",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_125(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = None

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_126(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(None):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_127(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(None)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_128(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(2.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_129(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(None)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_130(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(None)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_131(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(1.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_132(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = None
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_133(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(None)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_134(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = None
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_135(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(None)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_136(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = None
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_137(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(None) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_138(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 1
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_139(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = None
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_140(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(None)
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_141(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(None))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_142(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(None)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_143(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(None))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_144(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = None

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_145(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "XXconnectionXX": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_146(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "CONNECTION": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_147(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "XXidXX": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_148(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "ID": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_149(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("XXsXX", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_150(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("S", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_151(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "XXuuidXX": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_152(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "UUID": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_153(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("XXsXX", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_154(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("S", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_155(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(None)),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_156(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "XXtypeXX": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_157(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "TYPE": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_158(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("XXsXX", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_159(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("S", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_160(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "XXvlanXX"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_161(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "VLAN"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_162(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "XXautoconnectXX": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_163(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "AUTOCONNECT": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_164(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("XXbXX", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_165(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("B", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_166(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", True),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_167(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "XXvlanXX": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_168(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "VLAN": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_169(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "XXidXX": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_170(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "ID": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_171(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("XXuXX", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_172(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("U", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_173(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "XXparentXX": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_174(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "PARENT": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_175(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("XXsXX", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_176(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("S", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_177(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "XXipv4XX": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_178(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "IPV4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_179(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "XXmethodXX": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_180(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "METHOD": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_181(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("XXsXX", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_182(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("S", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_183(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "XXmanualXX"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_184(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "MANUAL"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_185(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "XXaddressesXX": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_186(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "ADDRESSES": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_187(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "XXaauXX",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_188(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "AAU",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_189(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "XXgatewayXX": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_190(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "GATEWAY": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_191(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("XXsXX", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_192(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("S", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_193(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway and ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_194(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or "XXXX"),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_195(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "XXdnsXX": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_196(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "DNS": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_197(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("XXauXX", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_198(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("AU", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_199(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "XXroute-metricXX": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_200(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "ROUTE-METRIC": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_201(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("XXiXX", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_202(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("I", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_203(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 501),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_204(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "XXipv6XX": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_205(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "IPV6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_206(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"XXmethodXX": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_207(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"METHOD": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_208(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("XXsXX", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_209(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("S", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_210(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "XXignoreXX")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_211(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "IGNORE")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_212(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = None
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_213(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(None)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_214(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(None, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_215(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, None, "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_216(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", None)
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_217(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection("/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_218(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_219(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", )
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_220(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "XX/XX", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_221(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "XX/XX")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_222(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(None)
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_223(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(None)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_224(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(2.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_225(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                None
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_226(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(None, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_227(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, None)
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_228(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_229(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_230(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(False, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_231(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_232(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(None, vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_233(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", None, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_234(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, None)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_235(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_236(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_237(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, )
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_238(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("XXFailed to create VLAN %d: %sXX", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_239(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("failed to create vlan %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_240(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("FAILED TO CREATE VLAN %D: %S", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_241(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit(None, str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_242(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", None)
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_243(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit(str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_244(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", )
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_245(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("XXcreate_vlanXX", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_246(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("CREATE_VLAN", str(exc))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_247(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(None))
            self.state_changed.emit(await self._build_current_state())

    async def xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_248(
        self,
        vlan_id: int,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Create and activate a VLAN connection with a static IP on the primary wired interface.

        Emits connection_result and state_changed when done.
        """
        if not self._primary_wired_path:
            self.error_occurred.emit("create_vlan", "No wired device")
            return
        try:
            if self._is_hotspot_active:
                await self._async_toggle_hotspot(False)

            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-VLAN disconnect ignored: %s", exc)
                await asyncio.sleep(0.5)

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._is_ethernet_connected():
                await self._nm().activate_connection("/", self._primary_wired_path, "/")
                await asyncio.sleep(1.5)

            iface = self._primary_wired_iface or "eth0"

            try:
                existing_conns = await self._nm_settings().list_connections()
                for existing_path in existing_conns:
                    try:
                        s = await self._conn_settings(existing_path).get_settings()
                        if (
                            s.get("connection", {}).get("type", (None, ""))[1] == "vlan"
                            and s.get("vlan", {}).get("id", (None, -1))[1] == vlan_id
                            and s.get("vlan", {}).get("parent", (None, ""))[1] == iface
                        ):
                            self.connection_result.emit(
                                ConnectionResult(
                                    False,
                                    f"VLAN {vlan_id} already exists on "
                                    f"{iface}.\nRemove it first before "
                                    "creating a new one.",
                                    "duplicate_vlan",
                                )
                            )
                            return
                    except Exception as exc:
                        logger.debug(
                            "Skipping connection in duplicate VLAN check: %s", exc
                        )
                        continue
            except Exception as dup_err:
                logger.debug(
                    "Duplicate VLAN check failed (non-fatal): %s",
                    dup_err,
                )

            vlan_conn_id = f"VLAN {vlan_id}"

            if await self._deactivate_connection_by_id(vlan_conn_id):
                await asyncio.sleep(1.0)

            await self._delete_all_connections_by_id(vlan_conn_id)
            await asyncio.sleep(0.5)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", vlan_conn_id),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "vlan"),
                    "autoconnect": ("b", False),
                },
                "vlan": {
                    "id": ("u", vlan_id),
                    "parent": ("s", iface),
                },
                "ipv4": {
                    "method": ("s", "manual"),
                    "addresses": (
                        "aau",
                        [[ip_uint, prefix, gw_uint]],
                    ),
                    "gateway": ("s", gateway or ""),
                    "dns": ("au", dns_list),
                    "route-metric": ("i", 500),
                },
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_path = await self._nm_settings().add_connection(conn_props)
            await self._nm().activate_connection(conn_path, "/", "/")
            self.state_changed.emit(await self._build_current_state())
            await asyncio.sleep(1.5)

            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} connected")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to create VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("create_vlan", str(exc))
            self.state_changed.emit(None)
    
    xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_1': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_2': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_3': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_4': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_5': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_6': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_7': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_8': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_9': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_10': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_11': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_12': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_13': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_14': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_15': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_16': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_17': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_18': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_19': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_20': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_21': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_22': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_23': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_24': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_25': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_26': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_27': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_28': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_29': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_30': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_31': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_32': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_33': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_34': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_35': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_36': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_37': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_38': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_39': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_40': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_41': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_42': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_43': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_44': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_45': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_46': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_47': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_48': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_49': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_50': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_51': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_52': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_53': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_54': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_55': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_56': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_57': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_58': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_59': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_60': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_61': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_62': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_63': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_64': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_65': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_66': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_67': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_68': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_69': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_70': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_71': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_72': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_73': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_74': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_75': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_76': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_77': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_78': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_79': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_80': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_81': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_82': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_83': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_84': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_85': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_86': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_87': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_88': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_89': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_90': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_91': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_92': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_93': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_94': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_95': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_96': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_97': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_98': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_99': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_100': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_101': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_102': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_103': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_104': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_105': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_106': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_107': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_108': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_109': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_110': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_110, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_111': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_111, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_112': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_112, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_113': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_113, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_114': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_114, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_115': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_115, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_116': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_116, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_117': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_117, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_118': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_118, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_119': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_119, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_120': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_120, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_121': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_121, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_122': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_122, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_123': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_123, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_124': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_124, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_125': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_125, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_126': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_126, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_127': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_127, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_128': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_128, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_129': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_129, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_130': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_130, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_131': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_131, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_132': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_132, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_133': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_133, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_134': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_134, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_135': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_135, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_136': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_136, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_137': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_137, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_138': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_138, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_139': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_139, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_140': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_140, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_141': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_141, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_142': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_142, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_143': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_143, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_144': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_144, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_145': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_145, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_146': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_146, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_147': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_147, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_148': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_148, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_149': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_149, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_150': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_150, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_151': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_151, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_152': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_152, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_153': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_153, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_154': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_154, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_155': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_155, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_156': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_156, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_157': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_157, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_158': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_158, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_159': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_159, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_160': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_160, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_161': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_161, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_162': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_162, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_163': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_163, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_164': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_164, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_165': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_165, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_166': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_166, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_167': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_167, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_168': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_168, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_169': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_169, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_170': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_170, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_171': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_171, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_172': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_172, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_173': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_173, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_174': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_174, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_175': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_175, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_176': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_176, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_177': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_177, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_178': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_178, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_179': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_179, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_180': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_180, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_181': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_181, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_182': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_182, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_183': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_183, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_184': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_184, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_185': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_185, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_186': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_186, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_187': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_187, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_188': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_188, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_189': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_189, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_190': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_190, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_191': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_191, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_192': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_192, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_193': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_193, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_194': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_194, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_195': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_195, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_196': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_196, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_197': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_197, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_198': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_198, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_199': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_199, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_200': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_200, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_201': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_201, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_202': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_202, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_203': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_203, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_204': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_204, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_205': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_205, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_206': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_206, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_207': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_207, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_208': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_208, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_209': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_209, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_210': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_210, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_211': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_211, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_212': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_212, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_213': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_213, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_214': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_214, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_215': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_215, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_216': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_216, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_217': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_217, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_218': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_218, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_219': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_219, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_220': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_220, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_221': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_221, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_222': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_222, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_223': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_223, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_224': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_224, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_225': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_225, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_226': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_226, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_227': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_227, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_228': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_228, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_229': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_229, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_230': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_230, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_231': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_231, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_232': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_232, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_233': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_233, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_234': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_234, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_235': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_235, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_236': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_236, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_237': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_237, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_238': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_238, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_239': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_239, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_240': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_240, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_241': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_241, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_242': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_242, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_243': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_243, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_244': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_244, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_245': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_245, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_246': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_246, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_247': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_247, 
        'xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_248': xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_248
    }
    xǁNetworkManagerWorkerǁ_async_create_vlan__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_create_vlan'

    async def _async_delete_vlan(self, vlan_id: int) -> None:
        args = [vlan_id]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_orig(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_1(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = None
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_2(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(None)
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_3(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                None,
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_4(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                None,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_5(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                None,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_6(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_7(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_8(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_9(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "XXDeleted %d VLAN profile(s) for VLAN %dXX",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_10(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "deleted %d vlan profile(s) for vlan %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_11(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "DELETED %D VLAN PROFILE(S) FOR VLAN %D",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_12(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                None
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_13(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(None, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_14(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, None)
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_15(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_16(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, )
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_17(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(False, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_18(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_19(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(None, vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_20(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", None, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_21(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, None)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_22(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_23(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_24(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, )
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_25(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("XXFailed to delete VLAN %d: %sXX", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_26(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("failed to delete vlan %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_27(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("FAILED TO DELETE VLAN %D: %S", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_28(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit(None, str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_29(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", None)

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_30(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit(str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_31(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", )

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_32(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("XXdelete_vlanXX", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_33(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("DELETE_VLAN", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_34(self, vlan_id: int) -> None:
        """Delete all NM connection profiles for *vlan_id* and emit connection_result."""
        try:
            deleted = await self._delete_all_connections_by_id(f"VLAN {vlan_id}")
            logger.info(
                "Deleted %d VLAN profile(s) for VLAN %d",
                deleted,
                vlan_id,
            )
            self.connection_result.emit(
                ConnectionResult(True, f"VLAN {vlan_id} removed")
            )
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to delete VLAN %d: %s", vlan_id, exc)
            self.error_occurred.emit("delete_vlan", str(None))
    
    xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_1': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_2': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_3': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_4': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_5': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_6': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_7': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_8': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_9': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_10': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_11': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_12': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_13': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_14': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_15': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_16': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_17': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_18': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_19': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_20': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_21': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_22': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_23': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_24': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_25': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_26': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_27': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_28': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_29': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_30': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_31': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_32': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_33': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_34': xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_34
    }
    xǁNetworkManagerWorkerǁ_async_delete_vlan__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_delete_vlan'

    async def _get_active_vlans(self) -> tuple[VlanInfo, ...]:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_orig(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_1(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = None
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_2(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = None
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_3(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = None
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_4(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(None)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_5(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = None
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_6(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = None
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_7(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(None).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_8(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = None
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_9(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get(None, (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_10(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", None)[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_11(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get((None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_12(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", )[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_13(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get(None, {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_14(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", None).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_15(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get({}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_16(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", ).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_17(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("XXconnectionXX", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_18(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("CONNECTION", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_19(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("XXtypeXX", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_20(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("TYPE", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_21(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, "XXXX"))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_22(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        2
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_23(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type == "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_24(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "XXvlanXX":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_25(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "VLAN":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_26(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        break

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_27(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = None
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_28(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get(None, (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_29(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", None)[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_30(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get((None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_31(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", )[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_32(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get(None, {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_33(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", None).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_34(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get({}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_35(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", ).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_36(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("XXvlanXX", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_37(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("VLAN", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_38(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("XXidXX", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_39(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("ID", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_40(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 1))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_41(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[2]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_42(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = None
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_43(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        None, (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_44(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", None
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_45(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_46(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_47(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get(None, {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_48(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", None).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_49(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get({}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_50(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", ).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_51(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("XXconnectionXX", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_52(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("CONNECTION", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_53(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "XXinterface-nameXX", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_54(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "INTERFACE-NAME", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_55(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "XXXX")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_56(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[2]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_57(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_58(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = None
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_59(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get(None, (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_60(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", None)[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_61(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get((None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_62(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", )[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_63(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get(None, {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_64(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", None).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_65(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get({}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_66(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", ).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_67(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("XXvlanXX", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_68(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("VLAN", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_69(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("XXparentXX", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_70(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("PARENT", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_71(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "XXeth0XX"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_72(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "ETH0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_73(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            2
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_74(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = None

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_75(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = None
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_76(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        None, (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_77(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", None
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_78(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_79(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_80(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get(None, {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_81(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", None).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_82(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get({}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_83(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", ).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_84(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("XXipv4XX", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_85(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("IPV4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_86(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "XXmethodXX", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_87(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "METHOD", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_88(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "XXautoXX")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_89(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "AUTO")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_90(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[2]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_91(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = None

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_92(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method == "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_93(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "XXmanualXX"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_94(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "MANUAL"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_95(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = None
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_96(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get(None, (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_97(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", None)[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_98(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get((None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_99(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", )[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_100(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get(None, {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_101(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", None).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_102(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get({}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_103(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", ).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_104(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("XXipv4XX", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_105(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("IPV4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_106(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("XXdns-dataXX", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_107(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("DNS-DATA", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_108(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[2]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_109(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = None
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_110(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = None
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_111(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(None)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_112(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(None) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_113(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = None
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_114(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get(None, (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_115(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", None)[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_116(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get((None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_117(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", )[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_118(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get(None, {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_119(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", None).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_120(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get({}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_121(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", ).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_122(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("XXipv4XX", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_123(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("IPV4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_124(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("XXdnsXX", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_125(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("DNS", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_126(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[2]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_127(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = None

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_128(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                None
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_129(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(None) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_130(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = None
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_131(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = "XXXX"
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_132(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = None
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_133(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = "XXXX"
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_134(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = None
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_135(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(None).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_136(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path or ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_137(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path == "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_138(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "XX/XX":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_139(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = None
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_140(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(None)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_141(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = None
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_142(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = None
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_143(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(None)
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_144(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[1]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_145(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["XXaddressXX"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_146(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["ADDRESS"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_147(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][2])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_148(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = None
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_149(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = None
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_150(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(None)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_151(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            None, exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_152(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", None
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_153(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_154(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_155(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "XXD-Bus IP read for VLAN failed, falling back to OS: %sXX", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_156(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "d-bus ip read for vlan failed, falling back to os: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_157(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-BUS IP READ FOR VLAN FAILED, FALLING BACK TO OS: %S", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_158(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = None

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_159(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(None)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_160(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr or iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_161(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_162(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = None

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_163(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(None)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_164(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        None
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_165(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=None,
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_166(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=None,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_167(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=None,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_168(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=None,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_169(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=None,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_170(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=None,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_171(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_172(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_173(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_174(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_175(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_176(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_177(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(None),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_178(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug(None, exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_179(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", None)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_180(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug(exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_181(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", )
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_182(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("XXSkipping connection in active VLAN list: %sXX", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_183(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("skipping connection in active vlan list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_184(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("SKIPPING CONNECTION IN ACTIVE VLAN LIST: %S", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_185(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    break
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_186(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug(None, exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_187(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", None)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_188(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug(exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_189(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", )
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_190(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("XXError getting active VLANs: %sXX", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_191(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("error getting active vlans: %s", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_192(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("ERROR GETTING ACTIVE VLANS: %S", exc)
        return tuple(vlans)

    async def xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_193(self) -> tuple[VlanInfo, ...]:
        """Return a tuple of VlanInfo for all currently active VLAN connections."""
        vlans: list[VlanInfo] = []
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    ac = self._active_conn(active_path)
                    conn_path = await ac.connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue

                    vlan_id = settings.get("vlan", {}).get("id", (None, 0))[1]
                    iface = settings.get("connection", {}).get(
                        "interface-name", (None, "")
                    )[1]
                    if not iface:
                        parent = settings.get("vlan", {}).get("parent", (None, "eth0"))[
                            1
                        ]
                        iface = f"{parent}.{vlan_id}"

                    ipv4_method = settings.get("ipv4", {}).get(
                        "method", (None, "auto")
                    )[1]
                    is_dhcp = ipv4_method != "manual"

                    dns_data = settings.get("ipv4", {}).get("dns-data", (None, []))[1]
                    dns_servers: tuple[str, ...] = ()
                    if dns_data:
                        dns_servers = tuple(str(d) for d in dns_data)
                    else:
                        dns_raw = settings.get("ipv4", {}).get("dns", (None, []))[1]
                        if dns_raw:
                            dns_servers = tuple(
                                self._nm_uint32_to_ip(d) for d in dns_raw
                            )

                    ip_addr = ""
                    gateway = ""
                    try:
                        ip4_path = await self._active_conn(active_path).ip4_config
                        if ip4_path and ip4_path != "/":
                            ip4_cfg = self._ipv4(ip4_path)
                            addr_data = await ip4_cfg.address_data
                            if addr_data:
                                ip_addr = str(addr_data[0]["address"][1])
                            gw = await ip4_cfg.gateway
                            if gw:
                                gateway = str(gw)
                    except Exception as exc:
                        logger.debug(
                            "D-Bus IP read for VLAN failed, falling back to OS: %s", exc
                        )
                        if iface:
                            ip_addr = await self._get_ip_by_interface(iface)

                    if not ip_addr and iface:
                        ip_addr = self._get_ip_os_fallback(iface)

                    vlans.append(
                        VlanInfo(
                            vlan_id=int(vlan_id),
                            ip_address=ip_addr,
                            interface=iface,
                            gateway=gateway,
                            dns_servers=dns_servers,
                            is_dhcp=is_dhcp,
                        )
                    )
                except Exception as exc:
                    logger.debug("Skipping connection in active VLAN list: %s", exc)
                    continue
        except Exception as exc:
            logger.debug("Error getting active VLANs: %s", exc)
        return tuple(None)
    
    xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_1': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_2': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_3': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_4': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_5': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_6': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_7': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_8': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_9': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_10': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_11': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_12': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_13': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_14': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_15': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_16': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_17': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_18': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_19': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_20': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_21': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_22': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_23': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_24': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_25': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_26': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_27': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_28': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_29': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_30': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_31': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_32': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_33': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_34': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_35': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_36': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_37': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_38': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_39': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_40': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_41': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_42': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_43': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_44': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_45': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_46': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_47': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_48': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_49': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_50': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_51': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_52': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_53': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_54': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_55': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_56': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_57': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_58': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_59': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_60': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_61': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_62': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_63': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_64': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_65': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_66': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_67': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_68': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_69': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_70': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_71': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_72': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_73': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_74': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_75': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_76': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_77': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_78': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_79': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_80': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_81': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_82': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_83': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_84': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_85': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_86': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_87': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_88': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_89': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_90': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_91': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_92': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_93': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_94': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_95': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_96': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_97': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_98': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_99': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_100': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_101': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_102': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_103': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_104': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_105': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_106': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_107': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_108': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_109': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_110': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_110, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_111': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_111, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_112': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_112, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_113': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_113, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_114': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_114, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_115': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_115, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_116': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_116, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_117': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_117, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_118': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_118, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_119': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_119, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_120': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_120, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_121': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_121, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_122': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_122, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_123': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_123, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_124': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_124, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_125': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_125, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_126': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_126, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_127': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_127, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_128': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_128, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_129': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_129, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_130': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_130, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_131': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_131, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_132': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_132, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_133': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_133, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_134': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_134, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_135': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_135, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_136': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_136, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_137': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_137, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_138': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_138, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_139': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_139, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_140': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_140, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_141': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_141, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_142': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_142, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_143': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_143, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_144': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_144, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_145': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_145, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_146': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_146, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_147': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_147, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_148': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_148, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_149': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_149, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_150': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_150, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_151': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_151, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_152': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_152, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_153': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_153, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_154': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_154, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_155': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_155, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_156': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_156, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_157': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_157, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_158': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_158, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_159': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_159, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_160': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_160, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_161': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_161, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_162': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_162, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_163': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_163, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_164': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_164, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_165': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_165, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_166': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_166, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_167': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_167, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_168': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_168, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_169': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_169, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_170': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_170, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_171': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_171, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_172': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_172, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_173': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_173, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_174': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_174, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_175': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_175, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_176': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_176, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_177': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_177, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_178': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_178, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_179': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_179, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_180': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_180, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_181': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_181, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_182': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_182, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_183': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_183, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_184': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_184, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_185': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_185, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_186': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_186, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_187': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_187, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_188': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_188, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_189': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_189, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_190': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_190, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_191': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_191, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_192': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_192, 
        'xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_193': xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_193
    }
    xǁNetworkManagerWorkerǁ_get_active_vlans__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_get_active_vlans'

    async def _deactivate_all_vlans(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_orig(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_1(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = None
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_2(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(None)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_3(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = None
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_4(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(None).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_5(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = None
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_6(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(None).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_7(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = None
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_8(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get(None, (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_9(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", None)[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_10(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get((None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_11(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", )[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_12(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get(None, {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_13(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", None).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_14(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get({}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_15(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", ).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_16(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("XXconnectionXX", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_17(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("CONNECTION", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_18(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("XXtypeXX", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_19(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("TYPE", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_20(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, "XXXX"))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_21(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        2
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_22(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type == "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_23(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "XXvlanXX":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_24(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "VLAN":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_25(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        break
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_26(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = None
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_27(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get(None, (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_28(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", None)[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_29(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get((None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_30(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", )[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_31(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get(None, {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_32(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", None).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_33(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get({}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_34(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", ).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_35(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("XXconnectionXX", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_36(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("CONNECTION", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_37(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("XXidXX", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_38(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("ID", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_39(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, "XXXX"))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_40(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[2]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_41(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(None)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_42(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug(None, conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_43(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", None)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_44(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug(conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_45(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", )
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_46(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("XXDeactivated VLAN '%s'XX", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_47(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("deactivated vlan '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_48(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("DEACTIVATED VLAN '%S'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_49(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug(None, exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_50(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", None)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_51(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug(exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_52(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", )
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_53(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("XXSkipping VLAN during deactivation: %sXX", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_54(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("skipping vlan during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_55(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("SKIPPING VLAN DURING DEACTIVATION: %S", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_56(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    break
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_57(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(None)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_58(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(1.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_59(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug(None, exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_60(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", None)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_61(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug(exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_62(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("Error deactivating VLANs: %s", )

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_63(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("XXError deactivating VLANs: %sXX", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_64(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("error deactivating vlans: %s", exc)

    async def xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_65(self) -> None:
        """Deactivate all active VLAN connections via the NM D-Bus interface."""
        try:
            active_paths = list(await self._nm().active_connections)
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().deactivate_connection(active_path)
                    logger.debug("Deactivated VLAN '%s'", conn_id)
                except Exception as exc:
                    logger.debug("Skipping VLAN during deactivation: %s", exc)
                    continue
            await asyncio.sleep(0.5)
        except Exception as exc:
            logger.debug("ERROR DEACTIVATING VLANS: %S", exc)
    
    xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_1': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_2': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_3': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_4': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_5': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_6': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_7': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_8': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_9': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_10': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_11': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_12': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_13': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_14': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_15': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_16': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_17': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_18': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_19': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_20': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_21': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_22': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_23': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_24': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_25': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_26': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_27': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_28': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_29': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_30': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_31': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_32': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_33': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_34': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_35': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_36': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_37': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_38': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_39': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_40': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_41': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_42': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_43': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_44': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_45': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_46': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_47': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_48': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_49': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_50': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_51': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_52': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_53': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_54': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_55': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_56': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_57': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_58': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_59': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_60': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_61': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_62': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_63': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_64': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_65': xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_65
    }
    xǁNetworkManagerWorkerǁ_deactivate_all_vlans__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_deactivate_all_vlans'

    async def _activate_saved_vlans(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_orig(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_1(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = None
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_2(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = None
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_3(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = None
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_4(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(None).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_5(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = None
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_6(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get(None, (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_7(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", None)[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_8(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get((None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_9(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", )[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_10(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get(None, {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_11(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", None).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_12(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get({}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_13(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", ).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_14(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("XXconnectionXX", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_15(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("CONNECTION", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_16(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("XXtypeXX", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_17(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("TYPE", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_18(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, "XXXX"))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_19(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        2
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_20(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type == "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_21(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "XXvlanXX":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_22(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "VLAN":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_23(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        break
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_24(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = None
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_25(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get(None, (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_26(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", None)[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_27(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get((None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_28(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", )[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_29(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get(None, {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_30(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", None).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_31(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get({}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_32(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", ).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_33(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("XXconnectionXX", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_34(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("CONNECTION", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_35(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("XXidXX", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_36(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("ID", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_37(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, "XXXX"))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_38(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[2]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_39(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(None, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_40(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, None, "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_41(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", None)
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_42(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection("/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_43(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_44(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", )
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_45(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "XX/XX", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_46(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "XX/XX")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_47(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug(None, conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_48(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", None)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_49(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug(conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_50(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", )
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_51(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("XXActivated saved VLAN '%s'XX", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_52(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("activated saved vlan '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_53(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("ACTIVATED SAVED VLAN '%S'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_54(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(None)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_55(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(2.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_56(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug(None, exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_57(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", None)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_58(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug(exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_59(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", )
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_60(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("XXFailed to activate VLAN: %sXX", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_61(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("failed to activate vlan: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_62(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("FAILED TO ACTIVATE VLAN: %S", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_63(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug(None, exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_64(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", None)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_65(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug(exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_66(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("Error activating saved VLANs: %s", )

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_67(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("XXError activating saved VLANs: %sXX", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_68(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("error activating saved vlans: %s", exc)

    async def xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_69(self) -> None:
        """Activate all saved VLAN connection profiles found in NM settings."""
        try:
            nm_settings = self._nm_settings()
            connections = await nm_settings.connections
            for conn_path in connections:
                try:
                    settings = await self._conn_settings(conn_path).get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "vlan":
                        continue
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    await self._nm().activate_connection(conn_path, "/", "/")
                    logger.debug("Activated saved VLAN '%s'", conn_id)
                    await asyncio.sleep(1.0)
                except Exception as exc:
                    logger.debug("Failed to activate VLAN: %s", exc)
        except Exception as exc:
            logger.debug("ERROR ACTIVATING SAVED VLANS: %S", exc)
    
    xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_1': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_2': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_3': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_4': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_5': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_6': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_7': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_8': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_9': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_10': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_11': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_12': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_13': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_14': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_15': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_16': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_17': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_18': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_19': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_20': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_21': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_22': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_23': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_24': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_25': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_26': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_27': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_28': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_29': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_30': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_31': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_32': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_33': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_34': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_35': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_36': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_37': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_38': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_39': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_40': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_41': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_42': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_43': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_44': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_45': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_46': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_47': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_48': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_49': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_50': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_51': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_52': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_53': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_54': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_55': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_56': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_57': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_58': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_59': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_60': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_61': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_62': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_63': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_64': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_65': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_66': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_67': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_68': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_69': xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_69
    }
    xǁNetworkManagerWorkerǁ_activate_saved_vlans__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_activate_saved_vlans'

    async def _reconnect_wifi_profile(self, ssid: str) -> None:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_orig(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_1(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            None,
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_2(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            None,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_3(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_4(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_5(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "XXReconnecting Wi-Fi profile '%s' to apply new settingsXX",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_6(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "reconnecting wi-fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_7(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "RECONNECTING WI-FI PROFILE '%S' TO APPLY NEW SETTINGS",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_8(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug(None, disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_9(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", None)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_10(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug(disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_11(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", )
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_12(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("XXDisconnect before reconnect: %sXX", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_13(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_14(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("DISCONNECT BEFORE RECONNECT: %S", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_15(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(None)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_16(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(2.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_17(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = None
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_18(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(None)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_19(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_20(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = None
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_21(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(None)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_22(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_23(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                None,
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_24(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                None,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_25(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_26(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_27(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "XXReconnect skipped: could not find saved profile for '%s'XX",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_28(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_29(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "RECONNECT SKIPPED: COULD NOT FIND SAVED PROFILE FOR '%S'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_30(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(None)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_31(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                None,
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_32(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                None,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_33(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                None,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_34(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_35(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_36(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_37(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "XXReconnect activate failed for '%s': %sXX",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_38(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_39(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "RECONNECT ACTIVATE FAILED FOR '%S': %S",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_40(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = None
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_41(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = None
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_42(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() - 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_43(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 11.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_44(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() <= deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_45(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(None)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_46(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(2.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_47(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = None
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_48(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = "XXXX"
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_49(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = None
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_50(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current or current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_51(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.upper() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_52(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() != ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_53(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.upper():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_54(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = None
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_55(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() and ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_56(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or "XXXX"
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_57(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_58(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = None
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_59(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") and ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_60(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback(None) or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_61(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("XXwlan0XX") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_62(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("WLAN0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_63(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or "XXXX"
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_64(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    None, exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_65(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", None
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_66(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_67(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_68(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "XXIP address lookup during connection wait ignored: %sXX", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_69(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "ip address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_70(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP ADDRESS LOOKUP DURING CONNECTION WAIT IGNORED: %S", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_71(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    None,
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_72(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    None,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_73(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    None,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_74(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_75(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_76(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_77(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "XXReconnect complete for '%s': IP=%sXX",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_78(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "reconnect complete for '%s': ip=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_79(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "RECONNECT COMPLETE FOR '%S': IP=%S",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_80(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        None
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_81(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        None,
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_82(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        None,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_83(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_84(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_85(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "XXCache refresh after reconnect failed: %sXX",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_86(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_87(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "CACHE REFRESH AFTER RECONNECT FAILED: %S",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_88(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning(None, ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_89(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", None)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_90(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning(ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_91(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("Reconnect for '%s': IP not assigned within 10 s", )

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_92(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("XXReconnect for '%s': IP not assigned within 10 sXX", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_93(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("reconnect for '%s': ip not assigned within 10 s", ssid)

    async def xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_94(self, ssid: str) -> None:
        """Disconnect, then re-activate the saved Wi-Fi profile for *ssid*.

        Waits up to 10 s for an IP address before returning.  Used after
        updating a connection's static IP or DHCP settings so the new
        configuration is applied immediately.
        """
        logger.debug(
            "Reconnecting Wi-Fi profile '%s' to apply new settings",
            ssid,
        )
        if self._primary_wifi_path:
            try:
                await self._wifi().disconnect()
            except Exception as disc_err:
                logger.debug("Disconnect before reconnect: %s", disc_err)
            await asyncio.sleep(1.5)

        fresh_path = await self._get_connection_path(ssid)
        if not fresh_path:
            fresh_path = await self._find_connection_path_direct(ssid)
        if not fresh_path:
            logger.warning(
                "Reconnect skipped: could not find saved profile for '%s'",
                ssid,
            )
            return

        try:
            await self._nm().activate_connection(fresh_path)
        except Exception as act_err:
            logger.warning(
                "Reconnect activate failed for '%s': %s",
                ssid,
                act_err,
            )
            return

        loop = asyncio.get_running_loop()
        deadline = loop.time() + 10.0
        while loop.time() < deadline:
            await asyncio.sleep(1.0)
            found_ip: str = ""
            try:
                current = await self._get_current_ssid()
                if current and current.lower() == ssid.lower():
                    found_ip = await self._get_current_ip() or ""
                    if not found_ip:
                        found_ip = self._get_ip_os_fallback("wlan0") or ""
            except Exception as exc:
                logger.debug(
                    "IP address lookup during connection wait ignored: %s", exc
                )

            if found_ip:
                logger.info(
                    "Reconnect complete for '%s': IP=%s",
                    ssid,
                    found_ip,
                )
                try:
                    self._invalidate_saved_cache()
                    self.saved_networks_loaded.emit(
                        await self._get_saved_networks_impl()
                    )
                except Exception as cache_err:
                    logger.debug(
                        "Cache refresh after reconnect failed: %s",
                        cache_err,
                    )
                return

        logger.warning("RECONNECT FOR '%S': IP NOT ASSIGNED WITHIN 10 S", ssid)
    
    xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_1': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_2': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_3': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_4': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_5': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_6': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_7': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_8': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_9': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_10': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_11': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_12': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_13': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_14': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_15': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_16': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_17': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_18': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_19': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_20': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_21': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_22': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_23': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_24': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_25': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_26': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_27': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_28': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_29': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_30': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_31': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_32': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_33': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_34': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_35': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_36': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_37': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_38': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_39': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_40': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_41': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_42': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_43': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_44': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_45': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_46': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_47': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_48': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_49': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_50': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_51': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_52': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_53': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_54': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_55': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_56': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_57': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_58': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_59': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_60': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_61': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_62': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_63': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_64': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_65': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_66': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_67': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_68': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_69': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_70': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_71': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_72': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_73': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_74': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_75': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_76': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_77': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_78': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_79': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_80': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_81': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_82': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_83': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_84': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_85': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_86': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_87': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_88': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_89': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_90': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_91': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_92': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_93': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_94': xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_94
    }
    xǁNetworkManagerWorkerǁ_reconnect_wifi_profile__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_reconnect_wifi_profile'

    async def _async_update_wifi_static_ip(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        args = [ssid, ip_address, subnet_mask, gateway, dns1, dns2]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_orig(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_1(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = None
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_2(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(None)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_3(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_4(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit(None, f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_5(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", None)
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_6(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit(f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_7(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", )
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_8(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("XXwifi_static_ipXX", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_9(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("WIFI_STATIC_IP", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_10(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = None
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_11(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(None)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_12(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = None
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_13(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(None, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_14(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, None)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_15(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_16(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, )

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_17(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = None
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_18(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(None)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_19(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = None
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_20(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(None)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_21(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = None
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_22(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(None) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_23(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 1
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_24(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = None
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_25(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(None)
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_26(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(None))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_27(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(None)

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_28(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(None))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_29(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = None
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_30(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["XXipv4XX"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_31(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["IPV4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_32(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "XXmethodXX": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_33(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "METHOD": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_34(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("XXsXX", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_35(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("S", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_36(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "XXmanualXX"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_37(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "MANUAL"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_38(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "XXaddressesXX": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_39(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "ADDRESSES": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_40(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "XXaauXX",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_41(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "AAU",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_42(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "XXgatewayXX": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_43(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "GATEWAY": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_44(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("XXsXX", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_45(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("S", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_46(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway and ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_47(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or "XXXX"),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_48(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "XXdnsXX": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_49(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "DNS": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_50(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("XXauXX", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_51(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("AU", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_52(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = None
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_53(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["XXipv6XX"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_54(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["IPV6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_55(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"XXmethodXX": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_56(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"METHOD": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_57(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("XXsXX", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_58(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("S", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_59(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "XXdisabledXX")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_60(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "DISABLED")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_61(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(None)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_62(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                None,
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_63(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                None,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_64(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                None,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_65(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                None,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_66(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                None,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_67(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_68(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_69(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_70(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_71(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_72(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "XXStatic IP set for '%s': %s/%d gw %s (IPv6 disabled)XX",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_73(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "static ip set for '%s': %s/%d gw %s (ipv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_74(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "STATIC IP SET FOR '%S': %S/%D GW %S (IPV6 DISABLED)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_75(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(None)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_76(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                None
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_77(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(None, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_78(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, None)
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_79(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_80(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, )
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_81(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(False, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_82(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(None)
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_83(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error(None, ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_84(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", None, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_85(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, None)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_86(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error(ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_87(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_88(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, )
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_89(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("XXFailed to set static IP for '%s': %sXX", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_90(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("failed to set static ip for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_91(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("FAILED TO SET STATIC IP FOR '%S': %S", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_92(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit(None, str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_93(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", None)

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_94(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit(str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_95(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", )

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_96(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("XXwifi_static_ipXX", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_97(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("WIFI_STATIC_IP", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_98(
        self,
        ssid: str,
        ip_address: str,
        subnet_mask: str,
        gateway: str,
        dns1: str,
        dns2: str,
    ) -> None:
        """Apply a static IPv4 configuration to a saved Wi-Fi profile and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_static_ip", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)

            prefix = self._mask_to_prefix(subnet_mask)
            ip_uint = self._ip_to_nm_uint32(ip_address)
            gw_uint = self._ip_to_nm_uint32(gateway) if gateway else 0
            dns_list: list[int] = []
            if dns1:
                dns_list.append(self._ip_to_nm_uint32(dns1))
            if dns2:
                dns_list.append(self._ip_to_nm_uint32(dns2))

            props["ipv4"] = {
                "method": ("s", "manual"),
                "addresses": (
                    "aau",
                    [[ip_uint, prefix, gw_uint]],
                ),
                "gateway": ("s", gateway or ""),
                "dns": ("au", dns_list),
            }
            props["ipv6"] = {"method": ("s", "disabled")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(
                "Static IP set for '%s': %s/%d gw %s (IPv6 disabled)",
                ssid,
                ip_address,
                prefix,
                gateway,
            )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(
                ConnectionResult(True, f"Static IP set for '{ssid}'")
            )
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to set static IP for '%s': %s", ssid, exc)
            self.error_occurred.emit("wifi_static_ip", str(None))
    
    xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_1': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_2': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_3': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_4': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_5': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_6': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_7': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_8': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_9': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_10': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_11': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_12': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_13': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_14': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_15': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_16': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_17': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_18': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_19': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_20': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_21': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_22': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_23': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_24': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_25': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_26': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_27': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_28': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_29': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_30': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_31': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_32': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_33': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_34': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_35': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_36': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_37': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_38': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_39': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_40': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_41': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_42': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_43': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_44': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_45': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_46': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_47': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_48': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_49': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_50': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_51': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_52': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_53': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_54': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_55': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_56': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_57': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_58': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_59': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_60': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_61': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_62': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_63': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_64': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_65': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_66': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_67': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_68': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_69': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_70': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_71': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_72': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_73': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_74': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_75': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_76': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_77': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_78': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_79': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_80': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_81': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_82': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_83': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_84': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_85': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_86': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_87': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_88': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_89': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_90': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_91': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_92': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_93': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_94': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_95': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_96': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_97': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_98': xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_98
    }
    xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_update_wifi_static_ip'

    async def _async_reset_wifi_to_dhcp(self, ssid: str) -> None:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_orig(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_1(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = None
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_2(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(None)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_3(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_4(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit(None, f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_5(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", None)
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_6(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit(f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_7(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", )
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_8(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("XXwifi_dhcpXX", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_9(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("WIFI_DHCP", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_10(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = None
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_11(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(None)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_12(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = None
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_13(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(None, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_14(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, None)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_15(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_16(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, )
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_17(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = None
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_18(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["XXipv4XX"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_19(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["IPV4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_20(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"XXmethodXX": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_21(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"METHOD": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_22(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("XXsXX", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_23(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("S", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_24(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "XXautoXX")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_25(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "AUTO")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_26(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(None)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_27(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(None, ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_28(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", None)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_29(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info(ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_30(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", )

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_31(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("XXReset '%s' to DHCPXX", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_32(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("reset '%s' to dhcp", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_33(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("RESET '%S' TO DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_34(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(None)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_35(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(None)
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_36(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(None, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_37(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, None))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_38(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_39(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, ))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_40(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(False, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_41(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(None)
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_42(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error(None, ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_43(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", None, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_44(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, None)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_45(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error(ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_46(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_47(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, )
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_48(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("XXFailed to reset '%s' to DHCP: %sXX", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_49(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("failed to reset '%s' to dhcp: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_50(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("FAILED TO RESET '%S' TO DHCP: %S", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_51(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit(None, str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_52(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", None)

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_53(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit(str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_54(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", )

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_55(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("XXwifi_dhcpXX", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_56(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("WIFI_DHCP", str(exc))

    async def xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_57(self, ssid: str) -> None:
        """Reset a saved Wi-Fi profile's IPv4 settings to DHCP and reconnect."""
        conn_path = await self._get_connection_path(ssid)
        if not conn_path:
            self.error_occurred.emit("wifi_dhcp", f"'{ssid}' not found")
            return
        try:
            cs = self._conn_settings(conn_path)
            props = await cs.get_settings()
            await self._merge_wifi_secrets(cs, props)
            props["ipv4"] = {"method": ("s", "auto")}
            await cs.update(props)
            self._invalidate_saved_cache()
            logger.info("Reset '%s' to DHCP", ssid)

            await self._reconnect_wifi_profile(ssid)

            self.connection_result.emit(ConnectionResult(True, f"'{ssid}' set to DHCP"))
            self.state_changed.emit(await self._build_current_state())
            self.reconnect_complete.emit()
        except Exception as exc:
            logger.error("Failed to reset '%s' to DHCP: %s", ssid, exc)
            self.error_occurred.emit("wifi_dhcp", str(None))
    
    xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_1': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_2': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_3': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_4': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_5': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_6': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_7': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_8': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_9': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_10': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_11': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_12': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_13': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_14': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_15': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_16': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_17': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_18': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_19': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_20': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_21': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_22': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_23': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_24': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_25': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_26': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_27': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_28': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_29': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_30': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_31': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_32': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_33': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_34': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_35': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_36': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_37': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_38': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_39': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_40': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_41': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_42': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_43': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_44': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_45': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_46': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_47': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_48': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_49': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_50': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_51': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_52': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_53': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_54': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_55': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_56': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_57': xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_57
    }
    xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_reset_wifi_to_dhcp'

    def _load_hotspot_config(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_orig(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_1(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = None
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_2(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_3(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section(None):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_4(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("XXhotspotXX"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_5(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("HOTSPOT"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_6(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section(None)

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_7(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("XXhotspotXX")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_8(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("HOTSPOT")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_9(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = None

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_10(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section(None)

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_11(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("XXhotspotXX")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_12(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("HOTSPOT")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_13(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option(None):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_14(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("XXssidXX"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_15(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("SSID"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_16(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = None
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_17(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get(None, str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_18(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", None, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_19(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, None)
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_20(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get(str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_21(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_22(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, )
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_23(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("XXssidXX", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_24(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("SSID", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_25(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "XXPrinterHotspotXX")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_26(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "printerhotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_27(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PRINTERHOTSPOT")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_28(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option(None, "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_29(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", None, "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_30(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", None)

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_31(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_32(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_33(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", )

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_34(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("XXhotspotXX", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_35(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("HOTSPOT", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_36(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "XXssidXX", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_37(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "SSID", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_38(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "XXPrinterHotspotXX")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_39(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "printerhotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_40(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PRINTERHOTSPOT")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_41(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option(None):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_42(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("XXpasswordXX"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_43(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("PASSWORD"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_44(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = None
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_45(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    None, str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_46(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", None, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_47(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, None
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_48(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_49(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_50(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_51(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "XXpasswordXX", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_52(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "PASSWORD", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_53(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "XX123456789XX"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_54(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option(None, "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_55(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", None, "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_56(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", None)

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_57(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_58(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_59(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", )

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_60(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("XXhotspotXX", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_61(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("HOTSPOT", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_62(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "XXpasswordXX", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_63(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "PASSWORD", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_64(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "XX123456789XX")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_65(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning(None, exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_66(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", None)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_67(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning(exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_68(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not load hotspot config, using defaults: %s", )

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_69(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("XXCould not load hotspot config, using defaults: %sXX", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_70(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("could not load hotspot config, using defaults: %s", exc)

    def xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_71(self) -> None:
        """Populate _hotspot_config from the config file.

        Writes defaults if missing.
        """
        try:
            cfg = get_configparser()
            if not cfg.has_section("hotspot"):
                cfg.add_section("hotspot")

            hotspot = cfg.get_section("hotspot")

            if hotspot.has_option("ssid"):
                self._hotspot_config.ssid = hotspot.get("ssid", str, "PrinterHotspot")
            else:
                cfg.add_option("hotspot", "ssid", "PrinterHotspot")

            if hotspot.has_option("password"):
                self._hotspot_config.password = hotspot.get(
                    "password", str, "123456789"
                )
            else:
                cfg.add_option("hotspot", "password", "123456789")

            cfg.save_configuration()
        except Exception as exc:
            logger.warning("COULD NOT LOAD HOTSPOT CONFIG, USING DEFAULTS: %S", exc)
    
    xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_1': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_2': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_3': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_4': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_5': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_6': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_7': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_8': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_9': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_10': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_11': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_12': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_13': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_14': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_15': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_16': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_17': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_18': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_19': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_20': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_21': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_22': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_23': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_24': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_25': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_26': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_27': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_28': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_29': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_30': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_31': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_32': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_33': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_34': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_35': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_36': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_37': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_38': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_39': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_40': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_41': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_42': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_43': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_44': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_45': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_46': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_47': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_48': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_49': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_50': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_51': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_52': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_53': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_54': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_55': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_56': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_57': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_58': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_59': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_60': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_61': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_62': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_63': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_64': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_65': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_66': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_67': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_68': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_69': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_70': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_71': xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_71
    }
    xǁNetworkManagerWorkerǁ_load_hotspot_config__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_load_hotspot_config'

    def _save_hotspot_config(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_mutants'), args, kwargs, self)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_orig(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_1(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = None
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_2(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option(None, "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_3(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", None, self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_4(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", None)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_5(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_6(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_7(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", )
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_8(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("XXhotspotXX", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_9(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("HOTSPOT", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_10(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "XXssidXX", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_11(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "SSID", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_12(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option(None, "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_13(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", None, self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_14(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", None)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_15(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_16(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_17(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", )
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_18(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("XXhotspotXX", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_19(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("HOTSPOT", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_20(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "XXpasswordXX", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_21(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "PASSWORD", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_22(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning(None, exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_23(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", None)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_24(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning(exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_25(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("Could not save hotspot config: %s", )

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_26(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("XXCould not save hotspot config: %sXX", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_27(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("could not save hotspot config: %s", exc)

    def xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_28(self) -> None:
        """Persist current _hotspot_config ssid/password to the config file."""
        try:
            cfg = get_configparser()
            cfg.update_option("hotspot", "ssid", self._hotspot_config.ssid)
            cfg.update_option("hotspot", "password", self._hotspot_config.password)
            cfg.save_configuration()
        except Exception as exc:
            logger.warning("COULD NOT SAVE HOTSPOT CONFIG: %S", exc)
    
    xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_1': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_2': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_3': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_4': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_5': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_6': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_7': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_8': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_9': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_10': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_11': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_12': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_13': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_14': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_15': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_16': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_17': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_18': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_19': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_20': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_21': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_22': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_23': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_24': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_25': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_26': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_27': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_28': xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_28
    }
    xǁNetworkManagerWorkerǁ_save_hotspot_config__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_save_hotspot_config'

    async def _async_create_and_activate_hotspot(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        args = [ssid, password, security]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_orig(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_1(
        self,
        ssid: str,
        password: str,
        security: str = "XXwpa-pskXX",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_2(
        self,
        ssid: str,
        password: str,
        security: str = "WPA-PSK",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_3(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = None
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_4(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid and "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_5(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "XXPrinterHotspotXX"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_6(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "printerhotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_7(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PRINTERHOTSPOT"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_8(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = None
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_9(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password and "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_10(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "XX123456789XX"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_11(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = None
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_12(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(None)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_13(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = None
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_14(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = None
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_15(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = None
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_16(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_17(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(None)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_18(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_19(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(None, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_20(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=None)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_21(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_22(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, )

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_23(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(False, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_24(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=9.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_25(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_26(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=None):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_27(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=9.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_28(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    None
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_29(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "XXwlan0 did not reach DISCONNECTED within 8 s; XX"
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_30(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach disconnected within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_31(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "WLAN0 DID NOT REACH DISCONNECTED WITHIN 8 S; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_32(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "XXproceeding with hotspot activation anywayXX"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_33(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "PROCEEDING WITH HOTSPOT ACTIVATION ANYWAY"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_34(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = None
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_35(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug(None, exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_36(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", None)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_37(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug(exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_38(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", )
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_39(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("XXPre-hotspot ethernet disconnect ignored: %sXX", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_40(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_41(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("PRE-HOTSPOT ETHERNET DISCONNECT IGNORED: %S", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_42(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(None)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_43(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(2.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_44(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(None, exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_45(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", None)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_46(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_47(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", )

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_48(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("XXPre-hotspot Wi-Fi disconnect ignored: %sXX", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_49(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("pre-hotspot wi-fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_50(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("PRE-HOTSPOT WI-FI DISCONNECT IGNORED: %S", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_51(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(None)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_52(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = None

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_53(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "XXconnectionXX": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_54(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "CONNECTION": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_55(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "XXidXX": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_56(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "ID": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_57(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("XXsXX", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_58(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("S", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_59(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "XXuuidXX": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_60(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "UUID": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_61(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("XXsXX", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_62(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("S", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_63(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(None)),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_64(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "XXtypeXX": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_65(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "TYPE": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_66(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("XXsXX", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_67(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("S", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_68(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "XX802-11-wirelessXX"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_69(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-WIRELESS"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_70(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "XXinterface-nameXX": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_71(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "INTERFACE-NAME": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_72(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("XXsXX", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_73(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("S", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_74(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "XXautoconnectXX": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_75(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "AUTOCONNECT": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_76(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("XXbXX", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_77(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("B", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_78(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", True),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_79(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "XX802-11-wirelessXX": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_80(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-WIRELESS": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_81(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "XXssidXX": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_82(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "SSID": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_83(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("XXayXX", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_84(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("AY", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_85(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode(None)),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_86(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("XXutf-8XX")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_87(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("UTF-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_88(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "XXmodeXX": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_89(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "MODE": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_90(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("XXsXX", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_91(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("S", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_92(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "XXapXX"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_93(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "AP"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_94(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "XXbandXX": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_95(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "BAND": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_96(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("XXsXX", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_97(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("S", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_98(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "XXchannelXX": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_99(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "CHANNEL": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_100(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "XXuXX",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_101(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "U",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_102(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "XXsecurityXX": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_103(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "SECURITY": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_104(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "XXsXX",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_105(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "S",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_106(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "XX802-11-wireless-securityXX",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_107(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-WIRELESS-SECURITY",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_108(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "XXipv4XX": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_109(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "IPV4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_110(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"XXmethodXX": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_111(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"METHOD": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_112(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("XXsXX", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_113(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("S", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_114(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "XXsharedXX")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_115(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "SHARED")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_116(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "XXipv6XX": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_117(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "IPV6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_118(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"XXmethodXX": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_119(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"METHOD": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_120(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("XXsXX", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_121(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("S", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_122(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "XXignoreXX")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_123(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "IGNORE")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_124(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = None
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_125(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["XX802-11-wireless-securityXX"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_126(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-WIRELESS-SECURITY"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_127(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "XXkey-mgmtXX": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_128(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "KEY-MGMT": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_129(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("XXsXX", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_130(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("S", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_131(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "XXwpa-pskXX"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_132(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "WPA-PSK"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_133(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "XXpskXX": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_134(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "PSK": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_135(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("XXsXX", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_136(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("S", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_137(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "XXpmfXX": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_138(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "PMF": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_139(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("XXuXX", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_140(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("U", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_141(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 1),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_142(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = None
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_143(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = None

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_144(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = None
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_145(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(None)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_146(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                None,
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_147(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                None,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_148(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                None,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_149(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_150(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_151(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_152(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "XXHotspot profile created at %s (security=%s)XX",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_153(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_154(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "HOTSPOT PROFILE CREATED AT %S (SECURITY=%S)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_155(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                None, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_156(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, None, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_157(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, None
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_158(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_159(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_160(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_161(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "XX/XX"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_162(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = None
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_163(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = False
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_164(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(None, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_165(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, None, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_166(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, None)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_167(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_168(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_169(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, )
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_170(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                None
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_171(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(None, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_172(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, None)
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_173(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_174(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, )
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_175(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(False, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_176(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(None)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_177(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(2.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_178(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(None)

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_179(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error(None, exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_180(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", None)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_181(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error(exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_182(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", )
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_183(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("XXHotspot create+activate failed: %sXX", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_184(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_185(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("HOTSPOT CREATE+ACTIVATE FAILED: %S", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_186(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = None
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_187(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = True
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_188(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_189(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(None, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_190(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, None, "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_191(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), None)
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_192(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_193(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_194(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), )
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_195(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(True, str(exc), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_196(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(None), "hotspot_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_197(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "XXhotspot_failedXX")
            )

    async def xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_198(
        self,
        ssid: str,
        password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Create a new WPA2-PSK AP-mode profile and activate it as a hotspot.

        Removes all stale AP-mode and same-name profiles before adding the new
        one.  Disconnects ethernet if active so the Wi-Fi radio is available.
        """
        try:
            config_ssid = ssid or "PrinterHotspot"
            config_pwd = password or "123456789"
            config_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = config_ssid
            self._hotspot_config.password = config_pwd
            self._hotspot_config.security = config_sec
            self._save_hotspot_config()

            if not await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)
                await self._wait_for_wifi_radio(True, timeout=8.0)

            if not await self._wait_for_wifi_device_ready(timeout=8.0):
                logger.warning(
                    "wlan0 did not reach DISCONNECTED within 8 s; "
                    "proceeding with hotspot activation anyway"
                )

            ethernet_was_active = await self._is_ethernet_connected()
            if ethernet_was_active:
                try:
                    await self._async_disconnect_ethernet()
                except Exception as exc:
                    logger.debug("Pre-hotspot ethernet disconnect ignored: %s", exc)
                # Brief pause to let eth0 finish deactivating before NM
                # processes the hotspot activation request.
                await asyncio.sleep(1.0)
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot Wi-Fi disconnect ignored: %s", exc)

            await self._delete_all_ap_mode_connections()
            # Also delete by connection id in case a non-AP profile shares the
            # hotspot name (e.g. a leftover infrastructure profile named the
            # same as the SSID). _delete_all_ap_mode_connections already caught
            # all AP-mode profiles, so this second list_connections call is a
            # narrow safety net.
            await self._delete_connections_by_id(config_ssid)

            conn_props: dict[str, object] = {
                "connection": {
                    "id": ("s", config_ssid),
                    "uuid": ("s", str(uuid4())),
                    "type": ("s", "802-11-wireless"),
                    "interface-name": ("s", self._get_wifi_iface_name()),
                    "autoconnect": ("b", False),
                },
                "802-11-wireless": {
                    "ssid": ("ay", config_ssid.encode("utf-8")),
                    "mode": ("s", "ap"),
                    "band": ("s", self._hotspot_config.band),
                    "channel": (
                        "u",
                        self._hotspot_config.channel,
                    ),
                    "security": (
                        "s",
                        "802-11-wireless-security",
                    ),
                },
                "ipv4": {"method": ("s", "shared")},
                "ipv6": {"method": ("s", "ignore")},
            }

            conn_props["802-11-wireless-security"] = {
                "key-mgmt": ("s", "wpa-psk"),
                "psk": ("s", config_pwd),
                "pmf": ("u", 0),
            }
            # AP mode is always WPA2-PSK; WPA3-SAE in AP mode requires driver
            # support not guaranteed on the target hardware.
            config_sec = HotspotSecurity.WPA2_PSK.value
            self._hotspot_config.security = config_sec

            conn_path = await self._nm_settings().add_connection(conn_props)
            logger.debug(
                "Hotspot profile created at %s (security=%s)",
                conn_path,
                config_sec,
            )

            await self._nm().activate_connection(
                conn_path, self._primary_wifi_path, "/"
            )
            self._is_hotspot_active = True
            self._invalidate_saved_cache()

            self.hotspot_info_ready.emit(config_ssid, config_pwd, config_sec)
            self.connection_result.emit(
                ConnectionResult(True, f"Hotspot '{config_ssid}' activated")
            )

            await asyncio.sleep(1.5)
            self.state_changed.emit(await self._build_current_state())

        except Exception as exc:
            logger.error("Hotspot create+activate failed: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "HOTSPOT_FAILED")
            )
    
    xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_1': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_2': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_3': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_4': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_5': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_6': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_7': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_8': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_9': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_10': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_11': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_12': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_13': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_14': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_15': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_16': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_17': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_18': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_19': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_20': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_21': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_22': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_23': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_24': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_25': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_26': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_27': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_28': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_29': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_30': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_31': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_32': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_33': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_34': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_35': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_36': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_37': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_38': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_39': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_40': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_41': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_42': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_43': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_44': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_45': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_46': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_47': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_48': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_49': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_50': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_51': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_52': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_53': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_54': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_55': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_56': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_57': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_58': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_59': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_60': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_61': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_62': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_63': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_64': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_65': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_66': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_67': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_68': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_69': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_70': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_71': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_72': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_73': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_74': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_75': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_76': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_77': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_78': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_79': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_80': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_81': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_82': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_83': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_84': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_84, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_85': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_85, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_86': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_86, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_87': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_87, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_88': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_88, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_89': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_89, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_90': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_90, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_91': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_91, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_92': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_92, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_93': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_93, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_94': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_94, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_95': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_95, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_96': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_96, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_97': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_97, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_98': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_98, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_99': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_99, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_100': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_100, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_101': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_101, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_102': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_102, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_103': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_103, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_104': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_104, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_105': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_105, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_106': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_106, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_107': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_107, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_108': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_108, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_109': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_109, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_110': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_110, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_111': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_111, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_112': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_112, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_113': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_113, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_114': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_114, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_115': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_115, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_116': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_116, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_117': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_117, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_118': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_118, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_119': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_119, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_120': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_120, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_121': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_121, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_122': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_122, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_123': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_123, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_124': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_124, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_125': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_125, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_126': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_126, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_127': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_127, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_128': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_128, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_129': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_129, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_130': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_130, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_131': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_131, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_132': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_132, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_133': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_133, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_134': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_134, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_135': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_135, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_136': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_136, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_137': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_137, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_138': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_138, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_139': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_139, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_140': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_140, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_141': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_141, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_142': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_142, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_143': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_143, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_144': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_144, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_145': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_145, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_146': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_146, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_147': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_147, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_148': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_148, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_149': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_149, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_150': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_150, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_151': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_151, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_152': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_152, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_153': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_153, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_154': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_154, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_155': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_155, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_156': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_156, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_157': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_157, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_158': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_158, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_159': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_159, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_160': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_160, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_161': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_161, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_162': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_162, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_163': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_163, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_164': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_164, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_165': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_165, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_166': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_166, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_167': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_167, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_168': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_168, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_169': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_169, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_170': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_170, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_171': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_171, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_172': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_172, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_173': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_173, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_174': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_174, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_175': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_175, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_176': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_176, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_177': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_177, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_178': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_178, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_179': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_179, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_180': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_180, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_181': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_181, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_182': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_182, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_183': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_183, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_184': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_184, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_185': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_185, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_186': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_186, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_187': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_187, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_188': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_188, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_189': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_189, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_190': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_190, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_191': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_191, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_192': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_192, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_193': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_193, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_194': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_194, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_195': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_195, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_196': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_196, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_197': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_197, 
        'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_198': xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_198
    }
    xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_create_and_activate_hotspot'

    async def _async_update_hotspot_config(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        args = [old_ssid, new_ssid, new_password, security]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_orig(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_1(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "XXwpa-pskXX",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_2(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "WPA-PSK",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_3(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = None

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_4(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active or self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_5(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(None, exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_6(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", None)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_7(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_8(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", )
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_9(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("XXPre-hotspot-update disconnect ignored: %sXX", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_10(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_11(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("PRE-HOTSPOT-UPDATE DISCONNECT IGNORED: %S", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_12(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = None

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_13(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = True

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_14(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = None
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_15(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(None)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_16(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                None,
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_17(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                None,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_18(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                None,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_19(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_20(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_21(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_22(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "XXCleaned up %d old hotspot profiles for '%s'XX",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_23(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_24(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "CLEANED UP %D OLD HOTSPOT PROFILES FOR '%S'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_25(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.upper() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_26(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() == old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_27(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.upper():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_28(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(None)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_29(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = None
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_30(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(None)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_31(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = None
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_32(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = None
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_33(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = None
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_34(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                None,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_35(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                None,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_36(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                None,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_37(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_38(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_39(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_40(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    None,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_41(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    None,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_42(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    None,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_43(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_44(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_45(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_46(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    None
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_47(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        None,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_48(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        None,
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_49(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_50(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_51(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        False,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_52(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error(None, exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_53(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", None)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_54(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error(exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_55(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", )
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_56(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("XXHotspot config update failed: %sXX", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_57(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_58(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("HOTSPOT CONFIG UPDATE FAILED: %S", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_59(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_60(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(None, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_61(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, None, "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_62(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), None)
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_63(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_64(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_65(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), )
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_66(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(True, str(exc), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_67(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(None), "hotspot_config_failed")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_68(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "XXhotspot_config_failedXX")
            )

    async def xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_69(
        self,
        old_ssid: str,
        new_ssid: str,
        new_password: str,
        security: str = "wpa-psk",
    ) -> None:
        """Update hotspot SSID/password and re-activate if the hotspot was running."""
        try:
            was_active = self._is_hotspot_active

            if was_active and self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Pre-hotspot-update disconnect ignored: %s", exc)
                self._is_hotspot_active = False

            await self._delete_all_ap_mode_connections()
            deleted_old = await self._delete_connections_by_id(old_ssid)
            logger.debug(
                "Cleaned up %d old hotspot profiles for '%s'",
                deleted_old,
                old_ssid,
            )

            if new_ssid.lower() != old_ssid.lower():
                await self._delete_connections_by_id(new_ssid)

            validated_sec = (
                security
                if HotspotSecurity.is_valid(security)
                else HotspotSecurity.WPA2_PSK.value
            )
            self._hotspot_config.ssid = new_ssid
            self._hotspot_config.password = new_password
            self._hotspot_config.security = validated_sec
            self._save_hotspot_config()

            self.hotspot_info_ready.emit(
                new_ssid,
                new_password,
                self._hotspot_config.security,
            )

            if was_active:
                await self._async_create_and_activate_hotspot(
                    new_ssid,
                    new_password,
                    self._hotspot_config.security,
                )
            else:
                self.connection_result.emit(
                    ConnectionResult(
                        True,
                        f"Hotspot config updated to '{new_ssid}'",
                    )
                )
        except Exception as exc:
            logger.error("Hotspot config update failed: %s", exc)
            self.connection_result.emit(
                ConnectionResult(False, str(exc), "HOTSPOT_CONFIG_FAILED")
            )
    
    xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_1': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_2': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_3': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_4': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_5': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_6': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_7': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_8': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_9': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_10': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_11': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_12': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_13': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_14': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_15': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_16': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_17': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_18': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_19': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_20': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_21': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_22': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_23': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_24': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_25': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_26': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_27': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_28': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_29': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_30': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_31': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_32': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_33': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_34': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_35': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_36': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_37': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_38': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_39': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_40': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_41': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_42': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_43': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_44': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_45': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_46': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_47': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_48': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_49': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_50': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_51': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_52': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_53': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_54': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_55': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_56': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_57': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_58': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_59': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_60': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_61': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_62': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_63': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_64': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_65': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_66': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_67': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_68': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_69': xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_69
    }
    xǁNetworkManagerWorkerǁ_async_update_hotspot_config__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_update_hotspot_config'

    async def _async_toggle_hotspot(self, enable: bool) -> None:
        args = [enable]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_orig(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_1(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    None,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_2(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    None,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_3(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_4(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_5(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = None
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_6(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = None
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_7(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = True
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_8(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(None, exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_9(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", None)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_10(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug(exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_11(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", )

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_12(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("XXHotspot-off disconnect ignored: %sXX", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_13(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_14(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("HOTSPOT-OFF DISCONNECT IGNORED: %S", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_15(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = None
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_16(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(None)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_17(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug(None, deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_18(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", None)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_19(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug(deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_20(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", )

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_21(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("XXHotspot OFF: cleaned up %d profile(s)XX", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_22(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("hotspot off: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_23(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("HOTSPOT OFF: CLEANED UP %D PROFILE(S)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_24(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active or await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_25(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(None)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_26(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(True)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_27(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(None)
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_28(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(None, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_29(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, None))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_30(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult("Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_31(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, ))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_32(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(False, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_33(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "XXHotspot disabledXX"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_34(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_35(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "HOTSPOT DISABLED"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_36(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(None)
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_37(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(None, exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_38(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", None)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_39(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error(exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_40(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", )
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_41(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("XXFailed to toggle hotspot: %sXX", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_42(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_43(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("FAILED TO TOGGLE HOTSPOT: %S", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_44(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = None
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_45(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = True
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_46(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                None
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_47(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=None,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_48(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=None,
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_49(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code=None,
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_50(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_51(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_52(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_53(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=True,
                    message=str(exc),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_54(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(None),
                    error_code="hotspot_toggle_failed",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_55(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="XXhotspot_toggle_failedXX",
                )
            )

    async def xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_56(self, enable: bool) -> None:
        """Enable or disable the hotspot, cleaning up profiles and Wi-Fi radio state."""
        try:
            if enable:
                await self._async_create_and_activate_hotspot(
                    self._hotspot_config.ssid,
                    self._hotspot_config.password,
                )
                return

            was_hotspot_active = self._is_hotspot_active
            self._is_hotspot_active = False
            if self._primary_wifi_path:
                try:
                    await self._wifi().disconnect()
                except Exception as exc:
                    logger.debug("Hotspot-off disconnect ignored: %s", exc)

            deleted = await self._delete_connections_by_id(self._hotspot_config.ssid)
            logger.debug("Hotspot OFF: cleaned up %d profile(s)", deleted)

            if was_hotspot_active and await self._nm().wireless_enabled:
                await self._nm().wireless_enabled.set_async(False)

            self.connection_result.emit(ConnectionResult(True, "Hotspot disabled"))
            self.state_changed.emit(await self._build_current_state())
        except Exception as exc:
            logger.error("Failed to toggle hotspot: %s", exc)
            self._is_hotspot_active = False
            self.connection_result.emit(
                ConnectionResult(
                    success=False,
                    message=str(exc),
                    error_code="HOTSPOT_TOGGLE_FAILED",
                )
            )
    
    xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_1': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_2': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_3': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_4': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_5': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_6': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_7': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_8': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_9': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_10': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_11': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_12': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_13': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_14': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_15': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_16': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_17': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_18': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_19': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_20': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_21': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_22': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_23': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_24': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_25': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_26': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_27': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_28': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_29': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_30': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_31': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_32': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_33': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_34': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_35': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_36': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_37': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_38': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_39': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_40': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_41': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_42': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_43': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_44': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_45': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_46': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_47': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_48': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_49': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_50': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_51': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_52': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_53': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_54': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_55': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_56': xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_56
    }
    xǁNetworkManagerWorkerǁ_async_toggle_hotspot__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_async_toggle_hotspot'

    async def _merge_wifi_secrets(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        args = [conn_settings, props]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_orig(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_1(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = None
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_2(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets(None)
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_3(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("XX802-11-wireless-securityXX")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_4(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-WIRELESS-SECURITY")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_5(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = None
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_6(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "XX802-11-wireless-securityXX"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_7(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-WIRELESS-SECURITY"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_8(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key not in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_9(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(None)
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_10(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(None, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_11(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, None).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_12(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault({}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_13(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, ).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_14(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug(None, exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_15(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", None)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_16(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug(exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_17(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("Could not fetch Wi-Fi secrets (NM may redact): %s", )

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_18(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("XXCould not fetch Wi-Fi secrets (NM may redact): %sXX", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_19(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("could not fetch wi-fi secrets (nm may redact): %s", exc)

    async def xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_20(
        self,
        conn_settings: dbus_nm.NetworkConnectionSettings,
        props: dict,
    ) -> None:
        """Fetch Wi-Fi secrets from NM and merge them into *props* in place.

        Required before calling update() so that the PSK is re-included;
        NM redacts secrets from get_settings() responses.
        """
        try:
            secrets = await conn_settings.get_secrets("802-11-wireless-security")
            sec_key = "802-11-wireless-security"
            if sec_key in secrets:
                props.setdefault(sec_key, {}).update(secrets[sec_key])
        except Exception as exc:
            logger.debug("COULD NOT FETCH WI-FI SECRETS (NM MAY REDACT): %S", exc)
    
    xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_1': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_2': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_3': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_4': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_5': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_6': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_7': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_8': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_9': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_10': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_11': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_12': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_13': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_14': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_15': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_16': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_17': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_18': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_19': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_20': xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_20
    }
    xǁNetworkManagerWorkerǁ_merge_wifi_secrets__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_merge_wifi_secrets'

    async def _deactivate_connection_by_id(self, conn_id: str) -> bool:
        args = [conn_id]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_orig(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_1(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = None
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_2(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = None
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_3(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(None).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_4(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = None
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_5(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(None).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_6(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = None
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_7(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get(None, (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_8(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", None)[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_9(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get((None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_10(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", )[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_11(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get(None, {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_12(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", None).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_13(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get({}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_14(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", ).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_15(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("XXconnectionXX", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_16(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("CONNECTION", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_17(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("XXidXX", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_18(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("ID", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_19(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, "XXXX"))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_20(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[2]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_21(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid != conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_22(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(None)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_23(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            None,
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_24(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            None,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_25(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_26(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_27(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "XXDeactivated active connection '%s'XX",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_28(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_29(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "DEACTIVATED ACTIVE CONNECTION '%S'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_30(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return False
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_31(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        None, exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_32(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", None
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_33(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_34(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_35(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "XXSkipping connection during deactivation lookup: %sXX", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_36(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_37(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "SKIPPING CONNECTION DURING DEACTIVATION LOOKUP: %S", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_38(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug(None, conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_39(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", None, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_40(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, None)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_41(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug(conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_42(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_43(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, )
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_44(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("XXError deactivating '%s': %sXX", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_45(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("error deactivating '%s': %s", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_46(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("ERROR DEACTIVATING '%S': %S", conn_id, exc)
        return False

    async def xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_47(self, conn_id: str) -> bool:
        """Deactivate the first active connection whose profile id matches *conn_id*."""
        try:
            active_paths = await self._nm().active_connections
            for active_path in active_paths:
                try:
                    conn_path = await self._active_conn(active_path).connection
                    settings = await self._conn_settings(conn_path).get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await self._nm().deactivate_connection(active_path)
                        logger.debug(
                            "Deactivated active connection '%s'",
                            conn_id,
                        )
                        return True
                except Exception as exc:
                    logger.debug(
                        "Skipping connection during deactivation lookup: %s", exc
                    )
        except Exception as exc:
            logger.debug("Error deactivating '%s': %s", conn_id, exc)
        return True
    
    xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_1': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_2': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_3': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_4': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_5': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_6': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_7': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_8': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_9': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_10': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_11': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_12': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_13': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_14': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_15': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_16': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_17': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_18': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_19': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_20': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_21': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_22': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_23': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_24': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_25': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_26': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_27': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_28': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_29': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_30': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_31': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_32': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_33': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_34': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_35': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_36': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_37': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_38': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_39': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_40': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_41': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_42': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_43': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_44': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_45': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_46': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_47': xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_47
    }
    xǁNetworkManagerWorkerǁ_deactivate_connection_by_id__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_deactivate_connection_by_id'

    async def _delete_all_connections_by_id(self, conn_id: str) -> int:
        args = [conn_id]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_orig(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_1(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = None
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_2(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 1
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_3(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = None
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_4(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = None
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_5(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(None)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_6(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = None
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_7(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = None
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_8(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get(None, (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_9(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", None)[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_10(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get((None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_11(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", )[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_12(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get(None, {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_13(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", None).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_14(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get({}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_15(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", ).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_16(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("XXconnectionXX", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_17(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("CONNECTION", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_18(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("XXidXX", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_19(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("ID", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_20(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, "XXXX"))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_21(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[2]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_22(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid != conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_23(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted = 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_24(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted -= 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_25(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 2
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_26(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        None, conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_27(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", None, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_28(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, None
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_29(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_30(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_31(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_32(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "XXSkipping connection in cleanup for '%s': %sXX", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_33(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_34(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "SKIPPING CONNECTION IN CLEANUP FOR '%S': %S", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_35(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error(None, conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_36(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", None, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_37(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, None)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_38(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error(conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_39(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_40(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("Cleanup for '%s' failed: %s", conn_id, )
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_41(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("XXCleanup for '%s' failed: %sXX", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_42(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("cleanup for '%s' failed: %s", conn_id, exc)
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_43(self, conn_id: str) -> int:
        """Delete every NM connection profile whose id exactly matches *conn_id*."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    cid = settings.get("connection", {}).get("id", (None, ""))[1]
                    if cid == conn_id:
                        await cs.delete()
                        deleted += 1
                except Exception as exc:
                    logger.debug(
                        "Skipping connection in cleanup for '%s': %s", conn_id, exc
                    )
        except Exception as exc:
            logger.error("CLEANUP FOR '%S' FAILED: %S", conn_id, exc)
        return deleted
    
    xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_1': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_2': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_3': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_4': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_5': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_6': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_7': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_8': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_9': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_10': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_11': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_12': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_13': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_14': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_15': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_16': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_17': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_18': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_19': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_20': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_21': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_22': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_23': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_24': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_25': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_26': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_27': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_28': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_29': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_30': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_31': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_32': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_33': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_34': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_35': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_36': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_37': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_38': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_39': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_40': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_41': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_42': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_43': xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_43
    }
    xǁNetworkManagerWorkerǁ_delete_all_connections_by_id__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_delete_all_connections_by_id'

    async def _delete_all_ap_mode_connections(self) -> int:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_orig(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_1(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = None
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_2(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 1
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_3(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = None
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_4(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = None
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_5(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(None)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_6(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = None
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_7(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = None
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_8(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get(None, (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_9(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", None)[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_10(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get((None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_11(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", )[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_12(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get(None, {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_13(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", None).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_14(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get({}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_15(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", ).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_16(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("XXconnectionXX", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_17(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("CONNECTION", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_18(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("XXtypeXX", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_19(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("TYPE", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_20(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, "XXXX"))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_21(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        2
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_22(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type == "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_23(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "XX802-11-wirelessXX":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_24(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-WIRELESS":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_25(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        break
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_26(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = None
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_27(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get(None, (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_28(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", None)[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_29(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get((None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_30(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", )[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_31(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get(None, {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_32(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", None).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_33(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get({}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_34(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", ).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_35(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("XX802-11-wirelessXX", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_36(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-WIRELESS", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_37(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("XXmodeXX", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_38(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("MODE", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_39(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, "XXXX"))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_40(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        2
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_41(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode != "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_42(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "XXapXX":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_43(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "AP":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_44(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = None
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_45(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get(None, (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_46(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", None)[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_47(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get((None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_48(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", )[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_49(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get(None, {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_50(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", None).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_51(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get({}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_52(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", ).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_53(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("XXconnectionXX", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_54(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("CONNECTION", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_55(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("XXidXX", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_56(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("ID", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_57(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, "XXXX"))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_58(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            2
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_59(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted = 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_60(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted -= 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_61(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 2
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_62(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            None, conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_63(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", None, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_64(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, None
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_65(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_66(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_67(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_68(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "XXRemoved stale AP profile '%s' at %sXX", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_69(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "removed stale ap profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_70(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "REMOVED STALE AP PROFILE '%S' AT %S", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_71(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug(None, exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_72(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", None)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_73(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug(exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_74(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", )
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_75(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("XXSkipping connection in AP profile cleanup: %sXX", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_76(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("skipping connection in ap profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_77(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("SKIPPING CONNECTION IN AP PROFILE CLEANUP: %S", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_78(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error(None, exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_79(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", None)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_80(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error(exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_81(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("Failed to remove stale AP profiles: %s", )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_82(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("XXFailed to remove stale AP profiles: %sXX", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_83(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("failed to remove stale ap profiles: %s", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_84(self) -> int:
        """Delete all saved Wi-Fi connections in AP mode.

        Called before creating a new hotspot to remove stale profiles from
        previous hotspot sessions, regardless of their SSID.  Without this,
        old AP-mode profiles accumulate in NetworkManager and NM may
        auto-activate them on the next boot.
        """
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_type = settings.get("connection", {}).get("type", (None, ""))[
                        1
                    ]
                    if conn_type != "802-11-wireless":
                        continue
                    mode = settings.get("802-11-wireless", {}).get("mode", (None, ""))[
                        1
                    ]
                    if mode == "ap":
                        conn_id = settings.get("connection", {}).get("id", (None, ""))[
                            1
                        ]
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Removed stale AP profile '%s' at %s", conn_id, conn_path
                        )
                except Exception as exc:
                    logger.debug("Skipping connection in AP profile cleanup: %s", exc)
        except Exception as exc:
            logger.error("FAILED TO REMOVE STALE AP PROFILES: %S", exc)
        if deleted:
            self._invalidate_saved_cache()
        return deleted
    
    xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_1': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_2': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_3': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_4': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_5': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_6': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_7': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_8': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_9': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_10': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_11': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_12': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_13': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_14': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_15': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_16': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_17': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_18': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_19': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_20': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_21': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_22': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_23': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_24': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_25': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_26': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_27': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_28': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_29': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_30': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_31': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_32': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_33': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_34': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_35': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_36': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_37': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_38': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_39': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_40': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_41': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_42': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_43': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_44': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_45': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_46': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_47': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_48': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_49': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_50': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_51': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_52': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_52, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_53': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_53, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_54': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_54, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_55': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_55, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_56': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_56, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_57': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_57, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_58': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_58, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_59': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_59, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_60': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_60, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_61': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_61, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_62': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_62, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_63': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_63, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_64': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_64, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_65': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_65, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_66': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_66, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_67': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_67, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_68': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_68, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_69': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_69, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_70': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_70, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_71': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_71, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_72': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_72, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_73': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_73, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_74': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_74, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_75': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_75, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_76': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_76, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_77': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_77, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_78': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_78, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_79': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_79, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_80': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_80, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_81': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_81, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_82': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_82, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_83': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_83, 
        'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_84': xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_84
    }
    xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_delete_all_ap_mode_connections'

    async def _delete_connections_by_id(self, ssid: str) -> int:
        args = [ssid]# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_orig'), object.__getattribute__(self, 'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_mutants'), args, kwargs, self)

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_orig(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_1(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = None
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_2(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 1
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_3(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = None
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_4(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = None
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_5(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(None)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_6(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = None
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_7(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = None
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_8(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get(None, (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_9(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", None)[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_10(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get((None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_11(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", )[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_12(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get(None, {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_13(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", None).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_14(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get({}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_15(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", ).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_16(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("XXconnectionXX", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_17(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("CONNECTION", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_18(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("XXidXX", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_19(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("ID", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_20(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, "XXXX"))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_21(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[2]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_22(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.upper() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_23(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() != ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_24(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.upper():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_25(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted = 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_26(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted -= 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_27(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 2
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_28(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            None,
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_29(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            None,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_30(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            None,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_31(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_32(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_33(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_34(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "XXDeleted stale profile '%s' at %sXX",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_35(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_36(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "DELETED STALE PROFILE '%S' AT %S",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_37(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        None,
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_38(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        None,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_39(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        None,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_40(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_41(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_42(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_43(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "XXSkip connection %s during cleanup: %sXX",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_44(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_45(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "SKIP CONNECTION %S DURING CLEANUP: %S",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_46(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                None,
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_47(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                None,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_48(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_49(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "Failed to enumerate connections for cleanup: %s",
                )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_50(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "XXFailed to enumerate connections for cleanup: %sXX",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_51(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "failed to enumerate connections for cleanup: %s",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted

    async def xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_52(self, ssid: str) -> int:
        """Delete every NM connection profile whose id matches *ssid* (case-insensitive)."""
        deleted = 0
        try:
            connections = await self._nm_settings().list_connections()
            for conn_path in connections:
                try:
                    cs = self._conn_settings(conn_path)
                    settings = await cs.get_settings()
                    conn_id = settings.get("connection", {}).get("id", (None, ""))[1]
                    if conn_id.lower() == ssid.lower():
                        await cs.delete()
                        deleted += 1
                        logger.debug(
                            "Deleted stale profile '%s' at %s",
                            conn_id,
                            conn_path,
                        )
                except Exception as exc:
                    logger.debug(
                        "Skip connection %s during cleanup: %s",
                        conn_path,
                        exc,
                    )
        except Exception as exc:
            logger.error(
                "FAILED TO ENUMERATE CONNECTIONS FOR CLEANUP: %S",
                exc,
            )
        if deleted:
            self._invalidate_saved_cache()
        return deleted
    
    xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_1': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_1, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_2': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_2, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_3': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_3, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_4': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_4, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_5': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_5, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_6': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_6, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_7': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_7, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_8': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_8, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_9': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_9, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_10': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_10, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_11': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_11, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_12': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_12, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_13': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_13, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_14': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_14, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_15': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_15, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_16': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_16, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_17': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_17, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_18': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_18, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_19': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_19, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_20': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_20, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_21': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_21, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_22': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_22, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_23': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_23, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_24': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_24, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_25': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_25, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_26': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_26, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_27': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_27, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_28': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_28, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_29': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_29, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_30': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_30, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_31': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_31, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_32': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_32, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_33': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_33, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_34': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_34, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_35': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_35, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_36': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_36, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_37': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_37, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_38': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_38, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_39': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_39, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_40': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_40, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_41': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_41, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_42': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_42, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_43': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_43, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_44': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_44, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_45': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_45, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_46': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_46, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_47': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_47, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_48': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_48, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_49': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_49, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_50': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_50, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_51': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_51, 
        'xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_52': xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_52
    }
    xǁNetworkManagerWorkerǁ_delete_connections_by_id__mutmut_orig.__name__ = 'xǁNetworkManagerWorkerǁ_delete_connections_by_id'

    @staticmethod
    def _ip_to_nm_uint32(ip_str: str) -> int:
        """Convert a dotted-decimal IPv4 string to a native-endian uint32 for NM."""
        return struct.unpack("=I", ipaddress.IPv4Address(ip_str).packed)[0]

    @staticmethod
    def _nm_uint32_to_ip(uint_ip: int) -> str:
        """Convert a native-endian uint32 from NM back to a dotted-decimal IPv4 string."""
        return str(ipaddress.IPv4Address(struct.pack("=I", uint_ip)))

    @staticmethod
    def _mask_to_prefix(mask_str: str) -> int:
        """Convert a subnet mask or CIDR prefix string to an integer prefix length."""
        stripped = mask_str.strip()
        if stripped.isdigit():
            prefix = int(stripped)
            if 0 <= prefix <= 32:
                return prefix
            raise ValueError(f"CIDR prefix out of range: {prefix}")
        return bin(int(ipaddress.IPv4Address(stripped))).count("1")
